# continuation-gate fixture: a project with no lifecycle

Deliberately empty of `.claude/expert/ledger.json`. This directory is the `cwd` for the
case that proves the Stop continuation gate is inert in ordinary sessions — the property
that keeps it from recreating the friction the 2026-07-22 out-of-scope ruling rejected.

This README exists only so git tracks the directory; the gate's behavior depends on the
ABSENCE of the ledger, so the directory must never gain one.
