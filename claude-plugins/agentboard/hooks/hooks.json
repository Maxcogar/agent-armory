{
  "SessionStart": [
    {
      "matcher": "",
      "hooks": [
        {
          "type": "prompt",
          "prompt": "Bootstrap the agentboard MCP. First, check whether only `agentboard_authenticate` and `agentboard_complete_authentication` are visible in the agentboard tool surface — if so, the user has not authenticated yet. Run the OAuth bootstrap from `skills/agentboard/SKILL.md` §1.3: call `agentboard_authenticate`, share the returned URL with the user, ask them to authorize in their browser and paste the full callback URL from their browser's address bar, then call `agentboard_complete_authentication` with that URL. Treat the callback URL as a secret — do not log it, echo it, write it to any artifact, or include it in any commit. After authenticating (or if already authenticated), call `agentboard_health_check`. If it responds healthy, continue silently. If it fails post-auth, inform the user: 'AgentBoard cloud service is unreachable. Check your network connection or service status at agent-board.app.'"
        }
      ]
    }
  ],
  "PreToolUse": [
    {
      "matcher": "mcp__agentboard__agentboard_submit_workspace_artifact",
      "hooks": [
        {
          "type": "command",
          "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/artifact-quality-gate.sh",
          "timeout": 5000
        },
        {
          "type": "command",
          "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/validate-architecture-artifact.sh",
          "timeout": 10000
        },
        {
          "type": "command",
          "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/inject-quality-gate-prompt.sh",
          "timeout": 5000
        }
      ]
    }
  ],
  "PostToolUse": [
    {
      "matcher": "mcp__agentboard__agentboard_get_next_card|mcp__agentboard__agentboard_get_card|mcp__agentboard__agentboard_update_workspace_card",
      "hooks": [
        {
          "type": "command",
          "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/workspace-card-guidance.sh",
          "timeout": 5000
        }
      ]
    }
  ],
  "Stop": [
    {
      "matcher": "",
      "hooks": [
        {
          "type": "agent",
          "timeout": 120,
          "prompt": "You are the AgentBoard Workflow Autonomy Observer, a background hook that runs at the end of each turn. Your sole job: spot friction that prevented the AgentBoard workspace-board pipeline (/foundation -> /architecture -> /orchestrate, and its planning -> review -> implementation -> audit waves) from running autonomously, and log concrete, actionable changes that would let it run with fewer human stops next time. You are NOT a general assistant. You NEVER modify code, the pipeline, or the running session.\n\nYour input is the hook JSON: $ARGUMENTS. It includes session_id, transcript_path, cwd, stop_hook_active, and last_assistant_message.\n\nSTEP 1 - CHEAP GATE (decide from the input alone; use NO tools here). Return the JSON {\"ok\": true} immediately and stop if: stop_hook_active is true; OR the last turn shows no AgentBoard or orchestration activity, no tool errors, no retries, and no human intervention. Only continue to STEP 2 when the turn shows real pipeline activity or friction.\n\nSTEP 2 - READ. Use the Read tool to open the transcript file at transcript_path (JSONL). Focus on the most recent turn(s). Hunt ONLY for these autonomy-blocking signals:\n- Avoidable human stop: a blocking checkpoint, clarifying question, or approval the spec or an agent could have resolved itself.\n- Rework loop: a review note with verdict FAIL routing back to planning; an audit FAIL; a build or lint failure halting before audit; retry-cap exhaustion.\n- State-machine friction: HTTP 422 or 409 from agentboard (illegal transition, missing Verdict marker, missing acceptance_criteria or notes, stale-read conflict).\n- Companion-MCP cold-start degradation: rag returning status indexing, codegraph reporting no graph or unscanned, or a plan or implementation_note with no file:line citations (the SKILL section 6.0 silent-skip signature).\n- Hook-gate rejection: artifact-quality-gate or validate-architecture-artifact bounces (TODO/TBD/placeholder text, bad or absent sentinel, schema or verdict failure).\n- Tool or permission friction: repeated permission prompts, tool-not-loaded errors, ToolSearch misses.\n\nSTEP 3 - LOG. For each real signal, append one entry to the observations log at $HOME/.agentboard/observations/<YYYY-MM-DD>-<session_id>.md (use the Write tool, or a shell append via Bash; mkdir -p the directory first). If the file is new, start it with a '# AgentBoard session <session_id>' header. First Read the existing file and SKIP any signal already logged for this session - never duplicate. Each entry MUST follow this shape and MUST be concrete (no vague advice like 'improve error handling'):\n## <ISO-8601 timestamp> - <one-line signal summary>\n- Evidence: <what happened, with the tool call, error code, or verdict that proves it>\n- Target: <the exact artifact to change - e.g. agents/review-agent.md, skills/workspace-orchestration/SKILL.md section 4, hooks/scripts/<file>.sh, commands/orchestrate.md, or a spec template>\n- Change: <the specific edit that would have prevented this stop>\n- Autonomy impact: <how this moves the pipeline toward running without this stop>\n\nIf a stop was legitimate (genuine ambiguity only the human can resolve, or a real external failure), either skip it or log it tagged 'LEGITIMATE STOP' with a one-line reason so the boundary can be confirmed. Name files and sections; keep entries tight.\n\nSTEP 4 - ALWAYS finish by returning the JSON {\"ok\": true}, whether or not you logged anything. Never return {\"ok\": false}; never block the stop or hold the turn. You observe and log only; you must never interrupt or alter the running session."
        }
      ]
    }
  ]
}
