# Expert review — `docs/plans/plan-phase-a.md`, round 5

**Date:** 2026-09-07
**Reviewer:** independent `/expert-review` pass (expert-review SKILL.md R1.2, Post-fix protocol). Not the plan's author, not the author of any prior review, not the author of `2026-09-07-round-5-author-gates-review.md`. The prior rounds reached this reviewer only as the written records named below; every closure row in the author's self-check was treated as a claim and re-derived from the working tree, and where the self-check and the source disagree the source is used.
**Artifact:** `middleware/context-oracle/docs/plans/plan-phase-a.md` as installed in the working tree at commit `c75faf8` (`git status` clean at every read; 8,507 lines; 16 sections; 40 steps each opening with a `step-decl` block; 122 test specifications; D-plan-1…28 with §10A entries; Q1…Q52; §14.4 passes 1–7; 17 probes under `docs/plans/plan-phase-a.probes/`). Graded against the contract revision installed at `c75faf8` (`.claude/skills/expert-plan/references/output-contract.md`, the revision that requires step declarations, `provides:` for exported names, the build-order and shared-call-identifier checks, section-scoped and load-repeated probes, and the `--impact` re-derivation walk).
**Round:** 5 — the last round the owner's rule allows. Round 4 = `docs/reviews/2026-09-07-round-4-expert-review.md` (20 findings: S1…S6 with SY-1 as S1's systemic form, M1…M3, m1…m11; tentative T1…T3) + `docs/reviews/2026-09-07-round-4-collapse-hunt.md` (22 findings: S-1, S-2, M-1…M-8, m-1…m-12; N-A…N-M map onto them). Together 42. The round-4 artifact is commit `6a2159b`, extracted to `<scratchpad>/plan-6a2159b-er.md` (8,160 lines) and read at every passage a provenance decision needed.

Every location is `docs/plans/plan-phase-a.md:start-end` unless another path is given; `arch` abbreviates `docs/architecture-phase-a.md`, `spec` abbreviates `docs/specs/spec-context-oracle.md`, `r4` abbreviates the `6a2159b` revision (evidence prose only, never a Location). Every literal-content claim below was Read at the cited lines in this session; every execution claim states the command and what it printed.

**Regression tagging rule used below.** A finding is tagged **REGRESSION (introduced by the round-4 corrections)** only where I verified the defect is absent at `6a2159b` and exists now because of a round-4 correction — *(text)* when the changed text is itself wrong, *(consistency)* when a correction moved a referent and left an unchanged sibling sentence now contradicting it. A defect present verbatim at `6a2159b` that no prior round reported is tagged **new**; a prior finding closed at its named instance but standing at another is **recurring**.

---

## Scope and Inventory

### Round and sources of the post-fix inventory

Per SKILL.md Step 2: the prior reviews' full inventory, the fix-diff (`6a2159b` → `c75faf8`: the plan, the probe directory, the new trace file `2026-09-07-plan-tool-traces-3.md`, the revised skill copy, the author's `2026-09-07-round-5-author-gates-review.md`), their dependents (none — the project's one code file, `tools/check_docs.py`, has zero dependents; `ls -a middleware/context-oracle/` this session: `.claude .mcp.json CLAUDE.md OWNER-LEDGER.md RETHINK.md docs tools`), and the 42 prior findings as closure items.

### Files in scope — every file below is `[x]`

- [x] `docs/plans/plan-phase-a.md` — **Read in full**, lines 1–8507 in fourteen contiguous reads (1–700, 700–1350, 1350–2000, 2000–2650, 2650–3300, 3300–3950, 3950–4600, 4600–5250, 5250–5900, 5900–6550, 6550–7200, 7200–7850, 7850–8507). Every step 1–40 walked in build order against its declaration as a dry-run implementer; every one of the 122 specifications read against `references/testing-standards.md`; every §11 claim read against its cited source lines; every §10 decision read against its trace label; §14.4's pass record read against the register and §10.
- [x] `<scratchpad>/plan-6a2159b-er.md` (`git show 6a2159b:…`, 8,160 lines) — Read at: Step 14 (r4:1975–2050), T-14-1 (r4:6284–6300), Step 26 (r4:2921–2980), the S13/S14/S15 declarations (r4:1892–1900, 1963–1971, 2059–2067), Step 11's `trust.ts` (r4:1715, 1737, 1741), Step 28's verb sentences (r4:3099, 3129), T-3-3 (r4:5978–5984), Step 31 item 1 (r4 Step 31 lines 18–20), Step 32's Source and verb (r4:3480, 3495, 3517), Step 33's "run at" (r4:3592), Step 27 (r4:3007), the `schema_meta` key list (r4:1329–1331), the miner format (r4:1906), §10A D-plan-13 (r4:5011), T-38-25 (r4:6417), the §11.4 sqlite entry (r4:5530).
- [x] `docs/reviews/2026-09-07-round-4-expert-review.md` — Read in full (406 lines). Provenance for closure items S1–S6/SY-1, M1–M3, m1–m11, T1–T3.
- [x] `docs/reviews/2026-09-07-round-4-collapse-hunt.md` — Read in full (810 lines). Provenance for S-1, S-2, M-1…M-8, m-1…m-12, N-A…N-M.
- [x] `docs/reviews/2026-09-07-round-5-author-gates-review.md` — Read in full (111 lines). Treated as claims; every row of its §2 table re-derived below.
- [x] `docs/reviews/2026-09-07-plan-tool-traces.md` — labels enumerated (`[D1 build-order]` 18, `[D2 test-execution]` 16, `[D2b typescript-pin]` 3, `[D3 exit-run]` 10, `[D4 settings-marker]` 13, `[D5 model-seam]` 13, `[D6 lag-window]` 8, `[D7 thresholds]` 8, `[D8 recursion-guard]` 8, `[D9 hooks-timeout-drift]` 8, `[D10 L11-verifications]` 8; 99 `sequential_thinking` + 14 `decision_framework` calls).
- [x] `docs/reviews/2026-09-07-plan-tool-traces-2.md` — labels enumerated: D-plan-2, 4, 5, 6, 8, 10, 13, 15–26 (112 + 10 calls).
- [x] `docs/reviews/2026-09-07-plan-tool-traces-3.md` — labels enumerated: D-plan-5 amended, D-plan-24 re-derived, D-plan-26 re-derived, D-plan-27, D-plan-28 (31 + 4 calls); the D-plan-24 and D-plan-26 chains Read in full (lines 31–110, 210–300).
- [x] `docs/plans/plan-phase-a.probes/*` — every script (01–17) and every `expected/*.txt` Read in full; `layout/prepare.sh` Read.
- [x] `.claude/skills/expert-plan/SKILL.md` — Read in full (391 lines).
- [x] `.claude/skills/expert-plan/references/output-contract.md` — Read in full (134 lines).
- [x] `.claude/skills/expert-plan/references/testing-standards.md` — Read in full (141 lines).
- [x] `.claude/skills/expert-plan/scripts/derive-plan-sections.mjs` — Read at the header (1–140) and the build-order / shared-identifier / phantom-path checks (400–620); `--check` and `--self-check` executed. `.claude/skills/expert-plan/scripts/run-plan-probes.mjs` — Read in full (146 lines).
- [x] `CLAUDE.md` (project) — Read in full (238 lines). `OWNER-LEDGER.md` — Read in full (80 lines).
- [x] `docs/specs/spec-context-oracle.md` — Read in full (1,139 lines). `docs/architecture-phase-a.md` — Read in full (2,058 lines).
- [x] `docs/collapse-log.md` — headings listed; Read `:1121–1242` (the 2026-09-04 and 2026-09-07 entries).
- [x] `docs/reviews/README.md` — Read in full.
- [x] External / executed, all this session (2026-09-07):
  - `node .claude/skills/expert-plan/scripts/derive-plan-sections.mjs docs/plans/plan-phase-a.md --check` → `OK: 40 steps, 13 elements, 122 test specs, 17 probes cited, regions current`, exit 0.
  - `… --self-check` → `self-check passed: 34 checks`, exit 0 (case list read; it includes the shared-call-identifier, action-item-names-later-step, and undeclared-path cases).
  - `… --impact 6a2159b` → 37 steps changed (S1–S7, S9–S35, S38–S40); 688 lines of restating surfaces, read against their steps for the closure table.
  - `run-plan-probes.mjs` — **three runs.** Run 1, `--layout <scratch>/probe-layout` on a directory I had created empty: `FAIL 03_tsc_exclude_fixture` (`build with exclude: exit=1`, `per-fixture bad: error TS5112`), 16 ok, exit 1. Cause established: the runner skips `layout/prepare.sh` when `--layout` is given (`run-plan-probes.mjs:81–89`), so the layout had no `node_modules`; `npx tsc` resolved to the global `/opt/node22/bin/tsc` (`tsc --version` → `Version 6.0.2`; TypeScript 5.9.3's `lib/_tsc.js` defines no `TS5112`). That failure is mine, not the plan's, and is recorded as such. Run 2, fresh layout prepared by `prepare.sh`, probes 01→02→03 in sequence: all ok. Run 3, same layout, `--repeat 2 --load 2`: all 17 ok, `all probes match their recorded expectations`, exit 0 (the optional probes 09, 12, 13, 14, 15, 17 each executed, none skipped).
  - `https://code.claude.com/docs/en/hooks` — `curl` (2,817,580 bytes), tag-stripped, split at headings, searched by section (quoted at M4 and in the AD-9/leg-2 verification).
  - **Live dry run of leg 2's premises** (the round-4 reviewers' "confirm by a dry run in the named environment"): a scratch folder `<scratchpad>/ptest` with `.claude/settings.json` hooks on `SessionStart`, `UserPromptSubmit`, `PreToolUse`, `PostToolUse`, `Stop`, `SessionEnd` appending their stdin JSON to a log; then `env -u CLAUDECODE -u CLAUDE_CODE_SESSION_ID -u CLAUDE_CODE_REMOTE_SESSION_ID -u CLAUDE_CODE_CHILD_SESSION -u CLAUDE_PID -u CLAUDE_CODE_ENTRYPOINT claude -p "Create a file named hello.txt … then reply with the single word done." --model claude-haiku-4-5-20251001 --max-turns 4 --output-format json`, and a second turn with `--resume <session_id>`. Output quoted in S1.
  - `claude --help` — the `-p`, `--permission-mode`, `--permission-prompts`, `--tools`, `--resume`, `--bare` entries (quoted in S1).
  - `mcp list_repos` (query `maxcogar`) — 41 repositories; `Maxcogar/NOVA` pushed 2026-09-04, `Maxcogar/Nova-Integrations` 2026-08-22; `has_more: false`. Matches §11.4.
  - `npm view` on `web-tree-sitter` (`0.26.13` 2026-08-23, `0.27.0` 2026-08-30, latest tail `0.27.0`), `typescript` (`7.0.2` 2026-07-08; tail `7.1.0-dev.20260907.1`), `tree-sitter-wasms@0.1.13` dependencies (`{"tree-sitter-wasms":"^0.1.11"}`), `web-tree-sitter@0.26.13` scripts (no install-phase script); `node_modules/web-tree-sitter/web-tree-sitter.d.ts:151–317` and `tree-sitter-wasms/package.json` (`"main": "bindings/node"`) in the prepared layout.
  - A scan script over the plan enumerating every backticked call-shaped identifier per step against `provides:` (output used in M2).
  - Node v22.22.2, typescript 5.9.3, npm 10.9.7 (the prepared layout).

### Tool plan (Step 3)

| Claim type | Instrument used |
|---|---|
| Literal content of the plan / spec / architecture / ledger / collapse-log / traces / probes | `Read` at file:line, recorded per finding |
| Absence claims over the plan | `grep` over the whole plan with the query and hit list recorded, then Read of the candidates |
| Declaration semantics (what `--check` does and does not measure) | Read of `derive-plan-sections.mjs:400–620`; `--check`, `--self-check`, `--impact` executed; a scan of call-shaped mentions vs `provides:` |
| Executed evidence (probes) | `run-plan-probes.mjs` executed three times as recorded above; every `expected/*.txt` compared line by line with the §11.4 entry that cites it |
| The leg-2 harness premise | executed: a `-p` session in an untrusted scratch folder with logging hooks; `--resume`; `claude --help` |
| Hooks-contract claims | `curl` of the live page, section-attributed search |
| Registry facts | `npm view`, `list_repos` |
| Prior-document claims (both round-4 reviews, the author's gates review) | re-derived from the current plan by Read/grep before any closure was recorded |
| Provenance (new / recurring / regression) | `git show 6a2159b:…`, Read at the passage |
| Structural / blast radius | not needed — a document with no code dependents |

**Unavailable instruments and disposition.** Clear Thought MCP is not exposed to this session; the metacognitive baseline and the multi-perspective check were performed manually. No Node 22.16.0 binary, no CI runner, no Docker daemon. The leg-2 dry run used haiku through the host CLI (the plan's own probe 15 does the same); no repository outside `middleware/context-oracle/` was read, and nothing in the repository was written or executed except read-only `git show`.

### Metacognitive baseline (recorded before drafting)

Known from verified reads: everything cited by line above. Inferred and therefore verified before use: whether the probe gate holds under the contract's own load condition (executed — it does, 17/17 under `--repeat 2 --load 2`, once the layout is prepared); whether my first probe failure was the plan's (executed — it was my unprepared `--layout`); whether a settings-file hook runs in a `-p` session in an untrusted folder (executed — it does, from `SessionStart`); whether a `-p` session runs a `Write` without a permission flag (executed — it does not); whether the `-p` transcript carries the human markers (executed — it does not); whether every D-plan has a trace chain (enumerated — it does); whether the `--check` sees consumption by exported name (read — only where `provides:` names it; measured which steps leave it empty). Not verifiable here and left at that status: the Node 22.16.0 floor; `unshare -rn` on the runner; whether the agent's container permits `--permission-mode acceptEdits`/`bypassPermissions` for a child `claude -p` (R13's permission-classifier class).

### Round-4 closure table (each of the 42 prior findings re-derived from the current plan; closed only against its originally named standard)

Expert-review items (20; SY-1 is S1's systemic statement and is folded into that row, as round 4 counted it):

| Prior finding | Original standard | Current state (Read) | Status |
|---|---|---|---|
| ER S1 / SY-1 / CH S-1 — `depends_on` hollow at 32 steps; `provides: []` at 33; check blind to consumption by name | expert-plan Step 8; output-contract item 7; Gate C | `depends_on` re-derived at every step (`plan:2878` S25 `[S1, S12, S21, S22, S23, S24]`; `plan:2305` S18 eight deps; `plan:3134` S28 fifteen); `provides:` non-empty at 26 steps; the contract's shared-call rule enforced (`--self-check` case "shared call identifier not provided"). **But** `provides: []` still stands at S9, S13, S15, S18, S19, S26, S27 (`plan:1587, 1945, 2119, 2303, 2407, 2982, 3064`), each of which creates functions or DAOs later steps call — the contract's own named non-compliance. | **Partially closed → recurring (M2)** |
| ER S2 — Step 28 consumed Step 30's fold/regret; `recordRegret` unwired | Step 8; Step 10; item 7 | `plan:3221–3223` item 10 finalizes only; `plan:3368` S30 `modify: [handler.ts, indexer.ts]`; `plan:3383–3386`, `3404–3407` wire both. | **Closed** |
| ER S3 / CH M-2 — T-14-2 asserted a spawn Step 14 no longer performs; handler spawned Step 32's verb | testing-standards; Gate A; round-6 lesson | `plan:6566–6583` T-14-2 re-derived (no child); `plan:3196–3199` the `index` verb is Step 28's (`src/cli/index.ts`, `provides: ctxoracle-index`); `plan:7140–7157` T-28-5 observes `.reindex.lock`. Residual: T-14-2 asserts "the spawn wrapper's audit (Step 5) is empty" — no such audit exists (m3); Step 28 still says "the single undocumented internal verb" while registering three (m4); Step 32's Source still cites AD-12/AD-13 for the verb it no longer creates (m5). | **Closed** (residuals m3, m4, m5) |
| ER S4 / CH M-6 / CH M-8 / ER m9 — acknowledgement lexicon and bare `later` held on direct answers | spec §11.5 / D-41; FR-B5; AD-9 fidelity | `plan:2709–2736` phrase-strip-then-floor, no acknowledgement vocabulary, no clause grammar; `plan:1878–1882` stoplist multi-word only, "a bare common word such as `later` is never a member"; `probe:16` records all twelve round-4 inputs clearing (`expected/16:8–18`); `plan:3001–3005` Step 26 excludes `deferral_only` only; grep `acknowledgement` → only the two sentences saying it is gone (`plan:2731`, `4816`). | **Closed** |
| ER S5 / CH S-2 / ER M5 — leg 2 rested on the inverted trust sentence, "same initialised clone", transcripts never moved | expert-plan Step 4; Gate C; CLAUDE.md rule 1; spec §11.5 | `plan:5769–5777` §11.4 quotes the settings-file rule and places the other sentence under "Hooks in skills and agents"; `probe:13` asserts both inside their sections (`expected/13:3–5`); my own fetch confirms the attribution; `plan:4136–4162` setup precedes every session, stores and transcripts local, no transfer. **But** the replacement protocol — `claude -p` conversations with tool events including an `Edit`, "the same invocation shape the model seam verified (V9)" — does not execute as written: a `-p` session denies `Write`/`Edit` without a permission mode, and the plan names none (executed, S1). | **Closed on the trust rule and collection; REGRESSED on executability (S1)** |
| ER S6 / CH M-1 — T-3-3's schedule timing-dependent; probe 07 failed under the runner | testing-standards anti-pattern 10; Gate C | `plan:6241–6254` causally forced (A reports "holding", C runs to `StoreBusy`, B's first failure is a marker file, A commits only then); `probe:07` implements exactly that signalling (`07_sqlite_busy_schedule.mjs:36–46`); passed 2/2 under `--repeat 2 --load 2` here. | **Closed** |
| ER M1 / CH m-9 — detectors read per-turn classifications the store never held | Step 8; AD-17 | `plan:1442–1448` `classified_turns` DDL; `plan:1619` DAO; `plan:2894–2897` written by catch-up; `plan:2994–3008` read by both detectors; `plan:7038–7042` T-26-1's cross-event case. Residual: on a `resume` rebuild every turn is re-recorded into a table whose PK is `(consumer, uuid)` with no stated conflict semantics (m8). | **Closed** (residual m8) |
| ER M2 / CH m-8 — `locate.ts` read `transcript_path` / recorded `agent_transcript_path` | Gate A; AD-6 | `plan:2537–2545` `locateTranscript(ev)` reads `ev.transcriptPath`; grep `agent_transcript_path` → `plan:3141`, `3154` only (the adapter's list and the scan list). | **Closed** |
| ER M3 / CH M-3 — seven steps modified earlier files with `modify: []` | item 7; §16 reconciliation | `plan:3062` S27, `3368` S30, `3453` S31, `3568` S32, `3653` S33, `3746` S34, `3807` S35 declare `modify:`; §5.1 rows `plan:352, 358, 390, 398`. | **Closed** |
| ER m1 / CH m-1 — T-38-25 "Step 1's job" | Gate C | `plan:7762–7763` "the `cold-container` CI job Step 38 adds". | **Closed** |
| ER m2 — §7 overview placed watchdog/guard in Steps 28–30 | Gate C | `plan:691–699` names Step 10 for the writers "with the watchdog and guard" and Step 29 for verification. | **Closed** |
| ER m3 — Step 10 `covers: [PA-5]` only | §2 coverage | `plan:1680` `covers: [PA-5, PA-9]`; `plan:197` generated row. | **Closed** |
| ER m4 — T-23-2 "three seeded lists and a string" | Gate C | `plan:6913–6915` "the seeded list, the seeded floor, and a string only". | **Closed** |
| ER m5 — convention sentence contradicted the narrowed list | Gate C | `plan:3227–3229` "for every identifier in the convention list above". | **Closed** |
| ER m6 / CH m-3 — negation clause untested | Step 9 | `plan:6760–6764` "Not done yet.", "This isn't finished.", "I haven't fixed it." and the earlier-clause positive. | **Closed** |
| ER m7 — network policy stated two ways | Gate C | `plan:4011` once; `plan:7767–7768` T-38-25 refers to it. | **Closed** |
| ER m8 — `status` could not read the pinned interpreter | Gate C | `plan:3489–3492` `schema_meta.pinned_interpreter`; `plan:3689–3691` `status` reads it; `plan:7341–7343` T-33-1 seeds it. **But** Step 7's `schema_meta` key list (`plan:1346–1348`) — which the author's row says was re-derived — still omits `pinned_interpreter` (and `index_stale`). | **Partially closed** (m6) |
| ER m9 / CH M-10 — lexicon members mislabelled | D-plan-7's rule | `plan:1883–1895` the command-class and completion lexicons are `plan_seed`; the one list-valued `architecture_default` is `index.ext_to_grammar`. | **Closed** |
| ER m10 / CH m-6 — slug rule unstated; second layout-knowing module | Gate C; AD-11 | `plan:2540–2544` `projectTranscriptDir(cwd)` in `locate.ts`; `plan:3680–3682` `status` calls it; `plan:5905–5913` the observation; matches the slug my own `-p` run produced. | **Closed** |
| ER m11 — Q22 not re-derived | "Still authored — re-derive" | `plan:8155–8160` re-derived from D-plan-26. | **Closed** |
| ER T1 — probe 14 verified `main`, not the 22.16.0 tag | (tentative) | `14_docker_node_git.optional.sh:5` fetches the `d073523` Dockerfile; `expected/14:1` `ENV NODE_VERSION 22.16.0`. | **Closed** |
| ER T2 — Node 22.16.0 floor executed only on 22.22.2 | (tentative) | Unchanged (`plan:5743–5744`, Q28). | **Carried** (T1 below) |
| ER T3 — `unshare -rn` on the runner | (tentative) | `plan:5891–5904` records the runner's refusal from CI run 34151517903; T-32-2/T-38-22 record instead of failing. The CI log is not reachable from here. | **Closed in form; carried as T2** |

Collapse-hunt items (22):

| Prior finding | Original standard | Current state (Read) | Status |
|---|---|---|---|
| CH S-1 | as ER S1 | as ER S1 | **Partially closed → recurring (M2)** |
| CH S-2 | as ER S5 | as ER S5 | **Closed on trust/collection; REGRESSED (S1)** |
| CH M-1 | as ER S6 | as ER S6 | **Closed** |
| CH M-2 | as ER S3 | as ER S3 | **Closed** (m3, m4, m5) |
| CH M-3 | as ER M3 | as ER M3 | **Closed** |
| CH M-4 / N-F — `large-store` declared as a Step 1 repository | item 7; testing-standards rule 2 | `plan:3299–3308` Step 29's `generate_large_store.ts` through the real migrations and DAOs; `plan:6181–6183` T-1-3 excludes it; D-plan-5 amended `plan:4510–4515`; Q52. | **Closed** |
| CH M-5 / N-G — four §11.4 entries described runs their probes do not perform | output-contract §11 (state only what the probe prints) | 06 (`plan:5862–5874` = `expected/06`), 10 (`plan:5734–5743` = `expected/10`), 15 (`plan:5834–5849` = `expected/15`) re-derived. **But** the 02 entry now says the probe "prints exactly … `sqlite_version: 3.51.2`" (`plan:5750–5756`) while the probe prints `sqlite_version >= 3.37 (…): true` and writes the version to stderr "informational, not compared" (`02_sqlite_features.cjs:6–8`); and the 08, 11, 12, 13, 17 entries quote values their probes do not print. | **Partially closed → recurring (M4)** |
| CH M-6 / N-H | as ER S4 | as ER S4 | **Closed** |
| CH M-7 / N-I — FTS5 fallback unbuildable | AD-2; Gate C | `plan:1454–1467` conditional `001b_phase_a_fts.sql`; `plan:1449–1451` always-created indexes; `plan:2030–2036` one `search.ts`; T-7-1/T-14-1 both flags. **But** Step 31 records `schema_meta.fts_state` "before the migrations run" (no table exists yet), the runner keys on a `{fts}` flag while Step 7's prose keys on `fts_state`, and nothing writes `fts_state = 'fts5'` on the success path (M1). | **Closed in form → REGRESSED (M1)** |
| CH M-8 / N-J — floor sample pooled legs | §11.5; per-leg rule | `plan:4184–4206` drawn and reported per leg, leg 2 labelled protocol-driven. | **Closed** |
| CH m-1 | as ER m1 | — | **Closed** |
| CH m-2 — §10A D-plan-13 steer stale | attestation | `plan:5181–5183` "Both tiers on every pull request from the step the first replay test exists". | **Closed** |
| CH m-3 | as ER m6 | — | **Closed** |
| CH m-4 — T-38-22 no refused-`unshare` branch | sibling of ER T3 | `plan:7726–7729`. | **Closed** |
| CH m-5 — Step 6 vs Step 12 on the missing-tuning fallback | Gate C | `plan:1223–1225` Step 6 names the `TuningReader` re-seed and the code. | **Closed** |
| CH m-6 | as ER m10 | — | **Closed** |
| CH m-7 — `.sql` delivery unspecified | Step 8; AD-25 | `plan:747–748` `files` includes `src/`; `plan:1481–1485` resolved from `import.meta.url`. | **Closed** |
| CH m-8 | as ER M2 | — | **Closed** |
| CH m-9 | as ER M1 | — | **Closed** (m8) |
| CH m-10 — replay-tier cost unquantified; no CI cache | stated cost | `plan:3304–3308` rebuilt per job, time printed by T-29-1; `plan:4047–4050` read into the exit report. | **Closed** |
| CH m-11 / N-L — §16 lacked the file-list reconciliation | output-contract §16 | `plan:8479–8486` item 7. | **Closed** |
| CH m-12 — T-16-1 varied `index_head` | Gate C | `plan:6618–6620` `ctx.indexStale`. | **Closed** |

**Totals over the 42:** closed 35; partially closed 4 (ER S1/CH S-1 → M2, ER m8 → m6, CH M-5 → M4 — three distinct rows plus the ER/CH pairing); regressed 3 (ER S5/CH S-2 → S1, CH M-7 → M1). The author's §2 table claims all 42 closed; the rows for ER S1/CH S-1 ("`provides:` carries exported names at 26 steps"), CH M-5 ("02: `sqlite_version 3.51.2`"), CH M-7 (D-plan-28), ER S5/CH S-2 (D-plan-26) and ER m8 ("Step 7 DDL") are contradicted by the source at the instances named above.

---

## Summary

This review returns **NEEDS FIXES** — but the shape of the round is different from rounds 2–4. The mechanical class the process was rebuilt to stop is largely gone: every `depends_on` now names consumed steps, seven shared call identifiers are all provided, `--check` and `--self-check` (34 cases) pass, the probe gate passes 17/17 under `--repeat 2 --load 2`, every D-plan-1…28 has a trace chain in one of the three files (the first file's D1…D10 map exactly as §10 states), §14.4's record is consistent with the document (Q49–Q52 exist and are closed; D-plan-27/28 carry §10A entries and traces-3 chains), the package pins and the Node floor are unchanged, no scope element moved, and on every axis but one the answer-drift block (Steps 21–27) is AD-9 and nothing more — the clear axis is now AD-9's two conditions with "content-free" operationalized in the direction FR-B5 leans. Of the 42 prior findings, 35 close against their original standard.

What remains is three Serious defects, one of them correction-induced. **(1) Leg 2 does not execute as written**: I ran the plan's own session kind — a scrubbed `claude -p` in an untrusted folder with settings-file hooks — and the hooks fired from `SessionStart` (the trust premise holds), but the `Write` the model attempted was denied by the permission system twice, no `PostToolUse` fired, no file was written, and the run ended with "I've requested permission to write the file"; the plan's "performs at least five tool events including one `Edit`" needs a permission mode, tool set and driver environment it never names, and its "same invocation shape the model seam verified (V9)" is false — V9 is `--tools ""` `--max-turns 1`. **(2)** Step 14's `runIndex` calls the frontends Step 15 creates, so T-14-1 cannot pass at the step that names it — the exact class the declaration grammar exists for, invisible to the check because the mention is neither a declared path nor a provided name. **(3)** Five acceptance replays (T-28-1, T-38-3, T-38-17, T-38-21, T-38-30) state only "fails when an output is emitted" against a handler AD-7 makes silent on every error — they pass on a dead pipeline. Five Moderate findings follow (the FTS-state bookkeeping D-plan-28 introduced is unexecutable at `init`; `provides:` is still empty at seven module-creating steps; the `HEAD` read has no symbolic-ref rule; six §11.4 entries still quote what their probes do not print; the replay harness has no store before `init` exists) and eleven Minor. Nineteen findings: 3 Serious, 5 Moderate, 11 Minor — seven of them correction-induced (five wrong changed text, two consistency).

---

## Upstream Contract Verification

The plan's upstream artifacts are the spec (OL-C6-signed, `spec:3–5`; `OWNER-LEDGER.md:71`) and the Phase A architecture. Both were Read in full this session.

**Spec §11.5 Phase A elements ↔ §2.3 coverage** (`plan:160–204`, generated from `covers:`): the seven model-free genres (PA-1 → S13–S19), the answer-drift safe skeleton (PA-2 → S21–S27), stores/index/miner (PA-3), delivery (PA-4 → S20, S28), self-observability (PA-5 → S6, S10, S26, S30, S33), security (PA-6 → S4, S11, S14, S19), the human-correction channel (PA-7 → S34, S35), the seams (PA-8 → S22, S24, S36), the recursion guard/degraded posture (PA-9 → S5, S10, S29, S36), test architecture and build-time verifications (PA-10), the CLI (PA-11), packaging (PA-12), the exit measurement (PA-13 → S39) — every §11.5 Phase A element and every architecture in-scope item (`arch:48–75`) maps; nothing added, nothing dropped; §2.2's deferrals equal the architecture's. **Package pins / floor:** `web-tree-sitter` 0.26.13, `tree-sitter-wasms` 0.1.13, `engines >=22.16.0` (`plan:742–745`) = V14 (`arch:138`), AD-2 (`arch:327–328`); dev pins 5.9.3 / 22.20.1 per D-plan-2; registry facts re-checked this session (`npm view`, above). Unchanged.

**Spec §14 acceptance — Phase A subset** (§12.4, `plan:7900–7957`, each T-ID Read against its spec lines): AC-1/1a–1d, AC-2, AC-2a, AC-2a-i allow-half, AC-3/3a/4/5/6, AC-7, AC-8/8a, AC-9, AC-10, AC-11, AC-12 (deterministic parts), AC-13, AC-14, AC-15, AC-17, AC-18, AC-19, AC-20, AC-21 (mechanism), AC-22, AC-23, AC-24 — honored in specification; deferrals (AC-2a-i deny-half, AC-2a-ii, AC-2b, AC-2c skill under-fire, AC-16, AC-21 full, AC-25) correctly per `spec:1122–1130`. Two qualifications: AC-2a-i allow-half and AC-4 are pinned by replays that cannot fail on a dead handler (S3); AC-2c over-fire's T-38-30 likewise.

**Architecture decisions** (honored / violated; method = Read of the cited ranges):

| AD | Verdict | Evidence |
|---|---|---|
| AD-1, AD-7 | Honored | `plan:3185–3225` exit 0 always; no daemon |
| AD-2 | Honored in design; **the fallback's bookkeeping is unexecutable at `init`** (M1) | `plan:948–966`; `plan:1454–1467`; `plan:3463–3468` |
| AD-3, AD-25, AD-26 | Honored | Steps 5, 1, 3/30 |
| AD-4, AD-5 | Honored; the plan's additions (`observed_actions.content_hash`, `regret`, `classified_turns`) are marked and reasoned | `plan:1338–1452`, `1541–1556` vs `arch:424–534`, `572–621` |
| AD-6, AD-20 | Honored | Steps 28, 31 |
| AD-8 | Honored | `plan:3185–3225` order; catch-up before block check |
| AD-9 | **Honored on every axis** — see the line-by-line read below | `plan:2697–2741`, `2882–2922`, `3070–3087` vs `arch:736–922` |
| AD-10 | Honored | `plan:2798–2823`; `probe:05` |
| AD-11 | Honored | `plan:2537–2567` |
| AD-12 | Honored in content; **Step 14 consumes Step 15's frontends before they exist** (S2); the `HEAD` read has no ref-resolution rule (M3) | `plan:2042–2061` |
| AD-13, AD-14, AD-15, AD-16 | Honored (one time-axis mismatch in the miner, m7) | Steps 13, 16, 17/18, 20 |
| AD-17 | Honored; the detectors now read persisted state (D-plan-27) | `plan:2988–3018`, `3662–3696` |
| AD-18 | Honored | `plan:3388–3409`, `3754–3767` |
| AD-19, AD-21, AD-22, AD-23 | Honored | Steps 11/19; 5/29/36; §2.2; Step 29 with §4 |
| AD-24 | Honored in content; **leg 2's named session kind cannot perform the tool events its protocol requires** (S1); five replays cannot fail (S3) | `plan:3991–4013`, `4116–4171` |

**The answer-drift block, Steps 21–27, read line by line against AD-9** (`arch:736–922`), the axis the dispatch asked to be read with particular care:

- *Intake* (`plan:2699–2708` ↔ `arch:747–760`): (i) ends with `?`, (ii) outside fences and quotes, (iii) not on the stoplist; multiple questions → multiple rows; `'already_open'` tolerated; the `requireTerminalMark` option is AD-18's `--missed-question` minus-the-`?` and its default is asserted by T-23-1. **Identical.**
- *Catch-up* (`plan:2887–2903` ↔ `arch:762–788`): markers, backfill by `content_hash`, voiding on an affirmatively non-human marker with the deny-fired flag, fresh open for an unmatched human question, bookmark over completed lines, `catchup_incomplete`. **Identical**, plus the D-plan-27 record of each classified turn — a persistence of AD-9's own detector inputs, not a new judgment.
- *Deny decision* (`plan:2904–2911` ↔ `arch:790–804`): main consumer only; at least one `open` row; `recognizeMove` true exactly for `Write`/`Edit`/`NotebookEdit` (`plan:2737–2739`); reason quotes the open question(s); audit before verdict; the bypass key recorded. **Identical.**
- *Lag-window hold* (`plan:2913–2922` ↔ `arch:812–826`): read-to-EOF + deny-on-open, clear-axis only, no lag estimator; `deny_after_answer_lag` and `deny_despite_answer_text` with the deferral exclusion. **Identical.**
- *Clear recognizer* (`plan:2709–2736` ↔ `arch:778–786`): AD-9's two conditions — substance above a small floor after stripping tool noise, and not a recognized content-free deferral of the "I'll get to that" class — with "content-free" operationalized as: strip every stoplist phrase, clear when what remains meets the floor. No acknowledgement vocabulary, no clause grammar, no bare-word members; the round-4 inputs all clear (`expected/16`). The one residual difference from a literal reading of AD-9 — a stoplist phrase plus non-answer filler ("I'll get to that after the refactor.") clears, because a model-free rule cannot tell that filler from a direct answer to "when?" — is disclosed at `plan:2727–2733`, reasoned in D-plan-24 and its §10A entry, sits in FR-B5's lean, and is measured by Step 39's escape fraction. **Not counted as an elaboration**: it is the operationalization AD-9 delegates ("the exact words are implementation vocabulary", `arch:1708`), in the safe direction. One factual claim about the floor's reach is wrong (m1).
- *Backstop and counter* (`plan:3079–3087` ↔ `arch:828–839`), *question lifetime* (`plan:3070–3077` ↔ `arch:852–867`), *seam* (`plan:2628–2649` ↔ `arch:869–879`): **identical.** Nothing widens the deny-eligible set; no question-type classifier; no `Bash` classifier; no per-question clear matcher.

---

## Critical & Serious Findings

No Critical findings — the full inventory was Read or executed per Gate B; `--check` exits 0; the probe gate holds under load once the layout is prepared; no Critical-class violation was observed.

### S1 — Leg 2's counted sessions cannot perform the tool events the protocol requires: a `claude -p` session denies `Write`/`Edit` without a permission mode the plan never names; "the same invocation shape the model seam verified (V9)" is false for what leg 2 needs; the driver's environment scrub is unstated — **REGRESSION (introduced by the round-4 corrections — the D-plan-26 re-derivation; text)**

**Class:** unbuildable-as-written protocol on the mission-load-bearing leg (the validity rule and every deny-derived exit number hang on it); a false premise stated as verified evidence.
**Location:** `docs/plans/plan-phase-a.md:4122–4134` ("**Every counted session is a `claude -p` conversation the implementing agent drives from its own environment** — the same invocation shape the model seam verified (V9, Step 36), as a child process of the agent's session, with `CTXORACLE_INTERNAL` absent from the child's environment"); `docs/plans/plan-phase-a.md:4142–4156` ("At least **three** `claude -p` conversations per repository … driven turn by turn by the agent. Inside each, the driver asks at least one `?`-terminated question and at least one indirect ask …, performs at least five tool events including one `Edit`"); `docs/plans/plan-phase-a.md:4175–4182` (the validity rule); `docs/plans/plan-phase-a.md:5391–5416` (§10A D-plan-26: "the invocation the model seam already verified (V9) and needs neither premise"); `docs/plans/plan-phase-a.md:4841–4869` (D-plan-26); `docs/reviews/2026-09-07-plan-tool-traces-3.md:237–241` (the chain's premise: "A `claude -p` conversation … the invocation the model seam already verified (V9; probe 15 authenticates under the scrub)"; its decision_framework criteria, `:291`, are trust rule, init-before-session, data locality, executable-with-tooling — none is "the session can run tools").

**What the sources say.** `arch:133` (V9): the verified invocation is `claude -p --model claude-haiku-4-5 --tools "" --max-turns 1 --output-format json` — every tool disabled, one turn. `claude --help` (this session): `-p, --print … The workspace trust dialog is skipped when Claude is run in non-interactive mode`; `--permission-mode <mode> … (choices: "acceptEdits", "auto", "bypassPermissions", "manual", "dontAsk", "plan")`; `--permission-prompts <target> … "none" (nobody: anything that would prompt is denied automatically …) (default: "host")`; `--tools`, `--allowedTools`, `--resume`. `OWNER-LEDGER.md:49` (OL-11): the agent designs, builds and verifies. `CLAUDE.md` rule 1: never claim something works without having run it.

**How verified (executed 2026-09-07).** Scratch folder `<scratchpad>/ptest` with `.claude/settings.json` logging every hook's stdin; then, with the six session-identity variables unset (the plan's own scrub), `claude -p "Create a file named hello.txt in the current directory containing the single word hi, then reply with the single word done." --model claude-haiku-4-5-20251001 --max-turns 4 --output-format json`. Result: `is_error: False, num_turns: 3, subtype: success`, `result: "I've requested permission to write the file. Please approve the permission prompt to proceed."`, `permission_denials: [{tool_name: Write, …hello.txt…}, {tool_name: Write, …}]`; `hello.txt exists: no`. Hook log, in order: `SessionStart {source: startup}`, `UserPromptSubmit`, `PreToolUse {tool_name: Write}`, `PreToolUse {tool_name: Write}`, `Stop`, `SessionEnd` — no `PostToolUse`, no `PostToolUseFailure`. A second turn, `claude -p --resume <session_id> "Reply with the single word ok."`: `SessionStart {source: resume}`, `UserPromptSubmit`, `Stop`, `SessionEnd`; result `ok`. The transcript landed at `~/.claude/projects/-tmp-claude-0--home-user-agent-armory-…-scratchpad-ptest/<session>.jsonl` (the plan's slug rule), and its two human turns carry **no `origin` and no `isMeta`** — the marker-less mode V12 records for `-p` (`('string', None, None): 2`).

Three facts the plan needs are therefore established, and one it asserts is refuted. Established: settings-file hooks run in a `-p` session in an untrusted folder from `SessionStart` (the trust rule the round-4 corrections adopted is right); `--resume` fires `SessionStart {source: resume}`; the transcript location matches Step 21's rule. Refuted: with the invocation the plan describes, **no `Edit`/`Write` ever executes** — the permission system denies it in non-interactive mode, the model gives up, and the session ends with `PreToolUse` fired but the action never taken. "Performs at least five tool events including one `Edit`" cannot happen; `observed_actions` never gains an `ok` edit row; Completeness and Verification never fire; the "denies escaped by a text turn" fraction, the lag-hold rate and the L11(b) induction all run against sessions whose edits are all permission-denied. The plan names no `--permission-mode`, no `--allowedTools`, no tool set (the model seam's `--tools ""` disables every tool), and no environment scrub for the driver — `probe:15` shows an unscrubbed child reports the *parent's* `session_id`, so a driver that does not strip the six variables measures the agent's own session, not three fresh ones. Each of these is a choice the implementer must make, which Gate C forbids.

**Provenance.** r4:3292–3301 had the bootstrap/counted protocol on remote sessions; the `claude -p` protocol, the V9 sentence and the tool-event requirement are the round-4 correction — **REGRESSION (text).** The trace chain records the premise honestly and reasoned from it; the premise was never executed.

**Standard violated.** CLAUDE.md rule 1 ("Never claim something works without having run it"); expert-plan Step 8 / Gate C (a deferred choice — permission mode, tool set, scrub — fails the gate); spec §11.5 (the exit is a measurement of the block on the owner's real repos — a leg whose edits never execute measures nothing the block does); collapse-log 2026-09-07 standing lesson (a reviewer's prescription — "dry-run the bootstrap-then-counted sequence" — transcribed as a re-derivation, not executed).

**Required change (hypothesis).** The counted-session invocation is stated in full and executed once before delivery: the permission mode (`--permission-mode acceptEdits` for `Edit`/`Write`, with `Bash` either allowed by `--allowedTools` for the runner commands the protocol needs or recorded as permission-denied per session), the tool set (never `--tools ""`), the six-variable scrub applied by the driver (so each session is fresh — the observable is a `session_id` unequal to the agent's), and `--resume` as the turn mechanism; the report records the flags. The V9 sentence is removed (V9 verified authentication and envelope shape, not a tool-enabled conversation). Because the `-p` transcript is marker-less, the protocol also states that every `--resume` turn's rebuild will raise `rebuild_recovered_nothing` and recover no question — mid-session enforcement rests on intake alone in leg 2, and the report labels the leg-2 marker table accordingly. Evidence that would confirm: one counted session, run with the stated flags, whose store holds a `SessionStart` liveness row *and* at least one `outcome='ok'` `Edit` row in `observed_actions`.

---

### S2 — Step 14's `runIndex` calls the frontends Step 15 creates ("calls the matching frontend or `generic_frontend` (Step 15)"), so T-14-1 — which asserts symbols, edges and `test_map` rows populate — cannot pass at the step whose Verification names it; the build-order check cannot see the mention — **new** (present verbatim at `6a2159b`, unreported)

**Class:** topological inversion between step artifacts and step text — the class the declaration grammar was installed to stop — at a site the check's vocabulary does not reach.
**Location:** `docs/plans/plan-phase-a.md:2042–2046` ("`runIndex(store, repoPath, {full})`: walks the working tree …; per file resolves the language via `index.ext_to_grammar` (Step 12) with `tuning` overrides; calls the matching frontend or `generic_frontend` (Step 15); writes `files`, `symbols`, `import_edges`"); `docs/plans/plan-phase-a.md:2018` (S14 `depends_on: [S1, S5, S9, S10, S11, S12, S13]` — no S15, and S15 `depends_on: [S1, S14]` at `plan:2121`); `docs/plans/plan-phase-a.md:2096–2103` (Step 14's Verification: "`T-14-1` (skeleton runs on `indexer-small` …; `files`/`symbols`/`import_edges`/`test_map` populate …)"); `docs/plans/plan-phase-a.md:6547–6564` (T-14-1: "3 `.ts` files (one importing another), 1 `.py`, 1 `.sh` … **Fails when** an expected symbol/edge/`test_map` row is missing"); `docs/plans/plan-phase-a.md:2125–2137` (Step 15 creates both `tree_sitter_frontend.ts` and `generic_frontend.ts`; the generic frontend "produces no `import_edges`").

**What the plan says, and why it cannot execute.** At Step 14 the `LanguageFrontend` interface exists (`plan:2022–2029`) and no implementation does — both are Step 15's. `runIndex` at Step 14 therefore has no frontend to call for any file, so no `symbols`, no `import_edges` (which the generic frontend never produces anyway), and no `test_map` "from import edges" can be written; T-14-1's stated failure condition fires on a conformant Step 14 build. The mention `generic_frontend` is not a declared path (the path ends in `.ts`) and not a provided name (S15 `provides: []`), so `--check` resolves nothing and exits 0 — the round-4 SY-1 mechanism one layer down.

**How verified.** Read of the cited lines; `--check` executed (exit 0); r4:1975–1991 and r4:6284–6300 carry the same sentences and the same test — **new**, unreported in rounds 2–4 (the round-4 S1 finding listed S15 → S14 as a missing dependency, not S14 → S15).

**Standard violated.** output-contract item 7 ("a step consuming something that does not exist when it is built"); expert-plan Step 8 (Verification "how the implementer will confirm this step is correct *after building it*"); Gate C (`provides:` complete for names later steps consume — a `provides: []` on a module-creating step "is a hollow declaration the check cannot see, and is non-compliance"); testing-standards ("the specification states what makes it fail" — the stated condition fires on correct behaviour).

**Required change (hypothesis).** Either the frontends move before the indexer (Step 15's two modules depend only on Step 6's types and the interface, which can move with them), with S14 depending on the frontend step and T-14-1 unchanged; or Step 14 ships the interface plus a registry with no implementations and T-14-1 is split — the Step 14 half asserting `files`, zones, the size cap, the secret, the lock and the FTS/`LIKE` hit-set equivalence over path tokens only, the symbol/edge/`test_map` assertions moving to Step 15's tests. Either way S15 `provides:` names the frontends and the mention is backticked as a provided name. Evidence that would confirm: `--check` fails on the current order after `provides: [treeSitterFrontend, genericFrontend]` is added to S15 and the Step 14 sentence backticks `genericFrontend`, and passes after the reorder.

---

### S3 — Five acceptance replays state only "fails when an output is emitted" against a handler that AD-7 makes silent on every error, so each passes on a dead pipeline (T-28-1, T-38-3, T-38-17, T-38-21, T-38-30) — **new** (present at `6a2159b`, unreported)

**Class:** testing-standards anti-pattern 4 ("cannot fail"); the fail-open design's own failure mode is invisible to the tests that pin AC-2a-i, AC-4, AC-2c over-fire, FR-B4 and AD-8's order.
**Location:** `docs/plans/plan-phase-a.md:7103–7112` (T-28-1: "**Fails when** a deny is emitted"); `docs/plans/plan-phase-a.md:7470–7480` (T-38-3: "**Fails when** the subagent's `Edit` is denied"); `docs/plans/plan-phase-a.md:7654–7665` (T-38-17: "**Fails when** the read-set subject is delivered OR the untouched-file candidate fires"); `docs/plans/plan-phase-a.md:7702–7712` (T-38-21: "**Fails when** the second emits `additionalContext`"); `docs/plans/plan-phase-a.md:7836–7853` (T-38-30: "**Fails when** the post-answer `Edit` is denied, OR any information-gathering move is denied"); against `docs/plans/plan-phase-a.md:3224–3225` (AD-7: "Exit 0 always; any error or watchdog fire ⇒ empty output + JSONL fault, no deny, no whisper") and `docs/plans/plan-phase-a.md:7118–7125` (T-28-3: an unopenable store, malformed stdin, and a truncated store each yield "exit 0, empty stdout, and a JSONL fault line").

**What the plan says.** A handler that fails to open its store, throws in the adapter, or trips the watchdog on every event emits nothing (AD-7, T-28-3). Each of the five specifications makes emission the *only* failure condition, so a build in which the pipeline never runs — the store missing (M5), the adapter broken, the deadline mis-set — passes all five. T-28-1 is the test that pins AD-8's load-bearing order; T-38-30 pins AC-2c's over-fire clause; T-38-3 pins AC-2a-i's allow-half; T-38-17 pins AC-4; T-38-21 pins FR-B4's single cycle. Every other replay carries at least one positive observable (a deny with the question quoted, a whisper with a ratio, a row expired, a fault recorded); these five carry none.

**How verified.** Read of every §12.2/§12.3 specification's `Fails when` clause; the five above are the only ones whose every clause is an emission. r4 text identical at each — **new**.

**Standard violated.** testing-standards core discipline ("Every test must be able to fail, and the specification states what makes it fail"; anti-pattern 4: "Check: name the behavioral breakage that fails this test"); expert-plan Step 9 item 5 ("any assertion that cannot fail when the behavior is broken"); Gate C ("any test whose … data source shaped backward … is non-compliance" applies by the same rule to an assertion set satisfied by silence).

**Required change (hypothesis).** Each of the five adds a positive observable that only a running pipeline produces: a `session_log` row per replayed event with `outcome` set and no JSONL fault for the stream (the runner already "exposes assertion helpers over … stores, and diagnostics", `plan:3179–3180`); T-28-1 additionally asserts the intake row exists and was closed by the clearing turn's uuid; T-38-30 asserts the `answered` row; T-38-17 asserts the read-set row for X; T-38-21 asserts the first `Stop` emitted; T-38-3 asserts the subagent event's `session_log` row. Evidence that would confirm: each spec's `Fails when` names a state the handler must have written, so a fail-open run is red.

---

## Systemic Patterns

### SY-1 — Where the round-4 corrections reasoned instead of executed, the premise was not run: the protocol (S1), the migration order (M1), the probe-02 entry (M4) and T-14-2's audit (m3) are each a correction whose text describes an execution or a mechanism that was not exercised

**Proactive scan.** Every correction the author's §3 lists as "reasoned rather than reconciled" was re-derived: D-plan-24 — executed by probe 16, holds (the twelve round-4 inputs clear); D-plan-26 — reasoned from a documentation read that is correct on trust and never executed on tools (S1; the chain's criteria contain no "tools run" axis); D-plan-27 — holds as a design (the rebuild idempotence, m8, is a residual); D-plan-28 — reasoned into an `init` sequence that writes a key into a table the same sequence has not yet created (M1); the D-plan-5 amendment — holds. Of the four places the author says the pass "reasoned rather than reconciled", two carry defects that one execution would have shown (an `init` on a fresh store; one `-p` session). The pattern is narrower than round 4's — the mechanical checks now measure their properties (`provides:` for names, section-scoped probes, load-repeated runs) and the residue is in judgment text no check reaches — but it is the same 2026-09-07 lesson at a smaller scale: a correction is a hypothesis to execute.

**Named standard.** CLAUDE.md rule 1; collapse-log 2026-09-07 standing lesson (2) ("a reviewer's prescription is a hypothesis to execute, never a sentence to transcribe"); expert-plan Step 6 (the reasoning must weigh the alternatives on the axis that decides — the D-plan-26 framework omitted the deciding axis).

**What correct looks like.** A protocol step names its executable command in full and a §11.4 entry records one execution of it (as `probe:15` does for the seam); a schema-touching sequence is walked against an empty store before it is written; a test's observable names a facility some step creates.

**No other systemic pattern** was found. Scans run: every `step-decl`'s `tests:` against §12 (`--check`: 122/122); every `depends_on` earlier than its dependent (`--check`); the shared call identifiers (seven, all provided — my scan); the six convention tests' allow/deny sets against every module a step creates under their scope — T-3-2, T-5-3, T-10-3 (pass; Step 26's detectors read `classified_turns`, not `session_log`), T-24-2, T-28-2 (pass — `locate.ts` names no wire field now), T-32-3, T-36-1 (pass); narration vocabulary over the plan (`round-1|round-2|round-3|round-4|prior version|was fixed|re-authored|the fix` → §14.4's pass record only, which is a record, not narration); deferral vocabulary over §7 (`either … or|one of the following|alternatively|if needed|as appropriate|TBD` → 0 hits; the deferred choices in S1, M1, M3 were found by Read).

---

## Moderate & Minor Findings

### Moderate

**M1 — The FTS-state bookkeeping D-plan-28 introduced is unexecutable at `init` and internally contradictory: Step 31 records `schema_meta.fts_state = 'fallback'` "before the migrations run" (the `schema_meta` table is created by migration 001), Step 7's prose says 001b is applied "only when `schema_meta.fts_state = 'fts5'`" while its runner signature keys on a `{fts: boolean}` argument, and nothing writes `fts_state = 'fts5'` on the success path although `search.ts` chooses its implementation by that key "at call time" — REGRESSION (introduced by the round-4 corrections — the CH M-7 closure; text).**
Location: `docs/plans/plan-phase-a.md:3463–3468` ("on a failed probe record `schema_meta.fts_state = 'fallback'` before the migrations run, so `applyMigrations(store, {fts: false})` skips the FTS migration"); `plan:3476–3478` ("`applyMigrations` for the project store (001, 001b when `fts_state` is `'fts5'`)"); `plan:1454–1459` ("which the runner applies only when `schema_meta.fts_state = 'fts5'`"); `plan:1477–1480` ("`applyMigrations(store, {fts: boolean})` … skipping a migration whose name carries the `_fts` suffix when `fts` is false"); `plan:2030–2036` (`search.ts` "chosen by `schema_meta.fts_state` at call time"); `plan:1345–1348` (`schema_meta` created by 001). Verified by Read; r4's Step 31 said only "on a failed probe continue with the `LIKE` fallback recorded in `schema_meta`" — the ordering sentence, the flag and the call-time key are new text. Standard: expert-plan Step 8 / Gate C (an implementer must decide the order and who writes `'fts5'`); Gate A. Required change (hypothesis): `init` runs 001 first, writes `fts_state` from the probe result, then applies 001b iff `'fts5'`; the runner's flag is dropped or derived from that row; T-7-1's two-flag data becomes two `fts_state` values. Evidence: a walk of `init` against an empty store names no write before the table exists.

**M2 — `provides:` is still `[]` at seven steps that create functions or DAOs later steps call — S9 (every DAO: `whisper_audit.append`, `questions.*`, `session_log.append`, `classified_turns.record` …), S13 (`mineCochange`, called at Step 14), S15 (the two frontends, called at Step 14), S18 (the generators and the done-claim recognizer, called at Steps 27, 28), S19 (`compose`, called at Step 28), S26 (the four detectors, called at Step 28), S27 (`handleSessionStart`, `outstandingQuestionLine`, called at Step 28) — and partial at S3 (`openStore`), S20 (`perConsumerDedup`, `reconcileDedupOnSessionStart`, `updateReadSet`, `recordDelivered`), S22 (`expireOnStartup`, `advanceBookmark`, `getBookmark`); the build-order check therefore has nothing to resolve for those modules, which is how S2 passed it — recurring (ER S1 / CH S-1 partially closed).**
Location: `docs/plans/plan-phase-a.md:1587, 1945, 2119, 2303, 2407, 2982, 3064` (`provides: []`); `plan:942, 2469, 2622` (partial lists); the consuming sentences at `plan:2054`, `2044–2045`, `3211–3220`, `3203–3210`, `3194`, `3219`, `3072`, `3463`. Verified by a scan over the plan: seven call-shaped identifiers are mentioned by two or more steps and all seven are provided; roughly ninety call-shaped identifiers are mentioned by exactly one step (their creator) and consumed elsewhere without parentheses, which the check does not scan (`derive-plan-sections.mjs:530–545`). Standard: output-contract item 7 (`provides:` = "every artifact a later step consumes by name rather than by path: the exported functions, types, and table DAOs other steps call"); Gate C (the quoted non-compliance sentence). Required change (hypothesis): each creating step lists the exported names later steps use, and consuming steps backtick them, so the check's *k*-word rule resolves them; S2's inversion is then a check error. Evidence: `--check` output on the current order after the lists are filled.

**M3 — `refreshIfStale` compares `schema_meta.index_head` to "`HEAD` read from the `.git` directory (a bounded file read)" with no rule for resolving a symbolic ref: on every ordinary checkout `.git/HEAD` is `ref: refs/heads/<branch>`, so the comparison against a commit hash reads "stale" at every `SessionStart`, records `index_stale`, and spawns a reindex each session; worktrees (`.git` a file naming `gitdir`) and packed refs are unaddressed — new (present at `6a2159b`, unreported).**
Location: `docs/plans/plan-phase-a.md:2055–2061`; `plan:3194–3199` (the handler spawns on `{stale: true}`); `arch:1490–1492` (AD-23 names "a bounded `.git` file read", not the resolution). Verified by Read; r4:2001–2003 identical. Standard: expert-plan Step 8 ("Name the file paths"); Gate C (a deferred choice); AD-23's inventory (the bound must name the reads: `HEAD`, then `refs/heads/<b>` or a `packed-refs` scan). Required change (hypothesis): state the resolution — read `.git` (file or directory), then `HEAD`; if `ref: <path>`, read `<gitdir>/<path>`, else scan `packed-refs` for the ref — each a bounded read, and the case of an unresolvable ref recorded as a diagnostic, never as stale; T-14-2 adds a packed-ref fixture. Evidence: T-14-2's "unmoved `HEAD` records nothing" passes on a fixture whose branch ref is packed.

**M4 — Six §11.4 entries still quote values, modes or runs their cited probes do not print (the CH M-5 class recurring): the probe-02 entry says the probe "prints exactly … `sqlite_version: 3.51.2`" while the probe prints `sqlite_version >= 3.37 (…): true` and sends the version to stderr as "informational, not compared" (REGRESSION — text, the very instance the author's closure row names); the probe-13 entry says "each passage re-fetched and asserted inside its own section" while three of the six needles carry no section and most quoted passages (the entry-field list, the three locations, `prompt`, "silent on injected turns", the two timeout sentences as quoted, the `transcript_path` lag sentence) are asserted by no needle; the probe-12 entry cites "lines 1200–1218 … and line 1697" of a mutable `main` file the probe greps for strings; the probe-08 entry quotes a `[UNDICI-EHPA]` warning the probe suppresses with `NODE_NO_WARNINGS=1`; the probe-11 entry quotes `"main": "bindings/node"` and the `.d.ts` signatures at lines 151–317; the probe-17 entry quotes 0.27.0's date, 7.0.2's date, the `7.1.0-dev.*` tail, the script list and the self-dependency — recurring.**
Location: `docs/plans/plan-phase-a.md:5745–5756` vs `02_sqlite_features.cjs:6–8`, `expected/02:2`; `plan:5757–5789` vs `13_hooks_reference.optional.sh:29–36`, `expected/13`; `plan:5700–5713` vs `expected/12`; `plan:5875–5890` vs `08_node_proxy_env.mjs:4`; `plan:5914–5928` vs `expected/11`; `plan:5790–5805` vs `expected/17`. Verified by line-by-line comparison of every entry against every `expected/*.txt`; the values themselves were re-checked and are true today (`npm view`, the `.d.ts`, the package manifest, my own fetch), so nothing rests on a false fact — the defect is that the entries attest what no probe reproduces. Standard: output-contract §11 ("an entry that quotes values, modes, or runs the probe does not print is a transcription with a citation attached, and is non-compliance"); Gate C; CLAUDE.md rule 1 (the 02 entry's "prints exactly" is false as written). Required change (hypothesis): each entry states what its probe prints and cites documentation reads separately as documentation reads (URL + date) where a value is a doc fact; the 02 probe either prints the version or the entry drops it; probe 13's unscoped needles gain their sections or the entry stops claiming section scoping for them.

**M5 — At Step 28 the replay harness must have a migrated, seeded, key-derived project store for T-28-1, T-28-4 and T-28-5 to run, and no step creates one before `init` (Step 31): the runner's description names no store preparation, the handler "open[s] the project store" and does not migrate on open, so every Checkpoint-3 replay runs against a missing store, fails open, and — per S3 — T-28-1 passes vacuously — new.**
Location: `docs/plans/plan-phase-a.md:3173–3183` (the runner: reads a stream, spawns the binary with `CTXORACLE_HOME` at a temp home, controls the transcript, records responses, exposes helpers — nothing about creating `<home>/projects/<key>/store.db`); `plan:3189–3190` (handler item 3 "open the project store"); `plan:1507–1509` (the runner applies migrations "at `init`/`index` open"); `plan:4370–4378` (Checkpoint 3: "no `init` verb exists"); `plan:7118–7125` (a missing store is a fail-open case). Verified by Read; r4 identical. Standard: expert-plan Step 8 (Verification runnable where placed); Step 10 (a checkpoint verifies accumulated state); Gate C. Required change (hypothesis): the runner's setup calls `ensureLayout`, `resolveRepoKey`, `applyMigrations` (both stores) and `seedDefaults` for the fixture repository before the first event — named in Step 28 with the functions it consumes (Steps 4, 5, 7, 8, 12 are in S28's closure) — and every Step 28 replay asserts the store was read (S3). Evidence: Checkpoint 3's replays are red when the setup is removed.

### Minor

**m1 — Step 23 says the seeded floor of 2 "rejects an empty or one-mark turn and nothing else" (and D-plan-7 repeats it); it also rejects every one-character answer ("y", "5", "k"), which are direct answers FR-B5 says must clear; T-23-2 has no one-character case, and T-38-6's "floor−1-character real answer" is exactly this class — new.** `docs/plans/plan-phase-a.md:2713–2717`, `4552–4556`, `6899–6900`, `7519–7521`; r4:2659 identical. Standard: CLAUDE.md rule 1 (a false claim about the rule's reach); Step 9 (the boundary case is the test's job). Required change: state the reach ("empty, one mark, or one character"), add "y" as a T-23-2 boundary case with its class decided (a floor of 1 clears it; a floor of 2 holds it and `deny_despite_answer_text` is the detector), and say which.

**m2 — D-plan-24 says "the twelve direct answers of the `T-23-2` case table all clear"; T-23-2 lists nine direct-answer cases (and eighteen cases in all), while probe 16 runs twenty-three, five of which T-23-2 does not list ("Yes.", "Yes, absolutely.", "Sure, it is safe to rename.", "The deploy fails …", "It is safe; …"); the §11.4 entry calls the probe "the T-23-2 case table … the twenty-three cases" — REGRESSION (text).** `docs/plans/plan-phase-a.md:4820–4822`, `6899–6917`, `6002–6011`; `16_clear_rule_cases.mjs:19–33`. Standard: Gate C (counts re-derived from the current step set). Required change: T-23-2's Data lists the probe's twenty-three cases, or the two counts are re-derived from what each holds.

**m3 — T-14-2 asserts "none was spawned by observing the spawn wrapper's audit (Step 5) is empty for the call"; Step 5 defines no spawn audit, and a spawn record inside `spawn.ts` would be the production-module test hook D-plan-22 forbids — REGRESSION (text; the T-14-2 re-derivation).** `docs/plans/plan-phase-a.md:6574–6576` vs `plan:1118–1138`, `4784–4792`. Standard: Step 8 ("Name the functions"); D-plan-22. Required change: the observable is the absence of a `.reindex.lock` and of any child (`oracleSpawn` is not called — assertable by passing a spy-wrapped `spawn` parameter, which must then be named), or T-14-2 drops the spawn clause and T-28-5 alone observes the child.

**m4 — Step 28 introduces `dispatch.ts` "with, at this step, the single undocumented internal verb `hook <event>`", then registers `hook integrity-check` ("this step's other internal verb") and the `index` verb in the same step — new (already two verbs at `6a2159b`; the third is the round-4 correction).** `docs/plans/plan-phase-a.md:3163–3166`, `3196–3200`, `3233–3238`; r4:3099, 3129. Standard: Gate C. Required change: "with, at this step, three verbs: `hook <event>`, `hook integrity-check`, and `index [--full]`".

**m5 — Step 32's Source still cites "`AD-12`/`AD-13` (index runs both indexer and miner)" for the `index` verb Step 32 no longer creates (it is Step 28's) — REGRESSION (consistency).** `docs/plans/plan-phase-a.md:3608–3610` vs `3196–3197`; r4:3495, 3517. Standard: Gate C. Required change: drop the clause (or move it to Step 28's Source).

**m6 — Step 7's `schema_meta` key list omits `index_stale` (written by Step 14) and `pinned_interpreter` (written by Step 31); the author's ER m8 closure row says "Step 7 DDL" was re-derived — REGRESSION (consistency) for `pinned_interpreter`; the `index_stale` omission pre-existed.** `docs/plans/plan-phase-a.md:1345–1348` vs `2058`, `3489–3490`; r4:1329–1331. Standard: "Still authored — re-derive"; Gate C. Required change: add both keys to the comment.

**m7 — Step 13 measures the horizon and the `fix_chatter` window "back from the committer timestamp of `HEAD`" but streams `--format=%H%x00%at` — the author timestamp — so the two ends of every window are on different time axes (rebased or cherry-picked history can separate them by weeks); Step 16's recency dampener inherits the same `ts` — new.** `docs/plans/plan-phase-a.md:1952–1961`, `2192–2195`; r4:1906 identical. Standard: AD-13 (`ts` recorded per commit; one axis); CLAUDE.md rule 1. Required change: one axis — `%ct` with the committer time of `HEAD`, or `%at` with `HEAD`'s author time — and T-13-1's fixture sets the two dates apart to pin it.

**m8 — On `resume`/`fork`/`compact` Step 27 resets the bookmark to 0 and the next catch-up re-classifies every entry: each assistant turn is re-recorded into `classified_turns` whose primary key is `(consumer, uuid)` with no stated conflict semantics for `record()` (an `INSERT` fails), and `checkDenyAfterAnswerLag` re-fires for every historical (clearing turn, later deny) pair on every rebuild — in leg 2's `-p --resume` regime that is every turn — REGRESSION (text) for the `classified_turns` half (D-plan-27), pre-existing for the lag half.** `docs/plans/plan-phase-a.md:3070–3077`, `1442–1445`, `1619`, `2894–2897`, `2990–2993`; r4:3007. Standard: Step 8 (the DAO's semantics named); testing-standards (T-27-1's rebuild case cannot pass with a UNIQUE failure). Required change: `record()` is `INSERT OR IGNORE` (the uuid is the identity), and the lag detector fires only for a deny not already named by a `deny_after_answer_lag` fault; T-27-1 adds a rebuild over a transcript with an existing `classified_turns` row and an existing lag fault.

**m9 — Step 33 says the totally-dead detector runs "at `status`, `init`, and `index`", but `init` (Step 31, earlier) and the `index` verb (Step 28, earlier) never call it, Step 33 declares no modification of `init.ts` or `index.ts`, and `status.ts` does not exist when either is built — new.** `docs/plans/plan-phase-a.md:3686–3688`, `3653`; r4:3592 identical. Standard: item 7 (`modify:` declares the edit); Gate C. Required change: either Step 33 declares `modify: [src/cli/init.ts, src/cli/index.ts]` and adds the call, or the sentence names `status` only (D-plan-25's job is served at the next CLI use).

**m10 — T-3-3's Verifies says "a third write contended *during the retry* raises `StoreBusy`" while its Data has C make both attempts while A holds, before B's first attempt and well before B's retry — new (the sentence pre-dates the round-4 schedule and was already inconsistent with it).** `docs/plans/plan-phase-a.md:6235–6237` vs `6241–6254`; r4:5980–5982. Standard: Gate C. Required change: "a third write contended while the lock is held raises `StoreBusy` after two attempts".

**m11 — `TuningReader` is a type (Step 6) that Step 12 says "re-seeds the key from its seed module" and Step 16 consumes, but no step names the function that constructs one over a store (`tuning.get(store, key)` is the DAO; `tuning.get(key)` is the reader) — the handler must invent it — new.** `docs/plans/plan-phase-a.md:1249`, `1843–1845`, `2186–2187`; r4 identical. Standard: Step 8 ("Name the functions"). Required change: Step 12 exports `makeTuningReader(store): TuningReader` (or names the equivalent) and `provides:` it.

---

## Tentative Findings

**T1 (carried) — Behaviour at the Node 22.16.0 floor** is executed only on v22.22.2 (`plan:5743–5744`; Q28); assigned to CI's matrix entry, which is honest.

**T2 (carried, narrowed) — `unshare -rn` on the `ubuntu-24.04` runner.** §11.4 records the runner's refusal from CI run 34151517903 (`plan:5891–5904`); the CI log is not reachable from this session, so the record rests on the author's report. T-32-2 and T-38-22 record a refusal instead of failing, so the plan no longer depends on the answer.

**T3 — Whether the implementing agent's container permits the child `claude -p` the permission mode S1 requires.** R13 (`plan:8019–8028`) records a permission classifier blocking an in-container hook install on 2026-09-06; whether `--permission-mode acceptEdits` (or `bypassPermissions`) is honoured for a child session in that environment could not be tested here without first stating the flags (S1). Verification gap: the one counted session S1's hypothesis names.

---

## Observations

- **Multi-perspective check (manual; Clear Thought unavailable).** *Standards persona:* the round's findings are no longer one shape; the mechanical class is closed and the residue is judgment text — an unexecuted protocol (S1), an inversion the check's vocabulary does not reach (S2), a test-design class (S3), a sequencing error inside a correction (M1). *Implementer persona:* the first stop in a dry-run walk is Step 14 (no frontend to call; T-14-1 red); the second is Checkpoint 3 (no store; every replay fails open and five of them pass anyway); the third is `init` on a fresh store (M1); the fourth is Step 39 leg 2 (the first `Edit` is permission-denied). *Owner persona:* the block's intake and move axes are still the skeleton, the clear axis grew nothing this round and lost the vocabulary that held on "Sure.", and the exit run's one closed-loop leg would, as written, report a block that never saw a completed edit.
- **The probe regime holds.** Once the layout is prepared, 17 of 17 probes reproduce under `--repeat 2 --load 2`, including the causally forced busy schedule (07) and the section-scoped hooks probe (13). My one failure was self-inflicted — the runner accepts an unprepared `--layout` and runs probes against a wrong environment without saying so (`run-plan-probes.mjs:81–89`); that is a skill observation for the skill's owner, not a plan finding.
- **Leg 2's session kind is the marker-less transcript mode.** My `-p` transcript's human turns carry no `origin`/`isMeta`, so in every counted session the `resume` rebuild recovers nothing and `rebuild_recovered_nothing` fires each turn; intake alone carries enforcement. The plan says mid-session enforcement never depends on markers (true), but does not say that its own exit run measures the block exclusively in that mode, nor that the rebuild diagnostic will be a constant of the leg. That belongs in Step 39's report fields (folded into S1's required change).
- **The trace attestation is true.** Every D-plan-1…28 has a chain; the first file's `D1`…`D10` tags map to D-plan-1, 3, 2 (first chain, `D2b`), 10, 6, 8, 9, 7, 14, 12, 11 in that order — exactly the coverage §10 states for it; traces-3 carries the five chains §10 names. The D-plan-26 chain records its premise honestly; its decision framework never weighed whether the session kind can run tools.
- **The clear axis under AD-9.** Read line by line: two conditions, no vocabulary beyond the stoplist, the stoplist multi-word only, the lean toward clearing. The disclosed residual (a stoplist phrase plus filler clears) is the operationalization AD-9 delegates and is in FR-B5's direction; a model-free rule that held "I'll get to that after the refactor." would also hold the same sentence given as a direct answer to "when?". Not counted.

---

## Convergence Record

- **Round number:** 5.
- **Trajectory (expert-review series):** R1: 10 (3 Serious, 5 Moderate, 2 Minor) → R2: 29 (1 Critical, 5 Serious, 1 Serious-Systemic, 11 Moderate, 11 Minor) → R3: 20 (1 Serious-Systemic, 3 Serious, 5 Moderate, 11 Minor) → R4: 20 (1 Serious-Systemic, 5 Serious, 3 Moderate, 11 Minor) → **R5: 19 (3 Serious, 5 Moderate, 11 Minor)**. The collapse-hunt series' items (R1: 13, R2: 20, R3: 23, R4: 22) are counted in the closure table, not in the trajectory.
- **Flow counts this round:** prior findings closed 35 of 42 (partially closed 4, regressed 3); **recurring 2** (M2 ← ER S1/CH S-1; M4 ← CH M-5); **new 10** (S2, S3, M3, M5, m1, m4, m7, m9, m10, m11); **regressions 7** (S1, M1, m2, m3, m8 by changed text; m5, m6 by consistency) — plus one correction-induced instance inside a recurring finding (M4's probe-02 entry), not double-counted.
- **Tripwire evaluation.** Condition (a): new + regression = 10 + 7 = 17 ≥ closed (35)? **No.** Condition (b): total = 19 ≥ 20? **No** — by one. **The tripwire does not fire.** The margin is stated so the owner can weigh it: one more finding of any severity would have fired it, and the round's Serious count (3) is the series' lowest since R1 while its correction-induced count (7) is the lowest since the correction loop began (R2: 10 per the collapse-hunt, R3: 12, R4: 15).
- **For the owner's separate rule** (a round-5 correction-induced defect count above zero means the second replacement did not hold either): **7** regression-tagged findings (5 wrong changed text, 2 consistency), plus the one correction-induced instance inside M4. The count is stated; the rule's consequence is the owner's. What the count is *not*: the round-4 mechanical class (paths, verbs, scripts consumed before they exist; counts left stale by a moved referent) accounts for two of the seven (m5, m6); the other five are judgment text a check cannot see.

---

## Open Findings Ledger

Not applicable — no operator-directed cycle stop.

---

## Recommended Priority

1. **S1** — state leg 2's invocation in full (permission mode, tool set, driver scrub, `--resume`) and run one counted session before the plan is delivered; remove the V9 sentence; record the marker-less consequence in the report fields.
2. **S2, M2** — reorder or split so no step consumes Step 15's frontends before they exist, and fill `provides:` at every module-creating step so the check can see the next inversion.
3. **S3, M5** — give the five negative-only replays a positive observable and give the replay harness a store before `init` exists.
4. **M1** — walk `init` against an empty store and fix the FTS-state order and writer.
5. **M3, M4**, then **m1–m11**, then the tentative items.

---

## What could not be checked

- Whether the implementing agent's container honours `--permission-mode acceptEdits`/`bypassPermissions` for a child `claude -p` (T3); the flags are not in the plan, so I did not pick one to test.
- Behaviour at the Node 22.16.0 floor (T1).
- The CI log for run 34151517903 (T2).
- The Clear Thought server's own run beyond what the three trace files record; the files were read for coverage of §10 (complete) and the D-plan-24/D-plan-26 chains were read in full (the latter's omitted axis is recorded at S1), not re-executed.
- Whether the round-4 collapse-hunt's D-plan-3 / D-plan-14 conceded notes (`createRequire`, empty test bodies) have changed — both are unchanged and were not counted, as round 4 did.
- Every other claim above rests on a read at the cited lines or on an execution whose command and output are recorded in the finding. Nothing rests on the author's round-5 self-check; where it and the source disagree (the `provides:` closure, the probe-02 entry, the D-plan-28 sequence, the D-plan-26 executability, the Step 7 key list), the source was used.

---

Verdict: NEEDS FIXES (19 findings: 3 Serious, 5 Moderate, 11 Minor; 7 REGRESSION-tagged — 5 text, 2 consistency — plus one correction-induced instance inside M4; tripwire not fired: new + regression 17 < closed 35, total 19 < 20)
