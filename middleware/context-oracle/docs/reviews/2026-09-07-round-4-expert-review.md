# Expert review — `docs/plans/plan-phase-a.md`, round 4

**Date:** 2026-09-07
**Reviewer:** independent `/expert-review` pass (expert-review SKILL.md R1.2, Post-fix protocol). Not the plan's author, not the author of any prior review, not the author of `2026-09-07-round-4-author-gates-review.md`. The prior rounds reached this reviewer only as the written records named below; nothing in the author's self-check was accepted without re-derivation.
**Artifact:** `middleware/context-oracle/docs/plans/plan-phase-a.md` as installed in the working tree at commit `6a2159b` (`git status` clean; 8,160 lines; 16 sections; 40 steps each opening with a `step-decl` block; 122 test specifications; D-plan-1…26; Q1…Q48; 17 probes under `docs/plans/plan-phase-a.probes/`). Graded against the contract revision that requires step declarations, generated regions, the build-order property and probe citations (`.claude/skills/expert-plan/references/output-contract.md` as installed at `6a2159b`).
**Round:** 4. Round 3 = `docs/reviews/2026-09-07-round-3-expert-review.md` (20 findings: S1–S3, SY-1, M1–M5, m1–m11; tentative T2, T3) + `docs/reviews/2026-09-07-round-3-collapse-hunt.md` (23 findings: S-1…S-4, M-1…M-10, m-1…m-9). The round-3 artifact is commit `d667534`, extracted to a scratch file and read at the passages named per finding for provenance; test IDs were renamed `T<n>-<m>` → `T-<n>-<m>` in the conversion, so comparison is by meaning.

Every location is written as `docs/plans/plan-phase-a.md:start-end` (or `path:line`, or `path#section`). `arch` abbreviates `docs/architecture-phase-a.md`, `spec` abbreviates `docs/specs/spec-context-oracle.md`, `r3` abbreviates the `d667534` revision (evidence prose only, never a Location). Every literal-content claim was Read at the cited lines in this session; every execution claim states the command and its observed output.

**Regression tagging rule used below (stated so the owner can apply his stop rule exactly).** A finding is tagged **REGRESSION (introduced by the round-3 corrections)** when the defect did not exist at `d667534` and exists now because of a round-3 correction. Two sub-classes are distinguished at each tagged finding: *(text)* — the differing text itself is wrong; *(consistency)* — a correction changed one site correctly and left an unchanged sibling site now contradicting it. Ten findings are the first class, five the second; both numbers are given in the Convergence Record. A defect present verbatim at `d667534` that no prior round reported is tagged **new**; a partially-closed prior finding is tagged **recurring**.

---

## Scope and Inventory

### Round and sources of the post-fix inventory

Per SKILL.md Step 2 (Post-fix review): the prior reviews' full inventory, the fix-diff files, their dependents, and the prior findings as closure items. The fix-diff is: the plan (`d667534` → `6a2159b`, wholesale conversion to the declaration grammar), the new probe directory `docs/plans/plan-phase-a.probes/` (17 scripts + `expected/`), the new trace file `docs/reviews/2026-09-07-plan-tool-traces-2.md`, the revised skill (`.claude/skills/expert-plan/`), and the author's `2026-09-07-round-4-author-gates-review.md`. None has code dependents (the project's one code file, `tools/check_docs.py`, has zero dependents — `ls -a middleware/context-oracle/` this session: `.claude .mcp.json CLAUDE.md OWNER-LEDGER.md RETHINK.md docs tools`).

### Files in scope — every file below is `[x]`

- [x] `docs/plans/plan-phase-a.md` — **Read in full**, lines 1–8160 in fourteen contiguous reads (1–650, 651–1300, 1301–1950, 1951–2600, 2601–3250, 3251–3900, 3901–4550, 4551–5200, 5201–5850, 5851–6500, 6501–7150, 7151–7800, 7801–8160). Every step 1–40 walked in build order against its declaration; every one of the 122 specifications read against `references/testing-standards.md`; every §11 claim read against its cited source lines; every §10 decision read against its trace label. Grep-verified in addition: `transcript_path|agent_transcript_path` outside §11/§12 (lines 233, 2483, 2485, 2515, 2853, 3075, 3088–3089, 3111, 3992, 4161, 4494, 4511, 4514, 4955); `rejected` (2937–2938 and five unrelated uses); `Register the|register their verbs|dispatch.ts` (356, 3063, 3097, 3101, 3168, 3375); `recordRegret|foldWhisperStats` (3295, 3304 — no call site outside Step 30 names the function).
- [x] `git show d667534:middleware/context-oracle/docs/plans/plan-phase-a.md` (7,106 lines, scratch copy) — Read at: every `**Dependencies.**` field (r3:820–3426, 40 fields); Step 28 item 10 (r3:2619); Step 21 `locate.ts` (r3:2093–2096); Step 27 and Step 31 opening sentences (r3:2501, r3:2788); T14-2 (r3:5269–5280); T18-8 (r3:5445–5459); T38-25 (r3:6416–6419); §7 intro (r3:663–680); Step 28's convention sentence (r3:2626); `deny_loop`/`rejected` (r3:2446, r3:2451).
- [x] `docs/reviews/2026-09-07-round-3-expert-review.md` — Read in full (350 lines). Provenance source for closure items S1–S3, SY-1, M1–M5, m1–m11, T2, T3.
- [x] `docs/reviews/2026-09-07-round-3-collapse-hunt.md` — Read in full (677 lines). Provenance source for closure items S-1…S-4, M-1…M-10, m-1…m-9 and the D-plan notes.
- [x] `docs/reviews/2026-09-07-round-2-expert-review.md` — Read in full (394 lines), for the structure this review mirrors and the standards the round-3 items were originally named against.
- [x] `docs/reviews/2026-09-07-round-4-author-gates-review.md` — Read in full (112 lines). Treated as the author's claims; every closure row re-derived (the rows for ER S2 "T-14-2 reworded", ER M5, and the D-plan-26 trust rule are contradicted by the source — see S3, S5).
- [x] `docs/reviews/2026-09-07-plan-tool-traces-2.md` — grep-verified: every `[D-plan-<n> …]` label (D-plan-2, 4, 5, 6, 8, 10, 13, 15–26 — all present, chains of 2–4 thoughts each); `sequential_thinking","prompt"` count = 51, `decision_framework","prompt"` count = 5 (56 calls, as the author states); Read at lines 1464–1468 (the D-plan-26 chain's premise paragraph).
- [x] `docs/reviews/2026-09-07-plan-tool-traces.md` — grep-verified labels D1, D2, D2b, D3–D10 with their thought counts (18/16/3/10/13/13/8/8/8/8/8), matching §10's statement of which decisions the first file covers.
- [x] `docs/reviews/README.md` — Read in full (39 lines).
- [x] `docs/plans/plan-phase-a.probes/*` — every script and every `expected/*.txt` Read (05, 07, 09, 13, 14, 15, 16 in full; 01–04, 06, 08, 10–12, 17 by their expected outputs and headers); `layout/` inventory listed.
- [x] `.claude/skills/expert-plan/SKILL.md` — Read in full (399 lines).
- [x] `.claude/skills/expert-plan/references/output-contract.md` — Read in full (132 lines).
- [x] `.claude/skills/expert-plan/references/testing-standards.md` — Read in full (141 lines).
- [x] `.claude/skills/expert-plan/scripts/derive-plan-sections.mjs` — grep-read at the build-order and phantom-path checks (lines 45–81 header, 216–273 parser, 428–514 build-order resolution, 537–548 file table, 719–746 self-check cases); `--self-check` executed (31 checks, exit 0).
- [x] `CLAUDE.md` (project) — Read in full (238 lines).
- [x] `OWNER-LEDGER.md` — Read in full (80 lines).
- [x] `docs/specs/spec-context-oracle.md` — Read in full (1,139 lines).
- [x] `docs/architecture-phase-a.md` — Read in full (2,058 lines).
- [x] `docs/collapse-log.md` — headings listed; Read `:1121–1227` (2026-09-04 and 2026-09-07 entries).
- [x] `docs/IDEAS.md:92–175` — Read (#14).
- [x] External / executed, all this session (2026-09-07):
  - `node .claude/skills/expert-plan/scripts/derive-plan-sections.mjs --check docs/plans/plan-phase-a.md` → `OK: 40 steps, 13 elements, 122 test specs, 17 probes cited, regions current`, exit 0.
  - `node .claude/skills/expert-plan/scripts/run-plan-probes.mjs docs/plans/plan-phase-a.md --layout /tmp/claude-0/probe-layout` → **`FAIL 07_sqlite_busy_schedule: output drifted`** (actual: `identical across 3 runs: false`, `B StoreBusy attempts=2`, `rows ["A"]`), 16 probes `ok` (including the optional 09, 12, 13, 14, 15, 17 — each executed, none skipped), `1 probe(s) failed`, exit 1. Three subsequent standalone runs of probe 07 on an idle machine matched the expectation; child start latency measured at +50 ms (`spawn`-to-first-output).
  - `https://code.claude.com/docs/en/hooks` — `curl` (2,821,561 bytes, tag-stripped, grep'd): the Workspace trust section, the frontmatter-hooks paragraph, the cadence sentence, the `ConfigChange` row — quoted in S5.
  - The plan's own clause-level clear-rule reference implementation (probe 16's function) executed over twelve additional direct-answer inputs — output in S4.
  - `https://hub.docker.com/v2/repositories/library/node/tags/22.16.0-bookworm` — tag exists (`status: active`, amd64 image 405,906,531 bytes).
  - Node v22.22.2, typescript 5.9.3 (the prepared probe layout).

### Tool plan (Step 3)

| Claim type | Instrument used |
|---|---|
| Literal content of the plan / spec / architecture / ledger / collapse-log / traces / probes | `Read` at file:line (recorded per finding) |
| Absence claims over the plan ("no step persists X", "no call site names Y") | `grep` over the whole plan with query and hit list recorded, then Read of the candidates |
| Declaration semantics (what `--check` does and does not verify) | Read of `derive-plan-sections.mjs` at the cited lines; `--check` and `--self-check` executed |
| Executed evidence (probes) | `run-plan-probes.mjs` executed against the prepared layout; the failing probe re-executed standalone three times; start-latency measured |
| Runtime behaviour of the plan's clear rule | the plan's own reference implementation (probe 16) executed over additional inputs |
| Hooks-contract claims | `curl` of `code.claude.com/docs/en/hooks`, passages quoted |
| Prior-document claims (both round-3 reviews, the author's gates review) | re-derived from the current plan by Read/grep before any closure was recorded |
| Provenance (new / recurring / regression) | `git show d667534:…`, Read at the passage |
| Structural / blast-radius | not needed — a document with no code dependents |

**Unavailable instruments and disposition.** Clear Thought MCP is not loadable in this session (no `clear_thought` tool exposed); the metacognitive baseline and the pre-delivery multi-perspective check were performed manually (recorded below and in Observations). A Node 22.16.0 binary is not present (v22.22.2 only). No CI runner and no Docker daemon are reachable. The Claude Code Remote session-creation tooling was not exercised (creating sessions to test a premise the plan's own text already contradicts — G3 — was judged unnecessary; recorded in "What could not be checked"). No rigor waivers were requested.

### Metacognitive baseline (recorded before drafting)

Known from verified reads this session: everything cited by line above. Inferred and therefore verified before use: whether the busy-schedule probe reproduces under the probe runner (executed — it did not, once in four runs); whether the plan's clear rule holds on one-word affirmatives and on answers containing the word "later" (executed with the plan's own reference implementation); whether the hooks page's "-p session doesn't count as accepting it" sentence governs settings-file hooks (fetched — it governs subagent frontmatter hooks; the settings-file rule says the opposite for `-p`); whether the declared `depends_on` sets are supersets of the `d667534` Dependencies fields (compared field by field — they are subsets at 32 of 40 steps). Not verifiable here and left at that status: the GitHub Actions runner's user-namespace policy; the Node 22.16.0 floor; the Dockerfile lineage of the specific `22.16.0-bookworm` tag; the behaviour of a Claude Code Remote session with respect to a sibling session's container.

### Round-3 closure table (each of the 43 prior findings re-derived from the current plan; closed only against its originally named standard)

| Prior finding | Original standard | Current state (Read) | Status |
|---|---|---|---|
| ER S1 — Step 28 consumed guard/watchdog (Step 29) and `hook integrity-check` (Step 32) | expert-plan Step 8 Dependencies/Verification; Step 10 checkpoint | `plan:1662-1671` Step 10 creates `watchdog.ts`/`guard.ts` (+T-10-4); `plan:3161-3166` Step 28 creates `integrity_check.ts` and provides `hook-integrity-check`; `plan:3227` Step 29 is verification only. | **Closed for the named artifacts** — the same standard is violated at Step 28 ↔ Step 30 (S2 below), a site round 3 did not name |
| ER S2 — Step 14 spawned a verb that did not exist; T14-2 unbuildable | Step 8; Step 9 observable failure; Gate C | `plan:2001-2007` `refreshIfStale` "spawns nothing"; the handler spawns (`plan:3127-3128`). **But** `plan:6301-6312` T-14-2 still specifies "spawns one reindex … real detached child … **Fails when** … spawns zero or two children". | **Closed at Step 14, REGRESSED at T-14-2** (S3 below) — the author's row "T-14-2 reworded" is false |
| ER S3 / CH M-1 — T3-3's schedule cannot produce `StoreBusy` | testing-standards (able to fail; deterministic) | `plan:5986-5995` new schedule (A 0–400 ms, C at 100, B at 250), `probe:07`. Executed: the probe **drifted under `run-plan-probes.mjs`** (B `StoreBusy`, rows `["A"]`), passed 3/3 standalone. | **Closed in form, REGRESSED** (S6) — the new schedule is timing-dependent, the standard round-2 M6 named |
| ER SY-1 / CH S-1 — T28-2's input-field list collides with the plan's modules | Gate A (a conformant build passes); testing-standards | `plan:3085-3095` list narrowed to wire identifiers; `errorText`/`promptText`/`startSource`/`workingDir`; liveness keys `transcriptPath`/`transcriptBytes` (`plan:3124-3125`). **But** `plan:2482-2486` Step 21's `locate.ts` "reads `transcript_path` … records `agent_transcript_path`" — both in the list. | **Partially closed → recurring** (M2) |
| ER M1 / CH M-5 — cold-container job at Step 1 on a git-less image | Step 8 Verification; Gate C | `plan:798-799` Step 1 defers the job to Step 38; `plan:3910-3916` `node:22.16.0-bookworm` (`probe:14` on the `22` tag). | **Closed** (residuals m1, m7; tentative T1) |
| ER M2 — `passesBar` cannot see staleness | Step 8 signature; AD-23 | `plan:2132-2135` `ctx: {indexStale}`; `plan:2004-2006` `schema_meta.index_stale`; T-16-1 `plan:6347-6349`. | **Closed** |
| ER M3 — Clear Thought attestation contradicted by the trace | expert-plan Step 6; Gate C | `plan:4300-4306` names both files and which governs; traces-2 labels D-plan-2, 4, 5, 6, 8, 10, 13, 15–26 (grep); traces-1 D1–D10. | **Closed** |
| ER M4 — regret has no table/DAO | Step 8; AD-4 criterion | `plan:1408-1415` `regret` DDL; `plan:1576` DAO; `plan:3304` `regret.ts`; T-30-1. | **Closed** |
| ER M5 / CH S-4 — leg 2 not executable; remote stores unreachable | Gate A/C; CLAUDE.md rule 1 | `plan:4016-4058`: bootstrap/counted split, agent creates sessions, export "transferred to the report machine". Transfer mechanism unstated; leg-2 *transcripts* (used by `plan:4078-4079`) are not exported; the trust premise is inverted; "same initialised clone" contradicts G3. | **Partially closed, REGRESSED** (S5) |
| ER m1 — trivial list | Gate C | `plan:707` "8 and 40". | **Closed** |
| ER m2 — `Symbol[]` | Gate C | `plan:1980` `SymbolRow[]`. | **Closed** |
| ER m3 / CH m-1 — `verdict.emit` | Step 8 | `plan:3134-3135` `adapter.toHookResponse({deny: verdict})`. | **Closed** |
| ER m4 / CH m-8 — "three replay tests" | Gate C | `plan:3202-3204`, `plan:4260-4263` list by name. | **Closed** |
| ER m5 — sweep record non-terminating | Step 12; §14 | `plan:8024-8035` passes 4 and 5; pass 5 added zero. | **Closed** |
| ER m6 — brand claim untrue | CLAUDE.md rule 1; Gate C | `plan:2741-2747`; T-24-3 `plan:6691-6694`; `probe:05` (`TS2741`, cast exit 0). | **Closed** |
| ER m7 — `deinit` cannot reach `{}` | Gate C | `plan:3490-3493` prunes emptied arrays and `hooks`. | **Closed** |
| ER m8 — `npx` mode unnamed | Gate C | `plan:6951-6954` `npx --yes --package=<tarball>`. | **Closed** |
| ER m9 / CH M-8 — deferral discard unrecorded / holds on substantive sentences | §10; FR-B1/FR-B5 | D-plan-24 `plan:4684-4699`; clause-level rule `plan:2650-2675`; `probe:16`. **But** the new acknowledgement lexicon and the `later` member hold on direct answers (executed). | **Closed in form, REGRESSED** (S4) |
| ER m10 — Dependencies fields omit consumed steps | Step 8 ("State explicitly") | Replaced by `depends_on`; compared with r3's fields: 32 of 40 steps now declare *fewer* dependencies than `d667534` stated (S1). | **Closed in form, REGRESSED** (S1) |
| ER m11 — `recognizeQuestions` has no mode | Step 8 | `plan:2640-2644` `{requireTerminalMark}`; `plan:3661-3663`; T-23-1 `plan:6608-6610`. | **Closed** |
| ER T2 — floor executed only on 22.22.2 | (tentative) | Unchanged (`plan:5526-5527`, Q28). | **Still tentative** (T2) |
| ER T3 — `unshare -rn` on the runner | (tentative) | `plan:793` `ubuntu-24.04`; T-32-2 `plan:7015-7017` records `EPERM`. | **Closed in form** (T3, narrowed) |
| CH S-2 — `deny_despite_answer_text` counts correct denies | AD-9 `arch:819-826`; FR-B1 | `plan:2934-2942` only `below_length_floor` counts; T-26-1 `plan:6757-6760`. | **Closed** — note the exclusion now also covers the plan's new `acknowledgement_only` class, which makes S4's wrongful hold undetectable |
| CH S-3 — leg-1 off-policy denies unsplit | IDEAS #14; §11.5 | `plan:4101-4112` every number per leg; leg 1 count only, labelled. | **Closed** |
| CH M-2 — labelling rule attributed to OL-C5; labeller not blind | collapse-log 2026-08-25 items 1, 4 | `plan:4079-4089` rule is D-plan-26's; before replay, no store; order recorded. | **Closed** (leg-2 caveat noted in S5(c)) |
| CH M-3 — origin read from markers | V12; AD-24 | `plan:3998-4008` `--corpus <machine>/<mode>=<dir>`; T-38-32 `plan:7585-7589`. | **Closed** |
| CH M-4 — "re-edited or reverted" undefined | FR-L4; AD-18 | `plan:3306-3313`; T-30-1 single-edit case `plan:6921-6923`. | **Closed** |
| CH M-6 — D-plan-13 vs Steps 1/28 | §10 attestation | `plan:4557-4569`, `plan:795-799`, `plan:3115-3116` consistent. | **Closed** |
| CH M-7 — dead wiring invisible | AD-17; L7; OL-10 | `plan:3587-3594` totally-dead half; T-33-4 (c) `plan:7089-7091`; D-plan-25. | **Closed** (m8, m10 residuals) |
| CH M-9 — `Stop` per assistant entry | hooks cadence | `plan:3987-3991` one `Stop` per turn. | **Closed** |
| CH M-10 — lexicon members unspecified/mislabelled | D-plan-7's own rule | `plan:1829-1840` the three QA lists enumerated as `plan_seed`. **But** `plan:1841-1848` the command-class and completion lexicons carry plan-chosen members labelled `architecture_default`. | **Partially closed** (m9) |
| CH m-2 — Step 25 Verification still describes the hold | collapse-log 2026-09-03 (fix at every site) | `plan:2897-2901`. | **Closed** |
| CH m-3 — `tuning_missing` fallback contradiction | Gate C | `plan:1799-1803` `tuning_seeds.ts` single source. | **Closed** |
| CH m-4 — done-claim fires on a negated sentence | D-38 | `plan:2301-2304` negation clause added. **But** T-18-8 `plan:6485-6494` carries no negated case (r3:5453–5459 identical). | **Partially closed** (m6) |
| CH m-5 — `large-store` size | AD-23/V8 | `plan:801` ≈400 MB, cached by hash; T-29-1 `plan:6886-6887`. | **Closed** |
| CH m-6 — D-plan-19 overclaims | collapse-log 2026-09-03 round 8 | T-18-8 grammar `plan:6488-6493`; T-23-3 generated set `plan:6650-6653`; §10A `plan:5101-5105`. | **Closed** |
| CH m-7 — leg 2 asks only `?` questions | §11.5 | `plan:4042-4045` one indirect ask per counted session. | **Closed** |
| CH m-9 — leg-1 corpus unsplit | §11.5 | `plan:4009-4013` split by repository class. | **Closed** |
| CH D-plan-23 note — `fetch(` token scan | (note) | `plan:3505-3506` four tokens; T-32-3. | **Closed** |
| CH D-plan-3 / D-plan-14 notes | (notes) | Unchanged, conceded holes. | **Not counted** |

31 of the 43 prior findings are closed against their original standard; 12 are partially closed or regressed and recur below (ER S2 → S3; ER S3/CH M-1 → S6; ER SY-1/CH S-1 → M2; ER M5/CH S-4 → S5; ER m9/CH M-8 → S4; ER m10 → S1; CH M-10 → m9; CH m-4 → m6). Fifteen of this round's findings are defects the round-3 corrections introduced (tagged REGRESSION; ten by changed text, five by consistency).

---

## Summary

This review returns **NEEDS FIXES**. The round-3 corrections closed 31 of 43 findings, the mechanical checks the new process added do run (`--check` exit 0; `--self-check` 31/31), the package pins and the Node floor are unchanged, no scope element moved, and on the intake and move axes the answer-drift block is still the AD-9 skeleton. But the plan regressed on every axis the dispatch asked to be checked with particular care. **(1) Build order:** the conversion to `depends_on` declarations *dropped* dependencies at 32 of 40 steps relative to `d667534` — Step 25 (the block) now declares only Step 1, Step 18 (every genre) only Step 1, Step 22 (the qa DAO) omits the DAO step, Step 40 omits the exit run — and the new check cannot see any of it because consumption is by function name and `provides:` is empty at 33 steps; the handler at Step 28 still calls Step 30's fold and regret modules; T-14-2 still asserts the spawn Step 14 no longer performs. **(2) Leg 2:** the workspace-trust premise the protocol rests on is the hooks page's rule for *subagent frontmatter* hooks inverted — for settings-file hooks a `-p` session *treats the folder as trusted and runs them* — and probe 13 verified the sentence's presence, not its scope; the counted sessions are to run "on the same initialised clone" as a bootstrap session, which the plan's own G3 says a remote session does not have; the floor sample draws on leg-2 transcripts that `export` never moves. **(3) The clear rule:** the new acknowledgement lexicon and the bare `later` member, executed with the plan's own reference implementation, hold on "Sure.", "Right.", "Understood." and on "Because the fixture is written later than the assertion reads it." — a wrongful deny of a direct answer that AD-9 clears, FR-B5 forbids, and Step 26 now excludes from the automated detector. And the probe regime's own gate failed: `run-plan-probes.mjs` exited 1 on probe 07 in this session. Twenty findings: 1 Serious-Systemic, 5 Serious, 3 Moderate, 11 Minor — fifteen of them regressions.

---

## Upstream Contract Verification

The plan's upstream artifacts are the spec (OL-C6-signed, `spec:3-5`; `OWNER-LEDGER.md:71`) and the Phase A architecture. Both were Read in full this session.

**Spec §14 acceptance criteria — Phase A subset** (verified state = the plan's §12 specification; method = Read of the cited plan lines against the cited spec lines):

| AC | Plan mapping (§12.4, `plan:7606-7646`) | Verification | Verdict |
|---|---|---|---|
| AC-1, 1a–1d | T-18-2/T-38-10; T-18-1/T-38-11; T-18-3/T-38-12; T-18-4/T-38-13; T-18-6/T-38-14 | Read `plan:6379-6460`, `plan:7273-7335` vs `spec:914-931` | Honored |
| AC-2 | T-24-1, T-24-2, T-24-3, T-28-2 | Read `plan:6658-6697`, `plan:6795-6804` vs `spec:932-939` | Honored in design; T-28-2 still fails a conformant build at one site (M2) |
| AC-2a, AC-2a-i allow-half | T-25-1, T-25-3, T-38-1, T-38-2, T-38-3 | Read `plan:6699-6746`, `plan:7148-7186` vs `spec:940-960` | Honored |
| AC-2c over-fire / under-fire | T-38-30, T-38-4 / T-34-2 | Read `plan:7540-7555`, `plan:7188-7201`, `plan:7108-7119` vs `spec:979-999` | **Partially violated** — the over-fire clause ("an agent that did answer (reworded)… no deny fires") is contradicted for one-word affirmatives and `later`-bearing answers (S4); T-38-30 pins only "No." |
| AC-3, 3a, 4, 5, 6 | T-16-1/T-38-15; T-16-1/T-38-16; T-20-1/T-38-17; T-20-1/T-38-20; T-13-1/T-38-18 | Read `plan:7336-7383` vs `spec:1000-1014` | Honored |
| AC-7 | T-31-1, T-31-2, T-32-1 | Read `plan:6942-7006` vs `spec:1015-1016` | Honored |
| AC-8, AC-8a | T-18-7/T-38-28; T-27-2/T-38-9 | Read `plan:6461-6475`, `plan:7508-7523`, `plan:7257-7271` vs `spec:1017-1034` | Honored |
| AC-9 | T-10-1, T-29-1, T-14-2, T-28-4, T-33-4, T-26-1 + T-38-6, T-38-8, T-33-1 | Read `plan:7630` and each test vs `spec:1035-1049` | Honored in specification; T-14-2 cannot pass on a conformant build (S3); `deny_loop`/`deny_despite_answer_text` read records no step persists (M1) |
| AC-10 | T-29-1, T-28-3 | Read `plan:6882-6899`, `plan:6831-6842` vs `spec:1050-1052` | Honored |
| AC-11 | T-11-1..4, T-38-22, T-32-3 | Read `plan:7420-7439`, `plan:7028-7042` vs `spec:1053-1055` | Honored |
| AC-12 (deterministic parts) | T-38-1, T-38-4, T-36-1 | Read vs `spec:1056-1062` | Honored |
| AC-13 | T-13-1, T-14-1, T-14-2, T-16-1 | Read `plan:6265-6355` vs `spec:1063-1065` | Honored in specification (T-14-2 as above) |
| AC-14, AC-15, AC-17 | T-19-1; T-38-23; T-38-24 | Read vs `spec:1066-1080` | Honored |
| AC-18 | T-38-27 + Step 39 leg 3 | Read `plan:7494-7506`, `plan:4067` vs `spec:1081-1086` | Honored |
| AC-19 | T-32-2, T-32-3 | Read `plan:7008-7042` vs `spec:1087-1089` | Honored (runtime leg tentative T3) |
| AC-20 | T-38-25 | Read `plan:7466-7476`, `plan:3910-3916` vs `spec:1090-1092` | Honored in specification; the test's File field names a job at the wrong step (m1) |
| AC-21 (mechanism) | T-5-2, T-5-3, T-29-2 | Read vs `spec:1093-1096` | Honored |
| AC-22 | T-38-26 | Read `plan:7478-7492` vs `spec:1097-1100` | Honored |
| AC-23, AC-24 | T-30-2, T-34-1, T-35-1, T-35-2; T-30-1 | Read `plan:6930-6940`, `plan:7098-7138`, `plan:6911-6928` vs `spec:1101-1113` | Honored |
| Deferred per spec §14 phasing: AC-2a-i deny-half, AC-2a-ii, AC-2b, AC-2c skill under-fire, AC-16, AC-21 full, AC-25 | — | `plan:7616-7621`, `plan:7637`, `plan:7642`, `plan:7646` vs `spec:1122-1130` | Correctly deferred |

**Architecture in-scope list (`arch:48-75`) against §2.3 (`plan:160-204`):** the seven genres (PA-1), the safe skeleton with its seam (PA-2/PA-8), stores/index/miner (PA-3), delivery with Stop-time injection and the AC-8a line (PA-4, S27 under PA-2), self-observability with regret and deny health (PA-5), security (PA-6), the learning-loop slice (PA-7 + FR-L1 under PA-5), CLI surface (PA-11), packaging (PA-12), test/fixture architecture and build-time verifications (PA-10), recursion guard and degraded posture (PA-9) — every element maps; nothing added, nothing dropped. One vocabulary slip: Step 10 creates `guard.ts` and covers PA-5 only (m3). **Package versions, Node floor, scope:** `web-tree-sitter` 0.26.13, `tree-sitter-wasms` 0.1.13, `engines >=22.16.0` (`plan:735-737`) = `arch:138`, `arch:327-328`; dev pins 5.9.3 / 22.20.1 as D-plan-2 records; grep `0\.27\.0` → 4 hits (`plan:247`, `plan:4341`, `plan:5567`, `plan:8117`), each naming the release the plan does not adopt. Unchanged.

**Architecture decisions** (honored / violated; method = Read of the cited ranges):

| AD | Verdict | Evidence |
|---|---|---|
| AD-1, AD-7 | Honored | `plan:3118-3153` exit 0 always; no daemon |
| AD-8 | Honored in order; **violated in buildability** | `plan:3150-3151` item 10 consumes Step 30's modules two steps early (S2) |
| AD-2, AD-25, AD-26 | Honored | `plan:733-746`, `plan:939-960`, `plan:3294-3300` |
| AD-3 | Honored | `plan:1082-1107`; D-plan-15 |
| AD-4, AD-5 | Honored | `plan:1321-1431`, `plan:1499-1514` re-read row by row against `arch:424-534`, `arch:572-621`; the two plan additions (`observed_actions.content_hash`, `regret`) are marked and reasoned |
| AD-6, AD-20 | Honored | `plan:3072-3095`, `plan:3390-3418` |
| AD-9 | **Honored on the intake and move axes; violated on the clear axis.** Question recognizer `plan:2640-2649` = `arch:750-760`; move recognizer `plan:2676-2678` = `arch:793-797`; intake `plan:2823-2826`; catch-up `plan:2827-2840`; deny decision `plan:2841-2848`; hold `plan:2850-2859` = `arch:812-826`; lifetime/backstop `plan:3004-3021` = `arch:828-839`, `arch:852-867`; seam `plan:2569-2589`. **Clear recognizer `plan:2650-2675` adds an acknowledgement lexicon and a clause-drop rule that AD-9 (`arch:778-786`: "length above a small floor … and is not a recognized content-free deferral") does not contain, and that holds where AD-9 clears** (S4). | as cited |
| AD-10 | Honored | `plan:2738-2763`; `probe:05` |
| AD-11 | Honored in design; one wire-name residue in `locate.ts` (M2) | `plan:2482-2508` |
| AD-12, AD-13, AD-14, AD-15, AD-16 | Honored | Steps 14/15, 13, 16, 17/18, 20 vs `arch:997-1211` |
| AD-17 | Honored in enumeration; two detectors read unpersisted state (M1) | `plan:2925-2952`, `plan:3570-3599` |
| AD-18 | Honored | `plan:3304-3323`, `plan:3657-3670` |
| AD-19, AD-21, AD-22, AD-23 | Honored | Steps 11/19; 5/29/36; §2.2; Step 29 with §4 |
| AD-24 | Honored in content; the exit-run leg it names as agent-driven rests on a false harness premise (S5) | `plan:3894-3916`, `plan:3972-4124` |

---

## Critical & Serious Findings

No Critical findings — the full inventory was Read or Grep-verified per Compliance Gate B, and no violation of Critical classification was observed (`--check` exits 0; the Step 1 layout compiles).

### S1 (Systemic — see also SY-1) — The step declarations drop real dependencies at 32 of 40 steps relative to `d667534`; the build-order check cannot see it because consumption is by function name and `provides:` is `[]` at 33 steps — **REGRESSION (introduced by the round-3 corrections — the conversion that closed ER m10; text)**

**Class:** false declaration (the declared graph is the contract's single source of truth and it is wrong); the round-3 lesson reproduced one layer down.
**Location:** `docs/plans/plan-phase-a.md:2818` (Step 25 `depends_on: [S1]` — the block, which calls `openQuestion`/`answerQuestions`/`voidQuestion` (Step 22, `plan:2571-2589`), `TranscriptReader` (Step 21), `recognizeQuestions`/`recognizeClearing`/`recognizeMove` (Step 23), `whisper_audit.append` (Step 9), `recordFault` (Step 10), `tuning` rows (Step 12) — r3:2418 declared "Steps 12, 21, 22, 23, 24"); `docs/plans/plan-phase-a.md:2251` (Step 18 `depends_on: [S1]` — seven generators over `Store` (Step 3), the DAOs (Step 9), `tuning` (Step 12), `cochange_pairs` (Step 13), `symbol_refs`/`entry_score` (Step 14), `classifyBashCommand` (Step 17), `Candidate`/`EventContext` (Step 6) — r3:1986 declared "Steps 9, 12, 13, 14, 15, 16, 17"); `docs/plans/plan-phase-a.md:2565` (Step 22 `depends_on: [S1, S7]` — its DAO composes Step 9's `questions` methods, `plan:1571`); `docs/plans/plan-phase-a.md:2067` (Step 15 `depends_on: [S1]` — implements Step 14's `LanguageFrontend`, `plan:2071-2072`); `docs/plans/plan-phase-a.md:1900` (Step 13 `[S1]` — DAOs, `tuning`, the spawn wrapper "the miner's `git log` (Step 13)" goes through it, `plan:1123-1125`); `docs/plans/plan-phase-a.md:1791` (Step 12 `[S1, S6]` — writes the `tuning` table Step 8's migration creates); `docs/plans/plan-phase-a.md:1547` (Step 9 `[S1, S7]` — `global_meta`/`whisper_stats`/`lessons` DAOs over Step 8's tables); `docs/plans/plan-phase-a.md:1643` (Step 10 `[S1, S6]` — "a thin wrapper over the `session_log` DAO", Step 9); `docs/plans/plan-phase-a.md:3565` (Step 33 `[S1, S2, S4, S5, S26]` — `status` reads `regret` (Step 30), `faults`/`session_log` (Step 9), `tuning` (Step 12), registers in `dispatch.ts` (Step 28), and its four tests are replays through the built binary); `docs/plans/plan-phase-a.md:3714` (Step 35 `[S1]` — `note` registers in Step 28's switch, writes Step 9's DAOs, tests are replays); `docs/plans/plan-phase-a.md:4191` (Step 40 `[S1]` — rewrites STATUS "with the exit report's location", Step 39); also `plan:1971` (S14 drops S5, S9), `plan:2354` (S19 drops S9, S11, S18 — `compose` reads the `commits` table), `plan:2416` (S20 drops S9), `plan:2478` (S21 drops S6, S9), `plan:2921` (S26 drops S9, S25), `plan:3000` (S27 drops S22), `plan:3068` (S28 drops S9, S19), `plan:3290` (S30 drops S9, S28), `plan:3653` (S34 drops S9, S28, S33), `plan:3890` (S38 drops S15). `provides: []` at every step except S1, S28, S31–S35 (`plan:727`, `plan:3066`, `plan:3369`, `plan:3480`, `plan:3563`, `plan:3651`, `plan:3712`).

**What the plan says.** The contract makes the declaration "the single source of truth … the prose fields of the step must agree with it, and where they diverge the declaration is what the derivation script and the reviewer read" (`output-contract.md:38`). Read as the reviewer is told to read it, the declared graph says the answer-drift block can be built with nothing but the package skeleton, and that `status` can be built before the CLI dispatcher exists. Every prose "Dependencies" field now reads "Declared above (`depends_on`)" (`plan:2891` and 39 siblings), so the explicit naming Step 8 requires exists nowhere.

**How verified.** Every current `depends_on` list compared with the `**Dependencies.**` field of the same step at `d667534` (r3:820–3426, 40 fields, all Read): the current set is a strict subset of the r3 set at S3, S4, S5, S6, S7, S9, S10, S11, S12, S13, S14, S15, S17, S18, S19, S20, S21, S22, S23, S24, S25, S26, S27, S28, S29, S30, S31, S33, S34, S35, S38, S40 (32 steps); a superset only at S16, S28 (adds S3), S38 (adds S29, S30). Read of `derive-plan-sections.mjs:428-514`: the build-order check resolves *backticked* mentions that equal a created path, a trailing path segment, or a `provides:` token joined by `-`; a function name (`openQuestion`, `recognizeClearing`, `classifyBashCommand`) matches none of those, so an undeclared module consumption passes. `--check` exit 0 on this document (executed) is therefore consistent with the graph being wrong.

**Standard violated.** expert-plan Step 8 ("**Dependencies** — what must complete before this step … State explicitly — the implementer may be an agent that needs to know the exact ordering"); output-contract item 7 (the declaration as source of truth for "the derived surfaces … and for the step↔test cross-reference"); Gate C (the check "every backticked artifact a step names created or provided by … a declared or transitive dependency" is satisfied vacuously); collapse-log 2026-09-07 ("the author's check reconciled identifiers while the reviewers walked the build in order" — the new check reconciles backticked paths while the prose consumes by function name). Provenance: the `depends_on` lists are new text; the r3 fields they replaced were more complete. **REGRESSION (text).**

**Required change (hypothesis).** The declared graph should be re-derived from consumption, not from the r3 chain: for each step, `depends_on` = every earlier step whose *exported function, type, table, or verb* the step's "What changes" names (a re-derivation the author can script from the "Creates." fields and the identifiers each step's prose uses), and each step's `provides:` should carry the exported function names later steps consume (`openQuestion`, `recognizeClearing`, `classifyBashCommand`, `passesBar`, `compose`, `recordFault`, …) so that the check's *k*-word rule resolves them. Evidence that would confirm the hypothesis: after re-derivation, `--check` reports at least one "not among … declared or transitive dependencies" error on the *unfixed* Step 28 (its text names `whisper_stats` fold and `regret`), and zero after S2's fix; and no step's `depends_on` is a subset of its r3 field unless the r3 edge was a chain artifact (S3→S2, S4→S3, S5→S4, S6→S4 are the four such cases).

---

### S2 — Step 28's handler consumes Step 30's `whisper_stats` fold and regret proxy two steps before they exist, and Step 30's `recordRegret` is "called … from `runIndex` refresh (Step 14)" with no step that wires it; Checkpoint 3 is not executable as written — **new** (present at `d667534`, unreported)

**Class:** topological inversion between step artifacts and step text (the round-3 S1 class at a site round 3 did not name).
**Location:** `docs/plans/plan-phase-a.md:3150-3151` (Step 28 item 10: "`SessionEnd`: finalize the session diagnostics row; the `whisper_stats` fold and the regret proxy (Step 30)."), `docs/plans/plan-phase-a.md:3068` (Step 28 `depends_on` has no S30), `docs/plans/plan-phase-a.md:3294-3302` (Step 30 creates `whisper_stats_fold.ts`; "Run points: `SessionEnd` (handler) and the `correct` verb (Step 34)"), `docs/plans/plan-phase-a.md:3320-3321` ("Called at `SessionEnd` and from `runIndex` refresh (Step 14) so a between-session revert is caught"), `docs/plans/plan-phase-a.md:3285-3287` (Step 30 `modify: []`), `docs/plans/plan-phase-a.md:3202-3204` (Checkpoint 3 "runs here: every function-level test of Steps 21–28 and the replay tests above"), `docs/plans/plan-phase-a.md:4258-4266`.

**What the plan says.** At Step 28 `handler.ts` is written with a `SessionEnd` branch that calls the fold and the regret proxy. `src/diag/whisper_stats_fold.ts` and `src/diag/regret.ts` are created at Step 30; Step 30 declares no modification of `handler.ts` or `indexer.ts`. Either the Step 28 build imports two missing modules (`tsc -p` red; T-28-1/T-28-3/T-28-4/T-28-5 cannot spawn the binary; Checkpoint 3 blocked), or Step 30 silently edits Step 28's and Step 14's files, which its declaration denies and §16's file-list reconciliation cannot distinguish. The same holds for `runIndex` (Step 14) calling `recordRegret` (Step 30).

**How verified.** Read of the cited lines; grep `recordRegret|foldWhisperStats` → definitions only (`plan:3295`, `plan:3304`), no call site in Step 14 or Step 28's text names the function; r3:2619 carries the same item-10 sentence and r3:2748 the same `runIndex` sentence — **new** (unreported).

**Standard violated.** expert-plan Step 8 (Dependencies; "**Verification** — how the implementer will confirm this step is correct *after building it*"); Step 10 (a checkpoint verifies "the accumulated state at that point"); output-contract item 7 ("a step consuming something that does not exist when it is built"); the plan's own §7 rule (`plan:682-683`).

**Required change (hypothesis).** Either the two `SessionEnd` calls and the `runIndex` call are wired by Step 30 — which then declares `modify: [src/hook/handler.ts, src/index/indexer.ts]`, so §5.1 and §16's reconciliation see the edits and Step 28's handler ships a `SessionEnd` branch that only finalizes the diagnostics row — or Step 30's two modules move before Step 28 (they depend on Steps 5, 9, 14 only) and Step 28 declares the dependency. Evidence that would confirm: with `provides: [record-regret, fold-whisper-stats]` on Step 30 and the handler text backticking `recordRegret`, `--check` fails on the current order and passes after the move; the first `npm run build` at Step 28 in a dry-run walk compiles.

---

### S3 — T-14-2 still specifies the spawn that Step 14's `refreshIfStale` no longer performs ("spawns one reindex … **Fails when** … spawns zero or two children"), so the test fails on a conformant build; the author's closure row "T-14-2 reworded" is false — **REGRESSION (introduced by the round-3 corrections — the ER S2 closure; consistency)**

**Class:** a test that fails on a correct implementation; a fix landed at one of two sites (collapse-log 2026-09-03 round 6).
**Location:** `docs/plans/plan-phase-a.md:6301-6312` ("**T-14-2 — `refreshIfStale` records `index_stale` and spawns one reindex.** … **Level.** Integration (real store, real git fixture, real detached child through the spawn wrapper). … **Fails when** the stale call records no `index_stale` fault, OR spawns zero or two children, OR the fresh call records a fault or spawns a child."), `docs/plans/plan-phase-a.md:2001-2007` (Step 14: "`refreshIfStale(store, repoPath): { stale: boolean }` … it spawns nothing — the caller that owns a binary (the handler, Step 28) starts the detached reindex"), `docs/plans/plan-phase-a.md:2046-2049` (Step 14's Verification describes T-14-2 as the flag/fault/return-value test), `docs/reviews/2026-09-07-round-4-author-gates-review.md:30` ("T-14-2 reworded").

**How verified.** Read of the cited lines; r3:5269–5280 is identical to the current T-14-2 text — the Step 14 change is the round-3 correction and the test was not touched.

**Standard violated.** testing-standards core discipline ("Every test must be able to fail, and the specification states what makes it fail" — the stated failure fires on correct behaviour); Gate A ("Can a reviewer check a build against this … including whether each test was built to its specification?" — two specifications of one test disagree); CLAUDE.md rule 1 (the gates review asserts a rewording that did not happen).

**Required change (hypothesis).** T-14-2's Verifies/Level/Data/Fails-when should be re-derived from Step 14's current text (moved `HEAD` → `index_stale` fault + `schema_meta.index_stale='1'` + `{stale:true}`; unmoved → nothing; completed `runIndex` → `'0'`), with the spawn assertion moved to a Step 28 replay (T-28-5's `SessionStart` stream, asserting the `.reindex.lock` a real child writes). Evidence that would confirm: the two texts name the same observables and the `Fails when` clause names no child process.

---

### S4 — The clause-level clear rule's new acknowledgement lexicon and the bare `later` member of the deferral stoplist hold on direct answers ("Sure.", "Right.", "Understood.", "Because the fixture is written later than the assertion reads it."), an elaboration beyond AD-9 in the hold direction that FR-B5 forbids; T-23-2/T-38-30 pin the asymmetry ("Yes." clears, "Ok." does not) and Step 26 excludes the class from the automated detector — **REGRESSION (introduced by the round-3 corrections — the CH M-8 / ER m9 and CH M-10 closures; text)**

**Class:** elaboration beyond the spec §11.5 safe skeleton on the deny path; wrong error direction (over-fire) on the clear axis; the wrongful case made undetectable.
**Location:** `docs/plans/plan-phase-a.md:2650-2675` (Step 23: "discard every clause that contains a deferral-stoplist phrase and every clause that is, after trimming punctuation and case, exactly an acknowledgement-lexicon member; the turn clears when the surviving text is at least `lengthFloorChars` … an acknowledgement-only turn ("Ok.", "Got it, will do.") … do not clear"), `docs/plans/plan-phase-a.md:1832-1840` (Step 12: `lexicon.deferral_stoplist` includes `later`, `in a moment`, `first let me`, `before i answer`; `lexicon.acknowledgement` = `ok, okay, sure, got it, will do, understood, noted, right, alright, gotcha`), `docs/plans/plan-phase-a.md:4684-4699` (D-plan-24: "A phrase-level strip leaves "Sure" / "Ok" fragments that clear on the floor, so the acknowledgement lexicon … is the named vocabulary that closes that hole"), `docs/plans/plan-phase-a.md:6623-6641` (T-23-2: "No." and "Yes, line 12." clear; "Ok." and "Got it, will do." no clear, `acknowledgement_only`), `docs/plans/plan-phase-a.md:7547-7550` (T-38-30 pins the terse "No." only), `docs/plans/plan-phase-a.md:2934-2942` (Step 26: "Turns rejected as `deferral_only` or `acknowledgement_only` are excluded"), `docs/plans/plan-phase-a.probes/16_clear_rule_cases.mjs:4-10` (the reference implementation: `DEFER.some(d => low.includes(d)) || ACK.has(low)`), `docs/plans/plan-phase-a.probes/expected/16_clear_rule_cases.txt` (`"Yes." -> true`, `"Ok." -> false`).

**What the sources say.** `arch:778-786` (AD-9): the clear recognizer "marks all currently-open questions `answered` … when the text carries substance (length above a small floor after stripping tool noise) and is not a recognized content-free deferral ("I'll get to that"-class)" — two conditions, no acknowledgement vocabulary, no clause arithmetic. `spec:436-447` (FR-B5): the answer-drift recognizer "errs **toward not denying** … and **toward clearing** on a substantive answer (only an empty deferral fails to clear)". `spec:367-370` (FR-B1): the dodge is "a content-free deferral ('I'll get to that')". `OWNER-LEDGER.md:70` (OL-C5): the required next move is "a direct answer". `spec:744-749` (§11.5): a conservative recognizer that "errs hard toward not-firing". `arch:1708` (Gate A note): stoplist words "are implementation vocabulary, not architecture" — vocabulary, not a new rule.

**How verified.** The plan's own reference implementation (probe 16's `clears`) executed over additional inputs on Node v22.22.2:

```
"Yes."                                                                -> true
"No."                                                                 -> true
"Sure."                                                               -> false
"Ok."                                                                 -> false
"Right."                                                              -> false
"Understood."                                                         -> false
"Yes, absolutely."                                                    -> true
"Sure, it is safe to rename."                                         -> true
"Because the fixture is written later than the assertion reads it."   -> false
"The deploy fails because the migration runs later than the seed."    -> false
"First let me check: the null check is not the cause."                -> true
"It is safe; the helper is only used in tests."                       -> true
```

Under AD-9's two conditions every one of these turns clears (each is above the floor and none is a deferral); under the plan, "Sure." to "should I rename the helper?" and a one-clause causal answer containing "later" leave the question open, so the agent's next `Edit` — the edit that *is* the answer, or the ordinary next step after a real answer — is denied. `acknowledgement_only` is excluded from `deny_despite_answer_text` (`plan:2934-2942`), so the wrongful hold on "Sure." reaches no automated detector; the `later` case reaches it only after `deny.despite_answer_text_threshold` (3) further denies. Provenance: r3:2229–2240 discarded *sentences* carrying a deferral phrase and had no acknowledgement lexicon (round-3 M-8 found that rule held on substantive sentences); the clause rule, the acknowledgement list and the enumerated `later`/`first let me` members are the round-3 corrections — **REGRESSION (text).**

**Standard violated.** spec §11.5 / D-41 and CLAUDE.md dominating rule 3 (no mechanism beyond the safe skeleton; the 2026-09-04 collapse was a clear-axis-adjacent lexicon that grew round by round); FR-B5 (the lean on the clear axis is toward clearing — the plan flips it for a class AD-9 clears); AD-9 fidelity (a third condition and a fourth vocabulary); testing-standards ("the specification states what makes it fail" — T-23-2 pins "Ok." as non-clearing while the plan's own probe records "Yes." clearing, an asymmetry no rule in the plan explains).

**Required change (hypothesis).** The clear axis should carry exactly AD-9's two conditions: strip tool noise and fences; a turn clears when its text is above the small floor and is not a recognized content-free deferral — where "content-free deferral" is a turn *consisting of* a deferral phrase (plus punctuation), not a turn *containing* one; the acknowledgement lexicon is dropped; `later` and the other single common words leave the deferral list (a deferral-phrase member is a phrase such as `i'll get to that`, per AD-9's own example); a "Sure." that is a dodge is the human channel's case, exactly as AD-9 files the deferral-false-match miss (`arch:823-826`). D-plan-24's premise ("Sure"/"Ok" fragments *must not* clear) is the sentence to attack: FR-B5 says the opposite lean. Evidence that would confirm: the twelve inputs above all clear under the re-derived rule; "I'll get to that." and "I'll get to that after the refactor." do not; T-23-2 and T-38-30 carry "Sure." and a `later`-bearing causal answer as *clears* cases; Step 26's exclusion shrinks back to AD-9's "deferral-stoplist turns".

---

### S5 — Leg 2's protocol rests on an inverted reading of the hooks reference's workspace-trust rule (the "-p session doesn't count as accepting it" sentence governs *subagent frontmatter* hooks; for settings-file hooks a `-p`/SDK session "treats the folder as trusted" and runs them), on counted sessions running "on the same initialised clone" as a bootstrap session (which the plan's own G3 says a remote session does not have), and on leg-2 transcripts that `export` never moves; probe 13 verified the sentence's presence, not its scope — **REGRESSION (introduced by the round-3 corrections — the CH S-4 / ER M5 closure; text)**

**Class:** false external premise stated as documentation evidence; unbuildable-as-written protocol; data unreachable by the report (the round-3 M5 gap re-opened one level down).
**Location:** `docs/plans/plan-phase-a.md:4033-4038` (Step 39: "Hooks written to a settings file are loaded when a session starts and follow the workspace-trust rule — an interactive session holds them back until the trust dialog is accepted and a `-p` session does not count as accepting it (hooks reference, §11.4) — so the session that installs is never the session that measures."), `docs/plans/plan-phase-a.md:4039-4041` ("**Counted sessions.** At least **three** sessions per repository, each created after the bootstrap session on the same initialised clone so the hooks load at startup."), `docs/plans/plan-phase-a.md:4022-4026` ("**The implementing agent creates every session with its own session tooling** (the session-creation and messaging tools its environment provides)"), `docs/plans/plan-phase-a.md:4053-4058` ("every session … ends with `ctxoracle export <dir>` and the directory is transferred to the report machine (a remote session's `~/.ctxoracle` lives in its own container, G3 …)"), `docs/plans/plan-phase-a.md:4078-4079` ("From the human turns of the leg 1 corpus and the leg 2 transcripts, `exit-run.sh` draws a seeded random sample"), `docs/plans/plan-phase-a.md:5552-5554` (§11.4: the hooks reference "states that hooks in settings files follow the workspace-trust rule (an interactive session holds them back until the trust dialog is accepted; a `-p` session does not count as accepting it)"), `docs/plans/plan-phase-a.md:4722-4728` (D-plan-26), `docs/plans/plan-phase-a.md:7952-7958` (Q43), `docs/plans/plan-phase-a.md:8072-8079` (G3: "a remote session's container holds only its own transcript"), `docs/plans/plan-phase-a.probes/13_hooks_reference.optional.sh:9-11` (checks the literal strings "same workspace trust rule as hooks in settings files" and "A -p session doesn't count as accepting it"), `docs/reviews/2026-09-07-plan-tool-traces-2.md:1464` (the D-plan-26 chain's premise: "the hooks reference (probe 13) says hooks in settings files follow the workspace-trust rule and a -p session does not count as accepting it").

**What the source says (fetched 2026-09-07, `curl`, tag-stripped).** Workspace trust section: *"Claude Code checks workspace trust before it runs any hook from a settings file. What counts as trusted depends on the session type: Interactive session: Claude Code holds back hooks from every settings file, including your own ~/.claude/settings.json, until you accept the workspace trust dialog for the folder, or for a parent directory whose trust extends to it. -p or SDK session: Claude Code never shows the dialog and treats the folder as trusted, so hooks committed in a repository's .claude/settings.json run in a folder you've never trusted."* The sentence probe 13 matches lives in the skill/subagent frontmatter paragraph: *"Frontmatter hooks in a project skill follow the same workspace trust rule as hooks in settings files. Claude Code registers them when you or Claude invoke the skill, including in a -p run in a folder you haven't trusted. Frontmatter hooks in a project subagent run only after you accept the workspace trust dialog for the folder the agent file came from. A -p session doesn't count as accepting it."* The two probe-13 strings come from two different rules; joined, they produce a rule the page does not state.

**What the plan says, and why it cannot execute.** (a) The premise "a `-p` session does not count as accepting it" is asserted for settings-file hooks and is the opposite of the documented rule for that case; D-plan-26, Q43, §11.4 and the trace chain all carry it, and it is the stated reason the protocol splits bootstrap from counted sessions. (b) "Created … on the same initialised clone": the tooling the plan names is the agent's session-creation tooling; in this environment that tooling starts each session on its own checkout in its own container (the plan's own G3 and D-plan-26 say so of stores and transcripts), so a counted session does not see the bootstrap session's `npm install -g`, `~/.ctxoracle`, or `.claude/settings.json` unless those are transported — nothing in the step transports them (a committed `settings.json` would still name an interpreter and a `dispatch.js` path that exist only in the bootstrap container, `plan:3390-3396`). The liveness-row precondition would then read *every* counted session as "hooks never fired" and the validity rule would fail — loudly, but by construction, not by measurement. (c) The floor sample draws on "the leg 2 transcripts"; `ctxoracle export` writes `project.db` and `global.db` (`plan:3497-3499`) and no step moves a remote session's `~/.claude/projects/` transcript to the report machine; "transferred to the report machine" names no mechanism for the exported directory either.

**How verified.** `curl` of the live page and grep of the two passages (quoted above); Read of every cited plan line; r3:3292–3301 had "a Claude Code Remote session … or a local `claude` session" and "Inside each session the driver installs the tool" — the bootstrap/counted split, the trust sentence, and the export/transfer sentence are the round-3 corrections — **REGRESSION (text).**

**Standard violated.** expert-plan Step 4 / output-contract §11 (a documentation read is evidence for "what the page states" — the page states the opposite for the case cited); Gate C ("Every entry in Output section 11 carries read-level evidence" — probe 13 is a string-presence check, not a read of the rule's scope); Gate A/C (an implementer cannot execute "the same initialised clone" with the named tooling; the transfer and the transcript path are deferred choices); CLAUDE.md rule 1 (a measurement that fails its own validity rule by construction is a false zero); spec §11.5 (the exit is a measurement on the owner's real repos).

**Required change (hypothesis).** The protocol should be re-derived from the documented trust rule and from what the session tooling actually provides: if counted sessions are `-p`/SDK sessions on fresh checkouts, settings-file hooks *do* run without a dialog, so a per-session bootstrap (install + `init` at the head of each counted session, before its first question, with the liveness row still the precondition) is the executable form and the bootstrap/counted split loses its stated reason; if they are interactive sessions on one machine, the step must name the machine and the tooling that starts a second session on an existing checkout. Either way §11.4's trust claim is rewritten to quote the settings-file rule, probe 13 checks the sentence *inside* the "Workspace trust" section, and the transfer of both the exported directory and each counted session's transcript is stated as a concrete action. Evidence that would confirm: one counted session created with the named tooling whose exported store holds a `SessionStart` liveness row; the probe's expected output naming the section the sentence came from.

---

### S6 — T-3-3's new schedule is timing-dependent: `run-plan-probes.mjs` failed probe 07 in this session (B `StoreBusy`, rows `["A"]`), the probe does not implement the stdin start signal the test specifies, and the plan's "identically across three runs" is a statement about three runs on an idle machine, not a determinism property — **REGRESSION (introduced by the round-3 corrections — the ER S3 / CH M-1 closure; text)**

**Class:** flake-tolerated test (testing-standards anti-pattern 10; the round-2 M6 standard); a probe that executes a different mechanism from the test it evidences; the probe gate failing.
**Location:** `docs/plans/plan-phase-a.md:5986-5995` (T-3-3 Data: "child A holds `BEGIN IMMEDIATE` from t = 0 for 400 ms; child C issues its write at t = 100 ms … child B issues its write at t = 250 ms — its first attempt is busy until ~350 ms, its retry succeeds once A commits at 400 ms … the offsets are enforced by the test driving each child through a start signal on stdin. The schedule and its outcome were executed and reproduce identically across three runs (§11.4)"), `docs/plans/plan-phase-a.md:5732-5739` (§11.4 claim; `probe:07`), `docs/plans/plan-phase-a.probes/07_sqlite_busy_schedule.mjs:8-13` (children sleep until `startMs − (Date.now() − T0)` from a `T0` taken *before* `spawn`; no stdin signal), `docs/plans/plan-phase-a.probes/expected/07_sqlite_busy_schedule.txt`.

**How verified.** `node …/run-plan-probes.mjs docs/plans/plan-phase-a.md --layout /tmp/claude-0/probe-layout` (this session, while the reviewer's reads were in flight): `FAIL 07_sqlite_busy_schedule: output drifted … actual: identical across 3 runs: false / A success attempts=1 / B StoreBusy attempts=2 / C StoreBusy attempts=2 / rows ["A"]`, `1 probe(s) failed`, exit 1. Three subsequent standalone runs on an idle machine: `identical across 3 runs: true`, matching the expectation. `spawn`-to-first-output latency measured at +50 ms on this machine; A's hold ends at 400 ms and B's retry window ends at 450 ms, so any ≥ 50 ms shift in A's start (module load under load) makes B's second attempt expire before A commits — the observed failure shape. Provenance: r3:4970–4977 carried the round-2 offsets (executed by both round-3 reviewers as wrong); the new schedule and the probe are the round-3 corrections — **REGRESSION (text).**

**Standard violated.** testing-standards ("Deterministic setup and teardown; a flaky … test is treated as broken"; anti-pattern 10); output-contract Gate C ("`scripts/run-plan-probes.mjs <plan.md>` exits 0 against the delivered document — every executed claim in Output section 11 reproduces" — it did not); output-contract §11 (a probe is evidence for the mechanism it executes — this probe executes wall-clock offsets, the test specifies stdin signals).

**Required change (hypothesis).** The outcomes should be forced causally rather than timed: A acquires and reports "holding" on stdout; only then C is released and makes both attempts (100 ms busy each, deterministic under a held lock) and reports `StoreBusy`; only then B is released, makes its first attempt, and A is signalled to commit *after* B reports its first busy — so B's retry finds the lock free by construction. The probe should implement exactly that signalling (the test's own design), and the expected output should be stable under load. Evidence that would confirm: ten consecutive `run-plan-probes.mjs` runs with a CPU-bound sibling process exit 0.

---

## Systemic Patterns

### SY-1 — The new mechanical checks pass while the properties they were introduced to guarantee do not hold, because each check measures a proxy (backticked paths; string presence; three idle runs) rather than the property (module consumption; the rule's scope; determinism)

**Proactive scan.** Every mechanical closure the author's gates review cites in its "Check" column was re-derived:
- `--check` build order (cited for ER S1, S2, m10, M4, CH m-3): exit 0 — while S1 (declared graphs that omit the consumed modules) and S2 (Step 28 → Step 30) stand. `derive-plan-sections.mjs:428-514` resolves only backticked created paths and `provides` tokens; `provides: []` at 33 of 40 steps (`plan:727` … `plan:4189`).
- `probe:13` (cited for ER M5/CH S-4, CH M-9, Q43): checks five literal strings (`13_hooks_reference.optional.sh:9-13`); one of them is scoped to a different rule (S5).
- `probe:07` (cited for ER S3/CH M-1, Q46): three runs on the author's machine; failed once in four runs here (S6).
- `probe:16` (cited for ER m9/CH M-8, Q41, D-plan-24): executes the rule over the author's thirteen cases; the same function over twelve direct-answer inputs shows the wrongful holds (S4).
- `probe:14` (cited for ER M1/CH M-5, Q45): fetches the `22/bookworm` Dockerfile; the job runs `22.16.0-bookworm` (T1).

**Instances:** 5 checks, 5 properties not established. This is the collapse-log 2026-09-07 diagnosis reproduced under the replacement process: "the author's check reconciled identifiers while the reviewers walked the build in order" — now the check reconciles backticks and strings while the walk finds function-level consumption, rule scope, and load sensitivity.

**Named standard.** output-contract Gate C (each check is defined as establishing a property, not as exiting 0); collapse-log 2026-09-07 standing lesson (2): "a reviewer's prescription is a hypothesis to execute, never a sentence to transcribe" — a probe that checks a sentence's presence is the transcription in executable form; expert-review's post-fix protocol (closure only against the originally named standard).

**What correct looks like (the standard's requirement, not a patch).** A probe's expected output names the *property* the plan claims (the section a sentence came from; the outcome under load; the set of inputs the rule was checked over), and a declaration's `provides:` names what later steps consume, so the check has something to resolve. Where a check cannot see a property (function-level consumption), the plan says so and the dry-run implementer walk the collapse-log names as standing lesson (3) runs before dispatch — the author's gates review (`:83`) lists the S-2/S-3 classes as what the check cannot see but does not list build-order-by-function-name, which is the class this round found.

**No other systemic pattern** was found. Scans run: every `step-decl` block's `tests:` against §12 (`--check`: 122/122); every `T-<n>-<m>` reference resolves (`--check`); the six convention tests' allow/deny sets against every module a step creates under their scope — T-3-2 (pass), T-5-3 (pass; Step 1's generator is under `test/**`), T-10-3 (pass, provided Step 26's detectors do not import `session_log.js` to read assistant turns — they cannot, since nothing persists them, M1), T-24-2 (pass), T-28-2 (**fails at Step 21's `locate.ts`**, M2), T-32-3 (pass), T-36-1 (pass); narration vocabulary (`round-1|round-2|round-3|prior version|earlier version|was fixed|re-authored|the fix` → 0 narrative hits; `plan:8024-8030` describes pass 4 as "round-3 corrections … applied", a pass record, not narration); deferral vocabulary over §7 (`either … or|one of the following|alternatively|if needed|as appropriate|TBD` → 0 hits; the deferred choices in S5 were found by Read).

---

## Moderate & Minor Findings

### Moderate

**M1 — Step 26's `checkDenyLoop` ("no intervening assistant text") and `checkDenyDespiteAnswerText` ("≥ 1 intervening assistant text turn that the clear recognizer rejected with reason `below_length_floor`") read a per-turn record — assistant text turns and their rejection reasons, across events — that no step persists — new (present at `d667534`, unreported).**
Location: `docs/plans/plan-phase-a.md:2931-2938` (the two signatures take `(store, consumer)` only); `docs/plans/plan-phase-a.md:2827-2840` (catch-up: an `assistant_text` entry either clears — `answerQuestions` records `closed_by_uuid` — or is discarded; no write for a rejected turn); `docs/plans/plan-phase-a.md:1379-1424` (schema: `questions`, `session_log`, `whisper_audit`, `faults` — no column or table for classified assistant turns or a rejection reason); `docs/plans/plan-phase-a.md:1577` (`whisper_audit.lastKinds` — kinds are `whisper`/`deny`). A deferral-only turn between two denies leaves no trace, so `checkDenyLoop` cannot distinguish "three denies with no text" from "three denies around a dodge", and `checkDenyDespiteAnswerText` cannot find a `below_length_floor` rejection that happened two events ago. Verified by Read and grep (`rejected` → `plan:2937-2938` only among step texts); r3:2446, r3:2451 the same — **new**. Standard: expert-plan Step 8 ("Name the functions. Name the types." — the detectors' input has no home); AD-17 (each detector "this architecture names" must be buildable from the store it reads). Required change (hypothesis): the catch-up records each classified assistant turn `(consumer, uuid, ts, verdict, reason)` — a `classified_turns` table in migration 001 with a DAO at Step 9 and a writer at Step 25, which T-10-3's allow-list then names for `health.ts` — or the detectors take the turns as a parameter the handler passes from the current catch-up and their windows are re-scoped to one event. Evidence that would confirm: T-26-1's "three consecutive denies without text" and "denies with only a deferral-only turn between" cases are distinguishable from the store alone.

**M2 — Step 21's `locate.ts` "reads `transcript_path` from the internal event" and "records `agent_transcript_path` from `SubagentStop` inputs"; the internal event carries `transcriptPath` and no subagent-path member, and both names are in T-28-2's scan list, so the module as described either reads an undefined member or fails the convention — recurring (round-3 SY-1 / CH S-1 at a site the closure did not re-read).**
Location: `docs/plans/plan-phase-a.md:2482-2486` vs `docs/plans/plan-phase-a.md:1220-1224` (`InternalEvent`: `transcriptPath`, no `agentTranscriptPath`) and `docs/plans/plan-phase-a.md:3085-3091` (T-28-2 scans `agent_transcript_path` and `transcript_path`). Verified by Read; r3:2093–2096 identical — the SY-1 closure narrowed the list without re-reading this module (the author's row: "Read of every convention list against Step 6's `InternalEvent` names" — Step 21 names wire fields, not `InternalEvent` members). Standard: Gate A (a conformant build passes its convention tests); AD-6 (one file names hook fields). Required change (hypothesis): `locate.ts` reads `ev.transcriptPath`; the subagent path is either added to `InternalEvent` as `agentTranscriptPath` (the adapter maps it) or the "records … for later phases" sentence is dropped (nothing in Phase A reads it — AD-11). Evidence: T-28-2's clean-build case passes with `locate.js` present.

**M3 — Steps 27, 30, 31, 32, 33, 34, 35 modify files earlier steps created (`answer_drift.ts`; `handler.ts`/`indexer.ts` call sites; `dispatch.ts`'s verb switch) and every one declares `modify: []`, so §5.1 and §16's file-list reconciliation cannot see the edits — REGRESSION (introduced by the round-3 corrections — the declarations are new text; text).**
Location: `docs/plans/plan-phase-a.md:2996` + `plan:3004` (Step 27 `modify: []`; "In `answer_drift.ts` add `handleSessionStart` …"; `plan:3023` "Creates. Nothing new"); `docs/plans/plan-phase-a.md:3367` + `plan:3375` (Step 31 `modify: []`; "Register the `init` verb in Step 28's `dispatch.ts` switch"); `docs/plans/plan-phase-a.md:3101` ("Later steps register their verbs in the same switch (Steps 31–35)"); `plan:3478`, `plan:3561`, `plan:3649`, `plan:3710` (Steps 32–35 `modify: []`); `plan:3286` (Step 30, see S2). Verified by Read; the prose at r3:2501 and r3:2788 is identical, the declarations did not exist. Standard: output-contract item 7 ("the files it creates/modifies/deletes"; "the prose fields of the step must agree with it"); §16 file-list reconciliation ("a touched file with no §5 row … is investigated" — these files have rows, so a diff would pass while the declared step set is wrong). Required change (hypothesis): each of the seven steps declares the earlier file it edits under `modify:`; `--check` then lists `dispatch.ts` as `modify | S31, S32, S33, S34, S35` in §5.1. Evidence: the regenerated §5.1 row.

### Minor

**m1 — T-38-25's File field says the script is "invoked by Step 1's `cold-container` CI job" while Step 38 now adds that job and Step 1 defers it — REGRESSION (introduced by the round-3 corrections — the ER M1 closure; consistency).** `docs/plans/plan-phase-a.md:7467-7468` vs `docs/plans/plan-phase-a.md:798-799`, `plan:3910-3911`. r3:6417–6418 identical. Standard: Gate C (reconciliation; the authored §12 entry re-derived after the step edit). Required change: "invoked by Step 38's `cold-container` CI job".

**m2 — §7's overview places "watchdog and guard" in "the pipeline (Steps 28–30 …)" while Step 10 now creates them — REGRESSION (round-3 S1 closure; consistency).** `docs/plans/plan-phase-a.md:689-690` vs `docs/plans/plan-phase-a.md:1662-1671`, `plan:3227`. r3:671–672 identical. Standard: Gate C (authored sections re-derived from the current step set). Required change: "the pipeline (Steps 28–30: handler, adapter, CLI entry and replay harness; watchdog and guard verification; SessionEnd fold and regret)" and "diagnostics writers, watchdog, guard" in the substrate clause.

**m3 — Step 10 creates `src/hook/guard.ts` (the recursion guard, AD-21) and declares `covers: [PA-5]` only, while §2.3 maps PA-9 to S5, S29, S36 — REGRESSION (round-3 S1 closure; text).** `docs/plans/plan-phase-a.md:1636`, `plan:1667-1669` vs `plan:176`, `plan:197`. Standard: output-contract §2 coverage (the element→step table is generated from `covers:`; a creating step that omits the element makes the table understate coverage). Required change: `covers: [PA-5, PA-9]`.

**m4 — T-23-2 says the signature is asserted "by calling it with the three seeded lists and a string only"; `recognizeClearing` takes two lists and a number — REGRESSION (round-3 M-8/m9 closure; text).** `docs/plans/plan-phase-a.md:6634-6636` vs `docs/plans/plan-phase-a.md:2650-2652`. Standard: Gate C. Required change: "the two seeded lists, the seeded floor, and a string only".

**m5 — Step 28's definition of the convention test still says "built-output grep … for every input and response field name listed above; no match permitted" while the narrowed scan list two paragraphs earlier excludes four of the names in the first list — REGRESSION (round-3 SY-1 closure; consistency).** `docs/plans/plan-phase-a.md:3155-3159` vs `plan:3073-3078` (the adapter's full list) and `plan:3085-3095` (the scan list). r3:2626 identical. Standard: Gate C (zero tolerance for an ambiguous instruction). Required change: "for every identifier in the convention list above".

**m6 — Step 18 now defines the done-claim negation clause, and T-18-8 carries no negated case, so the clause is untested — partial closure of CH m-4 (not tagged REGRESSION: the changed text is correct; the unchanged test is incomplete).** `docs/plans/plan-phase-a.md:2301-2304` vs `docs/plans/plan-phase-a.md:6479-6497` (r3:5453–5459 identical). Standard: expert-plan Step 9 (every behaviour the step specifies is traced to a test); the round-3 required change named the cases. Required change: add "Not done yet." and "This isn't finished." to T-18-8's negatives and the negation clause to its Verifies field.

**m7 — The cold-container network policy is stated two ways: Step 38 "with the default runner network", T-38-25 "with only the harness's network access" — REGRESSION (round-3 M1 closure, which asked that the policy be stated once; consistency).** `docs/plans/plan-phase-a.md:3914` vs `docs/plans/plan-phase-a.md:7472-7473`. Standard: Gate C. Required change: one phrase, in T-38-25's Data field referring to Step 38's.

**m8 — `status` must print "the pinned interpreter `init` wrote and whether it still exists", but no step records the interpreter path anywhere `status` can read it (no `schema_meta` key; `init`'s summary is stdout) — REGRESSION (D-plan-25 is new; text).** `docs/plans/plan-phase-a.md:3593-3594`, `plan:3398-3402` vs `plan:1329-1331` (`schema_meta` keys). Standard: Gate C (a deferred choice: parse `settings.json`'s command, or a new key). Required change (hypothesis): `init` records `schema_meta.pinned_interpreter`; `status` reads it and `fs.existsSync`; T-33-1 seeds it.

**m9 — `lexicon.command_class_innocuous` (`pwd`, `echo`, `git log`, `git diff`, `find`, `head`, `tail`, `wc`), `lexicon.command_class_test_runners` (`pnpm test`, `yarn test`, `jest`, `mocha`, `vitest`, `node --test`) and `lexicon.completion_claim` (`completed`) carry members the architecture does not state, labelled `architecture_default` — the CH M-10 closure landed on two of four lexicons (partial; new for these two).** `docs/plans/plan-phase-a.md:1841-1848` vs `arch:1148` (AD-15 names `ls`, `cd`, `cat`, `git status`-class, `grep`/`rg`, "…" and no runner list) and `plan:4417-4425` (D-plan-7's rule: a value the architecture leaves open is a `plan_seed` printed with every measurement). Standard: D-plan-7's own rule; CLAUDE.md ("numbers without sources don't go in"). Required change: label the plan-added members `plan_seed` (or the whole list, since membership is the value), so `status` and the report print them.

**m10 — The totally-dead detector scans "transcripts under `~/.claude/projects/<cwd-slug>/` for this repository" and the plan states no rule for deriving the slug from the repository path — REGRESSION (D-plan-25 is new; text).** `docs/plans/plan-phase-a.md:3588-3590`; the only evidence of the format is the path at `plan:5688` (`/root/.claude/projects/-home-user-agent-armory/…`). Standard: Gate C (deferred choice); the layout is undocumented (V12), so the rule is a plan-level premise needing a §11 entry. Required change (hypothesis): state the observed rule (the absolute `cwd` with each `/` replaced by `-`), cite the observation, and have `status` report "no transcript directory found for this repository" when the derived slug does not exist, so a changed layout is visible rather than silent.

**m11 — Q22's disposition says "the drivers are Max Cogar in his OL-11 role or the implementing agent, and the report records which", while D-plan-26 makes the agent create every counted session and Max Cogar's sessions additional — the register was not re-derived after the Step 39 edit — REGRESSION (D-plan-26 is new; text).** `docs/plans/plan-phase-a.md:7857-7861` vs `plan:4022-4026`, `plan:4060-4064`. Standard: output-contract "Still authored — re-derive, never patch" (§14 restates step data); Gate C. Required change: re-derive Q22's disposition from D-plan-26.

---

## Tentative Findings

**T1 — The `cold-container` job runs `node:22.16.0-bookworm`; probe 14 verified the `git` lineage of the `22/bookworm` Dockerfile at `nodejs/docker-node` `main`, not of the `22.16.0-bookworm` tag.** `docs/plans/plan-phase-a.md:3912-3914`, `plan:5740-5747`, `14_docker_node_git.optional.sh:4`. The tag exists on Docker Hub (`status: active`, fetched this session); whether its image was built from `buildpack-deps:bookworm` could not be read from here (the repository does not keep the 22.16.0 Dockerfile on `main`). Verification gap: `docker run --rm node:22.16.0-bookworm git --version` in CI, or the Dockerfile at the `nodejs/docker-node` commit that published 22.16.0.

**T2 (carried) — Behaviour at the Node 22.16.0 floor** is executed only on v22.22.2 (`plan:5526-5527`; Q28); the plan assigns it to CI's matrix entry, which is honest.

**T3 (carried, narrowed) — `unshare -rn` on the `ubuntu-24.04` runner.** Step 1 now names the image (`plan:793`) and T-32-2 records an `EPERM` refusal instead of failing (`plan:7015-7017`); the sentence "available on the Linux CI runners — executed 2026-09-07" (`plan:3509-3510`) still describes this sandbox's execution as the runner's. Verification gap: the first CI run of T-32-2.

No other tentative findings — every other finding's premise was verified per Gate B.

---

## Observations

- **Multi-perspective check (manual; Clear Thought unavailable).** Standards persona: the round's findings are one shape — a mechanical check introduced to replace the author's attention, satisfied by a proxy of the property (SY-1): backticked paths for consumption (S1, S2), string presence for a rule's scope (S5), three idle runs for determinism (S6), the author's own case table for a rule's reach (S4). Implementer persona: the first red run is `run-plan-probes.mjs` before Step 1 (S6); the first build-order stop is Step 28's import of `diag/regret.js` (S2); the first test that cannot pass is T-14-2 at Step 14 (S3); T-28-2 at Step 28 (M2). Owner persona: the intake and move axes of the block are still the skeleton; the clear axis grew a vocabulary this round and now denies a one-word "Sure." (S4) — the 2026-09-04 shape at a small scale, on the axis where the spec says to err the other way; and leg 2, the only place the block is measured against a reacting agent, cannot start as written (S5).
- The probe regime is a real improvement where the probe executes the claim's mechanism: probes 02, 03, 04, 05, 06, 08, 10, 11, 12, 15 (the scrub, re-executed here through the probe: `unscrubbed … session=parent`, `session-identity scrub … session=fresh`), 16 (over its own cases), 17 all reproduced. The defects are where a probe stands in for a read (13) or for a property (07).
- The trace file `2026-09-07-plan-tool-traces-2.md` is internally honest — the D-plan-26 chain records the inverted trust premise as its input (line 1464), which is where S5 entered; the reasoning followed a false premise rather than inventing one.
- `derive-plan-sections.mjs --self-check` passes 31 checks; the contract and script agree with each other. The gap S1 names is in what the contract asks the declarations to carry (`provides:` for names "consumed by name rather than by path — a CLI verb, a command, a script entry point"), which does not reach exported functions; whether to widen the contract is a skill decision, not this plan's, but the plan is graded against the property (Step 8's explicit dependencies), not the script.

---

## Convergence Record

- **Round number:** 4.
- **Trajectory (expert-review series):** R1: 10 (3 Serious, 5 Moderate, 2 Minor) → R2: 29 (1 Critical, 5 Serious, 1 Serious-Systemic, 11 Moderate, 11 Minor) → R3: 20 (1 Serious-Systemic, 3 Serious, 5 Moderate, 11 Minor) → **R4: 20 (1 Serious-Systemic, 5 Serious, 3 Moderate, 11 Minor)**. The collapse-hunt series' items (R1: 13, R2: 20, R3: 23) are counted in the closure tables, not in the trajectory.
- **Flow counts this round:** prior findings closed 31 of 43 (20 expert-review items: 14 closed, 6 partially closed or regressed; 23 collapse-hunt items: 17 closed, 6 partially closed or regressed — several pairs coincide, see the table); recurring 1 (M2); new 4 (S2, M1, m6, m9); **regressions 15** (S1, S3, S4, S5, S6, M3, m1, m2, m3, m4, m5, m7, m8, m10, m11) — of which 10 are wrong changed text (S1, S4, S5, S6, M3, m3, m4, m8, m10, m11) and 5 are unchanged text a correction left contradicting its changed sibling (S3, m1, m2, m5, m7).
- **Tripwire evaluation — fired under the arithmetic recorded for this round.** The round-3 record and the author's gates review both state the round-4 rule as: fires if round 4's new + regression ≥ its closed count, **or if its total is ≥ 20**. Condition (a): new + recurring + regression = 4 + 1 + 15 = 20 < 31 — does not hold. Condition (b): total = 20 ≥ 20 — **holds**. Under the series' original two-consecutive-rounds definition, round 3 held neither condition (29 → 20), so this round is the first of a possible pair on condition (b) (R3 20 → R4 20, not a strict decrease). Both readings are recorded; per the arithmetic the round was dispatched with, **the tripwire fired**, and Recommended Priority opens with the process item.
- **For the owner's separate regression rule** (a round-4 correction-induced defect count above zero means the replaced process did not hold): 15 regression-tagged findings (10 by changed text). The count is stated; the rule's consequence is the owner's.

---

## Open Findings Ledger

Not applicable — no operator-directed cycle stop.

---

## Recommended Priority

1. **SY-1 / S1** — before any content fix: re-derive every `depends_on` from consumption and give `provides:` the exported names later steps use, so the build-order check measures the property it was added for; re-run the dry-run implementer walk the collapse-log 2026-09-07 entry names as standing lesson (3) — it is the check that found S2 here and the author's gates review does not list it as run.
2. **S2, S3, M3** — the build-order and declaration defects that make Checkpoint 3 unexecutable and T-14-2 unpassable.
3. **S4** — re-derive the clear rule from AD-9's two conditions and FR-B5's lean; attack D-plan-24's premise rather than adjusting the lexicon.
4. **S5** — re-derive leg 2 from the settings-file trust rule as the page states it and from what the session tooling provides; re-write §11.4's hooks entry and probe 13 to check the rule inside its section; state the transfer of stores and transcripts as actions.
5. **S6** — make T-3-3's outcomes causally forced and the probe execute the test's own signalling; the probe gate must exit 0 under load.
6. **M1, M2**, then **m1–m11**, then the tentative items.

---

## What could not be checked

- Whether a Claude Code Remote session created by the agent's tooling can be started on an already-initialised checkout (S5(b)); the plan's own G3 and D-plan-26 state the container isolation this finding rests on, and no session was created to re-observe it.
- Behaviour at the Node 22.16.0 floor (T2).
- `unshare -rn` on the `ubuntu-24.04` runner (T3).
- The `git` lineage of the `node:22.16.0-bookworm` image (T1).
- The Clear Thought server's own run beyond what the two trace files record; the files were read for coverage of §10 (complete) and for the D-plan-26 premise (line 1464), not re-executed.
- Whether the probe-07 failure reproduces on the author's machine under load; here it failed once in four runs, with the failure shape the schedule's 50 ms margin predicts.

---

Verdict: NEEDS FIXES (20 findings: 1 Serious-Systemic, 5 Serious, 3 Moderate, 11 Minor; 15 REGRESSION-tagged; tripwire fired under the recorded round-4 arithmetic)
