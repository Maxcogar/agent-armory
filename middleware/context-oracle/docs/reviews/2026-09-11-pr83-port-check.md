<!-- Independent review output, 2026-09-11, written once and never edited (CLAUDE.md routing table). Produced by a fresh subagent with no exposure to the author session; scratchpad paths inside are that subagent's working directories and no longer exist. -->

# PR #83 lineage findings — port check against our plan (2026-09-11)

**Our plan:** `middleware/context-oracle/docs/plans/plan-phase-a.md`, working tree on
branch `claude/context-oracle-dw74hy` (9601 lines; line numbers below refer to it).
**Their lineage:** `origin/claude/plan-correction-strategy-57ot28` (PR #83), plan
7833 lines; both lineages share merge-base `9c05f4c`. Every review file on that
branch (`docs/reviews/2026-09-07-round-{2..12}-plan-{collapse-hunt,expert-review}.md`
plus `2026-09-07-clear-thought-verification-q-gap-5.md`) is distinct from ours, so
all 23 are classified here. Skipped by instruction: round 10 expert-review Critical
Finding 1 (web-tree-sitter 0.26.13 / tree-sitter-wasms 0.1.13 grammar ABI).

Verdicts: **PRESENT** = the named defect exists in our plan text; **ABSENT** = our
text differs or already handles it; **N-A** = the mechanism does not exist in our
plan. Step numbers in the "finding" column are THEIR plan's; the "location in OUR
plan" column gives ours.

## Classification table

| Round / file | Finding | Severity | Verdict | Location in OUR plan (heading; quoted anchor) | Evidence |
|---|---|---|---|---|---|
| R2 collapse-hunt | New-1 — `oracleSpawn` structural-confinement test (`T41-1d`) asserted in three places, specified nowhere | Collapse | ABSENT | Step 5 body: "A convention test (`test/conventions/child_process_single_importer.test.ts`, an import scan over `dist/src/**` …) passes only when `dist/src/util/spawn.js` is the sole importer." (:1140–1144); §12.1 `T-5-3 — node:child_process single-importer convention` (:7216); step-decl `create:` lists the file (:1088); §5.1 row (:464) | `grep -n child_process_single_importer ours.md` → 464, 1088, 1141, 7218 — the test is declared in the step-decl, the file map, the step body and §12. |
| R2 collapse-hunt | P2 — L11(b) "either way no wrongful deny" survives verbatim in §10 D-plan-6, §5.1 comment, §15 heading | Partial collapse | ABSENT | §10A D-plan-11 (:5750) "design-safe against a persistent wrongful deny, transient case …"; §15 (:9501) "safe against a persistent wrongful deny" | `grep -in 'either way no wrongful\|design-safe both ways\|design-safe either way' ours.md` → no hits; 7 hits for "transient wrongful" all state the asymmetry. |
| R2 collapse-hunt | C3 — §13 R7 still describes pre-fix mitigation; no concrete way to obtain a non-self repo | Partial collapse | ABSENT | Step 39 Leg 2: "The target repositories are `Maxcogar/NOVA` and `Maxcogar/Nova-Integrations` … if a named repository … cannot be cloned … the next repository in that listing's push-date order replaces it" (:4437–4443); Validity rule "the tool's own repository never counts toward the minimum" (:4547) | Our plan names concrete repos and a replacement rule; our R7 (:9065) is a different risk (thin inputs) whose mitigation matches Step 39's validity rule. No `.ctxoracle-exit-repos` mechanism exists in ours (`grep -i exit-repos` → 0). |
| R2 collapse-hunt | C1 — write-time cap on recognizer predicates is prose-only; §13 R1 not updated | Partial collapse | ABSENT | §10A D-plan-19 (negative-coverage tests) (:5875): "The assertion is the rule's complement over a generated corpus"; §13 R1 (:9026): "Mitigation: the rule-complement assertions in `T-23-1`, `T-23-2`, `T-23-3`, and `T-18-8` (D-plan-19)" | Our mechanism is a mechanical test (T-23-1/2/3, T-18-8 exist at :7836/:7858/:7908/:7710) not a prose cap, and R1 cites it. `grep -i 'write-time cap' ours.md` → 0. |
| R2 collapse-hunt | New defect — Step 23 disclosure calls four AD-14 defaults "unsourced plan judgment" | Serious (new defect) | ABSENT | Step 12: "`architecture_default` rows (AD-14, AD-13, AD-9): `bar.confidence_floor` = `0.6`; `bar.support_min` = `3`; `bar.noise_floor_support_min` = `2`; `bar.impact_read_min_coupled` = `2`" vs "`plan_seed` rows (reasoning in §10 D-plan-7)" (:1887–1900) | Provenance is split exactly as the finding asked. |
| R2 collapse-hunt | Structural gap — N1/N2/N5/N6 have no four-part §10A collapse-test | Procedural gap | ABSENT | §10A headings D-plan-14 (single spawn wrapper) :5796, D-plan-15 (URL normalization) :5811, D-plan-16 (bypass predicate bound) :5828, D-plan-7 (plan-seeded thresholds) :5639 | `grep -n '^#### ' ours.md` → 31 D-plan entries covering each. |
| R2 collapse-hunt | N1 note — "closes the owner-can't-tell hole" overstates what a raw identity string does for a non-programmer | Note (survives) | ABSENT | Step 5 Gate 3: "printing the identity string is what makes any remaining split visible" (:1171); Impact: "Mitigated by `status` displaying the key, mode, and identity string, and by AD-20's plain-language 'would change keying mode' prompt on re-init" (:1195) | Our wording says "visible", not "closes the hole"; `grep -i 'closing the' ours.md` on this topic → 0. |
| R2 expert | S3 — Q-gap-5 "resolved" (CodeGraph/Clear Thought not callable; judgment calls made without the mandated tools) | Serious | N-A | §10 intro: "Every decision below was reasoned through the Clear Thought MCP server … The captured stdio logs are `docs/reviews/2026-09-07-plan-tool-traces.md` …" (:4783–4790); §14.3 has only G1–G4, no Q-gap-5 | Our lineage has no Q-gap-5 entry (`grep -i 'Q-gap' ours.md` → 0); its decisions carry Clear Thought traces (`docs/reviews/2026-09-07-plan-tool-traces*.md` exist on disk, 11 files) and §11.4 :6821 records the stdio verification of both servers. |
| R2 expert | Serious — T2.5-1, T18-2, T21-2 cited as verification but have no §12 spec | Serious | ABSENT | §12 (all tiers) | Executed: `grep -oE '\bT-[0-9]+-[0-9]+[a-z]?\b' ours.md \| sort -u` → 123 ids; `grep -oE '^- \*\*T-[0-9]+-[0-9]+[a-z]?' ours.md` → 123 headings; `comm -23` → empty, `comm -13` → empty. Every cited test id has a §12 spec and vice-versa. |
| R2 expert | Systemic 1 — §10A D-plan-6 contradicts its own §10 retraction (owner-run L11 probes) | Systemic | N-A | §10 D-plan-11 "(L11 verifications executed by the build)" (:5732) | Our D-plan-6 is a different decision (settings marker); the L11 decision is D-plan-11, "executed by the build". `grep -i retracted ours.md` → 0 — no retraction exists to contradict. |
| R2 expert | Systemic 2 — D-plan-8 (§10 + §10A) defends a `"comment": "installed by ctxoracle"` marker Step 31 replaced | Systemic | ABSENT | §10 D-plan-6 "The `init`/`deinit` marker is the documented `command` field" (:4905); §10A D-plan-6 "Marker-by-pattern on a documented field" (:5613–5637); Step 31 "The marker is the command …" (:3767), "Not an extra `comment`/marker field" (:3823) | All three surfaces state the same mechanism (`grep -n 'D-plan-6' ours.md` → 4811, 4905, 5613, 9203, 9280 — each read). |
| R2 expert | Systemic 3 — `src/proc/oracle_spawn.ts` absent from §5.1 | Systemic | ABSENT | §5.1 "(generated from the step declarations)" (:330); row `src/util/spawn.ts` create S5 | §5.1 is derived mechanically by `derive-plan-sections.mjs --check` (STATUS.md); `grep -n 'util/spawn.ts' ours.md` → §5.1 row and S5 step-decl. |
| R2 expert | Systemic 5 — T41-1 Data field not extended for the 4th convention sub-file | Systemic | N-A | §12.1 T-5-3 is its own spec; convention tests are individually specified (T-5-3, T-28-?, etc.) | Our plan does not use a shared "T41-1 a/b/c/d" umbrella spec; each convention test has its own id (T-id diff clean). |
| R2 expert | Systemic 6 — §12.5 Step→T-ID table omits T25-8/T25-9 | Systemic | N-A | §12.4 Coverage reconciliation holds only the AC→T-ID table plus the closing sentence (:8962–9020) | There is no Step→T-ID table in ours; step↔test linkage is the `tests:` field of each step-decl, checked by `derive-plan-sections.mjs --check`. |
| R2 expert | Systemic 7 — Step 31 body says `src/cli.ts` while §5.1 says `src/cli/dispatch.ts` | Systemic | ABSENT | Step 28 "Create `src/cli/dispatch.ts` — the `bin` entry" (:3326); Step 31 "Register the `init` verb in Step 28's `dispatch.ts`" (:3722) | `grep -n 'src/cli\.ts' ours.md` → 0. |
| R2 expert | Moderate — T32-1a cited (`--purge`) but not defined | Moderate | ABSENT | §12.2 T-32-1 Data: "`deinit`; `deinit --purge`; `git status --porcelain` on the fixture." (:8303); Step 32 "`--purge` deletes the project store and its diagnostics directory" (:3870) | T-id diff clean; `--purge` is inside T-32-1's own data set. |
| R2 expert | Tentative — C1 write-time cap / C3 repo-set disclosure are process-only commitments | Tentative | ABSENT | D-plan-19 (mechanical rule-complement tests); Step 39 named repos + validity rule | See R2 collapse-hunt C1/C3 rows. |
| R3 collapse-hunt | Collapse 1 — "genuine independent check, not a formality" overclaims the Clear-Thought pass | Collapse | N-A | §10 intro (:4783) describes the traces as the reasoning record, not as independent verification | `grep -in 'independent check\|rubber.stamp' ours.md` → 0. The Q-gap-5 verification document does not exist in our lineage. |
| R3 collapse-hunt | **Collapse 2 — `deny_bypass_suspect`'s AD-9 two-directional disclosure is half-built: only the under-count (coverage bound) is printed in `status`; the over-count direction (an unrelated same-file shell rewrite counted as a bypass) is nowhere stated or tested** | Collapse | **PRESENT** | Step 26 `checkDenyBypassSuspect`: "The predicate's coverage bound is stated where the number is shown: `status` and the exit report print \"bypass diagnostic recognizes only: <the list>; other shell write paths are not measured\"" (:3168–3173); Step 33: "`deny_bypass_suspect` counts **with the bypass predicate's coverage bound printed beside the count**" (:3957–3959); T-33-1 "Fails when … the bypass bound … is absent" (:8347–8362); D-plan-16 "Cite: AD-9 (proxy, both directions stated)" (:5840); Step 26 Source cites "AD-9 (… the bypass predicate's over-/under-count disclosure)" (:3177) | AD-9 (`docs/architecture-phase-a.md:847–849`): "A proxy, not a measurement — it over-counts an unrelated same-file shell rewrite and under-counts a bypass to a different path; **both directions stated in `status`**." Executed: `grep -in 'unrelated same-file\|same-file shell\|not a bypass\|both directions\|over-count\|overcount' ours.md` → only :3177 (a Source citation naming the requirement) and :9570 (unrelated). The printed `status`/exit-report text and T-33-1's assertions cover only the under-count direction. Our plan cites the requirement and does not implement it — exactly the finding. |
| R3 collapse-hunt | Partial 1 — §10 D-plan-1 still says "Reasoned without Clear Thought MCP (unavailable this session)" | Partial | ABSENT | §10 intro: "Every decision below was reasoned through the Clear Thought MCP server" (:4783) | `grep -in 'without Clear Thought' ours.md` → 0. |
| R3 collapse-hunt | Partial 2 — N5's "sharpened" framing claimed in §10A N5 but not there | Partial | N-A | — | No Q-gap-5 / N5-sharpening claim exists in ours (`grep -i 'sharpen' ours.md` → 0 relevant). |
| R3 collapse-hunt | N1 note — `init` could warn when a new key's non-scheme components match an existing stored key | Note | N-A | Step 5 rule 2: "The scheme itself is dropped, so an SSH and an HTTPS clone of the same remote key identically" (:1109–1111) | Our normalization already folds scheme; the residual named is only path case on case-insensitive hosts, disclosed at :1111–1116 and D-plan-15. |
| R3 expert | Systemic Instance 1 — `deny_bypass_predicates_confined.test.ts` (T18-3) named in §5.1/§12 but built by no step | Serious-equivalent | ABSENT | §12 File fields vs. step-decl `create:`/`modify:` lists | Executed `file_check.py` (scratchpad): 123 §12 `**File.**` paths, 265 step-decl paths, **0** §12 file paths without a step-decl entry. Our predicate-list cap is D-plan-19's rule-complement tests, not a convention grep, so the specific file is N-A and the failure shape is absent. |
| R3 expert | Systemic Instance 2 — Step 8 seeds `tuning` inside the migration (contradicts AD-5 WRITER "seeded at init"); T8-1/T23-1 assert every seed "matches AD-14" | Serious-equivalent | ABSENT | Step 8 SQL comment "Schema only; the `tuning` seed rows are written by" Step 12 (:1593); Step 31 `init`: "`seedDefaults` (Step 12)" (:3345, :3739); T-8-1 "**NOT asserts.** Seed values (T-12-1)" (:7420); T-12-1 "every key in Step 12 read back with its value and `source`" (:7440) | Seeding is at `init` via `seedDefaults`, and T-12-1 checks per-row provenance (`architecture_default` vs `plan_seed`), not "matches AD-14" for all. |
| R3 expert | M1 — §2.3 table misattributes Stop-time delivery to Step 31 | Moderate | N-A | §2.3 "<!-- generated:coverage begin -->" PA-4 → S20, S28 (:184–200) | Our §2.3 is a generated element→step table from step-decl `covers:`; no prose "Delivery | Step 31" row exists. Stop-time channel is Step 20 (:2583) and the AC-8a line is Step 27 (:3240). |
| R3 expert | M2 — Step 19 cites "the Step 26 done-claim recognizer" (built in Step 25) | Moderate | ABSENT | Step 27: "at a `Stop` where `recognizeDoneClaim` (Step 18)" (:3240); Step 18 title "+ done-claim recognizer" (:2411) | Attribution matches. |
| R3 expert | m1 — Q-gap-5 verification doc preserves no script/raw transcript | Minor | N-A | — | No such document in our lineage. |
| R3 expert | Tentative — Step 32 export "tar?" reads as unresolved brainstorming | Tentative | ABSENT | Step 32 Gate 3: "`VACUUM INTO` per store into a user-named directory" (:3899) | `grep -n '\btar\b' ours.md` in Step 32 → 0. |
| R4 collapse-hunt | Collapse 1 — Step 14 still says `lexicon.stoplist` is "seeded … at Step 8" after Step 8 was made schema-only | Collapse | ABSENT | Step 23 `recognizeQuestions`: "(iii) it is not matched by `lexicon.stoplist`" (:2833) — no seeding-step attribution; Step 12 seeds it (:1908) | `grep -n 'lexicon.stoplist' ours.md` → :1908 (Step 12 seed table), :2833 (Step 23 predicate). Neither attributes seeding to Step 8. |
| R4 collapse-hunt | Collapse 2 — §2.3 table cites wrong step numbers for genres/indexer/self-observability/model seam | Collapse | N-A | §2.3 "<!-- generated:coverage begin -->" (:184) | Executed: `node middleware/context-oracle/.claude/skills/expert-plan/scripts/derive-plan-sections.mjs --check middleware/context-oracle/docs/plans/plan-phase-a.md` → `OK: 40 steps, 13 elements, 123 test specs, 19 probes cited, regions current`. The table is derived from step-decl `covers:`, so hand-typed step-number drift cannot exist. |
| R4 expert | Systemic Instance A — Step 40 / §14.1 Q13 still describe owner-run L11 markdown probes retracted by D-plan-6 | Systemic | ABSENT | §10 D-plan-11: "The two L11 build-time verifications are executed by the build, not handed to the owner" (:5040); §14.1 Q21: "Are the L11 build-time verifications owner-run? … Answered: no — D-plan-11" (:9213); Step 38 creates `test/build_time/marker_presence.ts` (:4285); Step 39 leg 2 item 2 runs the L11(b) induction (:4474) | `grep -in 'owner-run\|copy-and-paste' ours.md` → only negations (:4348 "Not owner-run markdown probes", :5046). No `real_transcript_marker_probe.md`/`user_prompt_submit_provenance.md` (`grep` → 0). |
| R4 expert | Systemic Instance B — Step 32 Verification omits `T32-1a` and misattributes `--purge` to `T32-1` | Systemic | ABSENT | Step 32 Verification cites T-32-1 whose own Data includes "`deinit --purge`" (:8303) | No `T32-1a` exists; T-32-1 covers `--purge` in its own spec, so the step's citation is accurate. |
| R4 expert | Tentative — §14.4 sweep record has no "Pass J" for round 3's fix pass | Tentative | N-A | §14.4 Reconciliation sweep (:9424–9470) | Our §14.4 is a different record (see note below); no Pass A–I ledger exists to be missing an entry from. |
| R5 collapse-hunt | Collapse 1 — §3 cites "Step 32 (concurrency)"; concurrency is Step 37 | Collapse | ABSENT | §3 "**SQLite WAL semantics** … Governs Step 3." (:281–282); Step 3 heading "Store adapter: the single `node:sqlite` importer, concurrency discipline" (:939) | Read §3 in full (:210–293); every "Governs Step N" resolves to a matching heading (WAL→3; VACUUM→32 export/import; hooks reference→20,21,25,28,29,31,32; Node docs→1,2,3,32,37). |
| R5 collapse-hunt | Collapse 2 — §5.3 attributes the `init` verb to Step 30 | Collapse | ABSENT | §5.3: "hook entries added by `ctxoracle init` (Step 31); removed by `ctxoracle deinit` (Step 32)" (:629–630) | Correct in ours. |
| R5 collapse-hunt | N2 note — N2's prose conflates predicate-set drift test with disclosure-text drift test | Note | N-A | — | Ours has no T18-3-style predicate grep; the disclosure test is T-33-1 alone (which is itself incomplete — see R3 Collapse 2 PRESENT). |
| R5 expert | Systemic Instance A — "Step 30's `init`" at four sites (Steps 2, 8, 23, 29) | Systemic | ABSENT (specific) / **PRESENT (class, one site)** | Specific: every `init` mention cites Step 31 (:902, :970, :1477, :1506, :2250, :3350, :3383 …). **Class instance found in ours:** §12.3 T-38-31 "**Verifies.** Step 25 (deny), **Step 27 (open-scoped dedup index)**, Step 34 (`correct`)" (:8915) | Executed: dumped all 584 "Step N" mentions with context (`step_mentions.txt`) and read each against the 40 headings. One misattribution: the open-scoped dedup index is Step 7's DDL ("the open-scoped dedup index (Step 7)", Step 22 :2758; §11.2 :6345 "`q_open_dedup` open-scoped index") consumed by Step 22's `openQuestion` DAO; Step 27 is "Question lifetime across session boundaries + the AC-8a line" and its body (:3222–3286) mentions no dedup index; T-38-31's own Data (re-ask after a blanket clear, no session boundary) exercises Step 22/25, not Step 27. Also noted, not a defect: `src/security/trust.ts` is Step 6's per step-decl (:1207) and §5.1 (:409) while Step 11's heading says "trust type" — Step 11's body correctly says "The `Trust` type and its helpers are Step 6's" (:1814) and Step 11 only adds `typecheck_trust` (T-11-5); a heading nit at most. |
| R5 expert | Systemic Instance B — §10A "Test tier split" Job/Steers fields still say "markdown probes" | Systemic | ABSENT | §10A D-plan-11: Job "without handing a non-programmer a probe"; Steers "Executing the scripts and recording outcomes" (:5732–5761); D-plan-13 (CI tiers) :5779; D-plan-17 (two test levels) :5844 | `grep -in 'probe' ours.md` within §10A → only D-plan-11's negations and `probe:` ids of `run-plan-probes.mjs`. |
| R5 expert | Minor — Step 38's `oracleSpawn` paragraph duplicated verbatim | Minor | ABSENT | — | Executed duplicate-paragraph scan over all 483 paragraphs >200 chars → 0 duplicates. |
| R5 expert | Tentative — §14.2 "(Step 8)" ambiguity | Tentative | N-A | §14.2 "**None.**" (:9404) | No such entry. |
| R6 collapse-hunt | Collapse 1 — Step 9 cites "Step 41's ULID util"; ULID is Step 37 | Collapse | ABSENT | Step 9 "Create `src/util/ulid.ts` (ULID generation, AD-26)" (:1626); §5.1 row `src/util/ulid.ts` create S9 (:450) | ULID util is built in the same step that uses it; no forward citation. |
| R6 collapse-hunt | D-plan-2 residual — no committed lockfile, so a fresh `npm install` may drift from the audited version | Residual note | **PRESENT (as R7 Critical 1, below)** | Step 1 (:746–749) exact pins; no lockfile anywhere | Exact pins remove the version-drift half; the lockfile absence is fatal for a different reason — see R7 expert Critical 1. |
| R6 collapse-hunt | Exit-run report shape — bootstrap of captured hook streams | Note | N-A | Step 39 leg 1 reconstructs hook streams from transcripts (:4396–4405) | Different design; no "captured hook streams" premise. |
| R6 expert | Moderate 1 — §3 says the web-tree-sitter bump question is "bin 1 answered" after reclassification to bin-2 | Moderate | ABSENT | §3 npm registry entry: "The plan pins the runtime dependencies to the versions the architecture verified (V14)" (:249–252); §14.2 "**None.**" (:9404) | `grep -in 'bin 1\|bin-1\|bin 2\|bin-2' ours.md` → only the §14 headings. |
| R6 expert | Moderate 2 — D-plan-2/§14.2 claim `^0.26.13` accepts 0.27.0 (false under node-semver) | Moderate | N-A | Step 1: `{"web-tree-sitter": "0.26.13", "tree-sitter-wasms": "0.1.13"}` "(exact pins, no range)" (:747–748); Gate 3 "Not a caret range on the runtime deps" (:863) | No caret claim exists (`grep -n caret ours.md` → :863 only, a negation). |
| R6 expert | Minor 1 — §14.4 has no Pass L | Minor | N-A | §14.4 lists Pass 1–7 (:9426–9469) | Different ledger; last pass is the round-4 corrections + re-read, consistent with STATUS.md. |
| R6 expert | Minor 2 — "`.mcp.json`-registry re-check" garbled phrase | Minor | ABSENT | — | `grep -n '\.mcp\.json' ours.md` → :323, :6956, both correctly "MCP configuration". |
| R7 collapse-hunt | Collapse 1 — Step 2 cites a `SqliteError` class `node:sqlite` does not export | Collapse | ABSENT | — | `grep -in 'SqliteError\|ERR_SQLITE' ours.md` → 0. |
| R7 collapse-hunt | Minor 1 — §5.1 lacks "# Step N" attribution for three files | Minor | N-A | §5.1 generated table has a `step` column for every row (:330–620) | Derived from step-decls; `derive --check` OK. |
| R7 collapse-hunt | Minor 2 — §10 D-plan-2 heading says "(exact)" while Step 1 ships a caret | Minor | ABSENT | Step 1 exact pins; §10A "D-plan-2 (dependency pins)" (:5541) | Consistent. |
| R7 collapse-hunt | Minor 3 — §12.5 AC-21 row has an extra column | Minor | ABSENT | §12.4 AC→T-ID table (:8964–9008) | Executed: 41 table rows, every row has exactly 3 cells. |
| R7 expert | **Critical 1 — Step 1 specifies `npm ci` but no `package-lock.json` is ever created; T-1-1 and every CI run fail unconditionally on a fresh checkout** | Critical | **PRESENT** | Step 1 CI: "`test`, `runs-on: ubuntu-24.04`, runs `npm ci`, `npm run build`, `npm test`" (:808); Verification "`T-1-1` (clean `npm ci` + `npm run build` from a fresh checkout …)" (:869); T-1-1 "**Data.** The committed `package.json`/`tsconfig.json`; `npm ci …`" (:7055); cold-container job "`npm ci`, `npm run build`, `ctxoracle init`" (:4322); Step 1 step-decl `create:` has no lockfile | Executed in scratchpad `npmci_test/` with a `package.json` carrying our exact pins and no lockfile: `npm ci` → `npm error code EUSAGE … The npm ci command can only install with an existing package-lock.json …`, exit 1 (npm 10.9.7 / Node 22.22.2). `grep -in 'package-lock\|lockfile\|shrinkwrap' ours.md` → only :2080 (a `.gitignore` pattern list in Step 14's zone classifier, unrelated). Our exact pins do not rescue this: `npm ci` refuses before resolution. |
| R7 expert | Moderate 1 — D-plan-2 heading "(exact)" contradicts caret in Step 1 | Moderate | ABSENT | as R7 collapse-hunt Minor 2 | — |
| R8 collapse-hunt | **Collapse 1 — Step 5 rule 1 keys on `git rev-parse --is-inside-work-tree` printing "false"; in the non-git directory the rule exists for, git exits 128 with no stdout** | Collapse | **PRESENT** | Step 5 "Rules, in order: 1. `git rev-parse --is-inside-work-tree` false → rule 4." (:1101); rule 4 "SHA-256 of `fs.realpathSync(repoPath)`; `mode='path'`. Also taken, with a diagnostic, when `--is-shallow-repository` prints anything other than `true`/`false`." (:1118–1120); T-5-1 "Fails when … the non-git directory is not path-keyed" (:7197) | Executed (git 2.43.0): in a fresh non-git directory `git rev-parse --is-inside-work-tree` → `fatal: not a git repository …`, exit 128, empty stdout; inside `.git/` → prints `false`, exit 0; in a work tree → `true`. Grep of Step 5 (:1082–1200) for `exit|fatal|throw|non-zero|error` → 0 hits: the plan never says what a failed invocation routes to. Narrower than theirs (our destination is rule 4, not rule 3), but the stated trigger condition does not occur for the fixture `repo-key-nongit`; an implementer must invent "non-zero exit ⇒ rule 4". |
| R8 collapse-hunt | **Collapse 2 — the miner runs `git log --numstat -M` and never handles `-M`'s rename line syntax (`old => new`, `dir/{a => b}/f`)** | Collapse | **PRESENT** | Step 13: "runs `git log --no-merges --numstat -M --format=%H%x00%at%x00 <watermark>..HEAD` streamed line-by-line; … generates all canonical-ordered (`a < b`) file pairs from the touched-file set" (:1994–2004); T-13-1 Data plants no rename (:7453–7462) | Executed in scratchpad `renamerepo/`: `git log --no-merges --numstat -M --format=%H%x00%at%x00` prints `0\t0\ta.txt => b.txt` and `0\t0\tsrc/{utils => other}/c.txt` for renamed files (also without `-M`, since git's default `diff.renames` is on for log). `grep -in 'rename\| => \|similarity' ours.md` → no miner-related hit. A third-field path extraction inserts `a.txt => b.txt` as a file identity or drops the line; no fixture catches it; the exit run on real repos is where it would silently surface. |
| R8 expert | Serious 1 — Step 37 takes the reindex lock "via `flock`(2)", which `node:fs` does not expose | Serious | ABSENT | Step 14 `acquireReindexLock(home, key)`: "the exclusive advisory lock on `<home>/projects/<key>/.reindex.lock` (`fs.openSync` with `wx`, pid written, stale-lock detection by pid liveness)" (:2141–2145) | `grep -in flock ours.md` → 0. Mechanism is the dependency-free `wx` create. (Release/staleness/race coverage assessed under R9/R12 below.) |
| R8 expert | Moderate 1 — blanket claim that `node:test` cannot run `.ts` on Node ≥ 22.16 (false from 22.18.0) | Moderate | ABSENT | §3: "type stripping is default from v22.18.0 (experimental behind `--experimental-strip-types` from v22.6.0)" (:241–243); Step 1 Gate 3 (:835–836); §11.4 claim :6459 with changelog evidence; §11.4 :6488 `const enum` claim | Our text states the version boundary and the `const enum` reason. |
| R9 collapse-hunt | Collapse 1 — Step 20's rename fix names a regex but never specifies brace expansion | Collapse | N-A | — | Ours has no rename handling at all (the upstream R8 Collapse 2 is PRESENT); nothing to expand. The fix for R8 C2 must specify brace expansion so this does not become PRESENT. |
| R9 collapse-hunt | Finding 2 — round-8 collapse findings not swept into §11's claims registry | Systemic | N-A (but see fix order) | §11.4 (:6457–6913) | Our §11.4 has no entry for `git rev-parse` failure semantics or `--numstat -M` rename syntax (`grep -n 'rev-parse\|numstat\|rename' ours.md` in §11 → 0); when R8 C1/C2 are fixed the executed evidence belongs there. |
| R9 collapse-hunt | Finding 3 (Minor) — "mtime/PID staleness check" unspecified and untested | Minor | **PRESENT (partial)** | Step 14: "(`fs.openSync` with `wx`, pid written, stale-lock detection by pid liveness) that `runIndex` takes; a second concurrent index refuses. The handler never waits on the lock" (:2141–2145); T-14-1 "**Fails when** … two concurrent reindexes both proceed" (:7486) | Executed greps: `grep -in 'unlink\|release\|reclaim' ours.md` → no lock-related hit; §12 lock mentions are T-14-1 (refuse), T-14-2 (absence during `refreshIfStale`), T-28-5 (presence as trace). The staleness mechanism is named ("pid liveness") but no reclaim algorithm (read pid → `process.kill(pid,0)` → unlink → retry?) and no test plants a dead-pid lock and asserts reclaim. |
| R9 expert | Serious 1 — brace-abbreviated rename form not expanded | Serious | N-A | — | Same as R9 collapse-hunt Collapse 1. |
| R9 expert | **Serious 2 — lock has no release step and no staleness threshold; no test exercises acquire/release/staleness; self-refresh would stall after the first successful run** | Serious | **PRESENT (reduced)** | Step 14 :2141–2145 as above | No release (`unlink` on completion) is specified anywhere. Consequence is reduced in ours because "stale-lock detection by pid liveness" would reclaim a lock whose recorded pid has exited — so the permanent stall does not follow — but (a) the reclaim algorithm is unspecified, (b) pid reuse makes a dead lock look live indefinitely with no threshold to break the tie, (c) no §12 test covers reclaim, and (d) no fault code exists for "lock held / not acquired" (Step 6 list :1216–1235). |
| R10 collapse-hunt | **Collapse 1 (Serious, silent) — stale-lock reclaim is an unsynchronized check-then-act: two reclaimers of the same abandoned lock both end up believing they hold it** | Serious | **PRESENT** | Step 14: "`acquireReindexLock(home, key)`: the exclusive advisory lock on `<home>/projects/<key>/.reindex.lock` (`fs.openSync` with `wx`, pid written, stale-lock detection by pid liveness) that `runIndex` takes; a second concurrent index refuses." (:2141–2144); T-14-1 "Fails when … two concurrent reindexes both proceed" (:7486) tests only the fresh-lock refuse path | Re-executed against OUR design (scratchpad `lockrace/`, Node v22.22.2): `lock.js` implements the plan's text literally (`openSync(p,'wx')`, write pid; on `EEXIST` read pid, `process.kill(pid,0)` liveness, unlink-and-retry when dead — the reclaim step the plan leaves unspecified); `race.js` plants a lock holding a genuinely dead pid and launches two real processes behind a barrier, 200 iterations → `bothAcquired=29`; sample `[{"pid":3544,"outcome":"acquired-after-reclaim","lockHolderAtEnd":"3544"},{"pid":3545,"outcome":"acquired-after-reclaim","lockHolderAtEnd":"3544"}]`. Their mechanism (mtime threshold) differs; the TOCTOU root is identical because our plan names "pid liveness" as the staleness test and no atomic reclaim step. No test in §12 races two reclaimers. |
| R10 collapse-hunt | Collapse 2 — `reindex.lock_stale_ms` has no §10A collapse-test | Collapse | N-A | — | No such tuning key in ours (`grep lock_stale ours.md` → 0). The lock mechanism itself also has no §10A entry in ours (D-plan-* list :5517–6164 read; none covers the reindex lock) — fold into the fix for R10 C1. |
| R10 collapse-hunt | Finding 3 (Minor) — brace regex mis-parses filenames containing literal `{`/`}` | Minor | N-A | — | No rename handling exists (R8 C2 PRESENT); the fix for R8 C2 should handle or disclose this case. |
| R10 collapse-hunt | Closure note — T37-3's "NOT asserts" points to T37-1 for cross-process contention, which does not test the directory lock | Note | N-A | — | No such cross-reference in ours; T-14-1 does test two concurrent reindexes (fresh lock). |
| R10 expert | Critical 1 — web-tree-sitter 0.26.13 cannot load tree-sitter-wasms 0.1.13 grammars | Critical | (skipped by instruction) | Step 1 pins 0.26.13 / 0.1.13 (:747) | Handled separately. |
| R10 expert | Moderate 1 — `reindex.lock_stale_ms` 600000 ms default asserted "well beyond any real index run" with no measurement | Moderate | N-A | — | No such threshold in ours. |
| R11 collapse-hunt | Collapse 1 (Serious) — atomic-rename reclaim still loses to a delayed check-then-act (rename-vs-recreate) | Serious | N-A (mechanism) / root PRESENT | — | Ours has no rename reclaim; the root race is PRESENT as R10 C1 above. |
| R11 collapse-hunt | Collapse 2 (Moderate) — §13 R11 "not data corruption" contradicts Step 37 "silent index corruption" | Moderate | N-A | §13 R9–R12 (:9092–9112) hold no concurrent-reindex risk entry | No contradiction exists because no risk entry describes the concurrent-reindex consequence at all; the fix for R10 C1 should add one (two unserialized `mineCochange` passes double-count `cochange_pairs` for the overlapping range — Step 13 accumulates with `INSERT … ON CONFLICT DO UPDATE` :2003). |
| R11 collapse-hunt | Finding 3 (Minor) — §14.4 has no entries for rounds 6–10 | Minor | N-A | §14.4 Pass 1–7 (:9426–9469) | Different ledger, consistent with STATUS.md. |
| R11 expert | Serious 1 — rename reclaim fix closes only the rename-vs-rename race | Serious | N-A (mechanism) / root PRESENT | — | As R11 collapse-hunt Collapse 1. AD-26's disclosure note also N-A. |
| R12 collapse-hunt | Collapse 1 (Serious) — content-token mismatch-restore step is an unguarded write a third acquirer can land inside | Serious | N-A (mechanism) / root PRESENT | — | No token/restore step in ours. Their eventual remedy (commit `2535371`: mutual exclusion as a transactional `store.db` row) is the recommended shape for our fix below. |
| R12 collapse-hunt | Finding 2 (Minor) — orphaned `.reclaiming-<pid>` file on crash between sub-steps | Minor | N-A | — | No `.reclaiming-*` files in ours. |
| R12 collapse-hunt | Dismissed concerns (UUID collision; token read racing a write; 3-way rename race) | — | N-A | — | Not findings; mechanisms absent in ours. |
| R12 expert | Serious 1 — restore-window race (same as R12 collapse-hunt Collapse 1) | Serious | N-A (mechanism) / root PRESENT | — | As above. |
| Clear-thought verification doc | Outcome (self-administered pass; N5 sharpening) | — | N-A | — | Process artifact of their lineage; it names no defect in the plan text. |

## Executed checks over OUR plan (summary)

- `derive-plan-sections.mjs --check` → `OK: 40 steps, 13 elements, 123 test specs, 19 probes cited, regions current`.
- Test-id reconciliation: 123 cited ids = 123 §12 headings, `comm` both ways empty.
- §12 `**File.**` paths vs step-decl `create:`/`modify:` → 0 orphans (`file_check.py`).
- 584 "Step N" mentions dumped with context and read against the 40 headings → one misattribution (T-38-31 → "Step 27").
- Duplicate-paragraph scan (483 paragraphs > 200 chars) → 0.
- AC→T-ID table: 41 rows × 3 cells.
- `npm ci` without lockfile → `EUSAGE`, exit 1.
- `git rev-parse --is-inside-work-tree` in a non-git dir → exit 128, no stdout; inside `.git/` → `false`.
- `git log --no-merges --numstat -M --format=…` on a repo with renames → `a.txt => b.txt`, `src/{utils => other}/c.txt`.
- Two-process stale-lock reclaim race per Step 14's text → 29/200 iterations both acquire.

## PRESENT findings, in the order to fix

1. **R7 expert Critical 1 — `npm ci` with no lockfile (Step 1, T-1-1, CI, cold-container job).** Our plan must say: Step 1 runs `npm install` once after authoring `package.json` and commits the generated `package-lock.json`; add `middleware/context-oracle/ctxoracle/package-lock.json` to S1's `create:` list (so §5.1 derives it) with a note that it is npm-generated and must be regenerated whenever a pin changes; T-1-1 Data: "the committed `package.json`, `tsconfig.json`, and `package-lock.json`; `npm ci …`"; keep `npm ci` everywhere (it is the reproducible install AD-25 wants, and with exact pins the lockfile is a faithful record). Add a §11.4 entry with the executed `EUSAGE` evidence. (Fix first: every other step's build depends on it.)

2. **R8 collapse-hunt Collapse 2 — miner rename lines (Step 13, T-13-1).** Our plan must say: a `--numstat` line whose third field contains ` => ` is a git-detected rename; expand the brace form `prefix{old => new}suffix` (including an empty side, e.g. `{ => dir}/f`, and nested-directory prefixes) to `prefix+old+suffix` and `prefix+new+suffix`, and add **both** paths to the commit's touched-file set (the rename commit touches both identities); a third field containing a literal `{`/`}` that does not parse unambiguously is skipped with a `miner_unparsed_numstat` diagnostic rather than guessed (name the code in Step 6 or reuse an existing one explicitly). T-13-1's fixture must plant one plain rename and one brace-form rename inside the coupling history and fail when either identity is missing from the pair counts or a ` => ` string lands in `files`/`cochange_pairs`. Record the executed output shapes in §11.4.

3. **R8 collapse-hunt Collapse 1 — Step 5 rule 1 (T-5-1).** Our plan must say: each `git` invocation in the resolver goes through a helper returning `{ok:true, stdout} | {ok:false, code, stderr}`; rule 1 is "`git rev-parse --is-inside-work-tree` **fails** (non-zero exit — `fatal: not a git repository`) **or prints `false`** → rule 4"; rules 2–3 likewise route any failed invocation to rule 4 with the `head_unresolved`-style diagnostic already used for the shallow-probe anomaly. T-5-1 adds the assertion that `repo-key-nongit` is path-keyed *through the failure branch* (assert the diagnostic detail names exit 128), and §11.4 gets the executed evidence (exit 128 vs literal `false` inside `.git/`).

4. **R10 collapse-hunt Collapse 1 + R9 expert Serious 2 + R9 collapse-hunt Finding 3 — the reindex lock (Step 14, T-14-1/T-14-2/T-28-5, §13).** Our plan must replace the unspecified pid-liveness reclaim with a race-free mechanism and specify release. Recommended (the shape their round 12 converged on, and one our substrate already has): mutual exclusion as a `schema_meta` row (`reindex_owner_pid`, `reindex_started_at`) taken inside one `Store.transaction` (`BEGIN IMMEDIATE`, Step 3) — the transaction reads the row, treats it as held only when the pid is alive (`process.kill(pid,0)`, `EPERM` counts as alive) and, if free or stale, writes its own pid in the same transaction; the check-and-write is atomic under SQLite's write lock, so two reclaimers cannot both win; release clears the row in a `finally` on completion or failure; a lock that cannot be taken is recorded as a new fault code `reindex_locked` (add to Step 6's list) rather than silently skipped. Update T-14-2 and T-28-5, which currently use the presence/absence of `.reindex.lock` as the observable, to observe the row instead; extend T-14-1 with a two-real-process case: a planted stale row (dead pid) raced by two `runIndex` children, asserting exactly one proceeds and the row is cleared afterwards (the `lockrace/race.js` harness is the template). Add a §10A entry for the lock decision (there is none) whose hardest question is the two-reclaimer race, and a §13 risk naming the consequence of two unserialized passes (double-counted `cochange_pairs` for the overlapping range — R11 C2's substance). If the file lock is kept instead, the reclaim must be atomic (rename-to-unique-path then verify content token) *and* release must be specified — but the transactional row avoids the whole lineage of races rounds 10–12 chased.

5. **R3 collapse-hunt Collapse 2 — `deny_bypass_suspect` both directions (Step 26, Step 33, Step 39 report, T-33-1).** Our plan must say the printed line is: "bypass diagnostic: recognizes only <list> (a bypass by any other shell write path is not counted — under-count); a same-target shell write that was not a bypass is counted (over-count); a proxy, not a measurement (AD-9)". Step 33's `status` bullet and Step 39's report field carry both directions; T-33-1 fails when either direction is absent from the rendering (mirror the wording as the assertion, as their round-4 fix did).

6. **R5 expert Instance A (class) — T-38-31 Verifies field.** Our plan must say "Step 22 (open-scoped dedup index via `openQuestion`; index DDL Step 7)" instead of "Step 27 (open-scoped dedup index)" at :8915. (Optional heading nit: Step 11's title says "trust type" while `trust.ts` is Step 6's — rename to "…, trust-type compile check" or drop the phrase.)

Follow-on when fixing 2 and 3: R9 collapse-hunt Finding 2 asks that the executed evidence land in §11.4 — do it in the same edit so the next round does not re-derive it.

## Counts

76 findings classified (plus 1 skipped by instruction — R10 expert Critical 1): **PRESENT 9 · ABSENT 34 · N-A 33.**
The 9 PRESENT rows collapse to 6 distinct defects to fix (R6's lockfile residual is the same defect as R7 Critical 1; R9 Finding 3, R9 Serious 2 and R10 Collapse 1 are one lock defect), listed in fix order above.
