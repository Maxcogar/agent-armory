# Expert review — `docs/plans/plan-phase-a.md`, round 3

**Date:** 2026-09-07
**Reviewer:** independent `/expert-review` pass (expert-review SKILL.md R1.2, Post-fix protocol). Not the plan's author, not the author of any prior review or of `2026-09-07-round-3-author-gates-review.md`. The prior rounds reached this reviewer only as the written records named below; nothing in the author's self-check was accepted without re-derivation.
**Artifact:** `middleware/context-oracle/docs/plans/plan-phase-a.md` as installed in the working tree at commit `d667534` (`git status` clean; 7,106 lines; 16 sections; 40 steps; 122 test specifications, `**T<step>-<n>` definitions counted = 122 and every T-ID referenced anywhere in the document resolves to one of them — `comm` of the referenced set against the defined set is empty; D-plan-1…23).
**Round:** 3. Round 1 = `docs/reviews/2026-09-06-plan-expert-review.md` + `2026-09-06-plan-collapse-hunt.md`; round 2 = `docs/reviews/2026-09-07-round-2-expert-review.md` (29 findings: C1, S1–S5, SY1, M1–M11, m1–m11, tentative T1–T2) + `docs/reviews/2026-09-07-round-2-collapse-hunt.md` (20 findings: S-1…S-5, M-1…M-8, m-1…m-7). The round-2 artifact is commit `149ffc8`, used below only for provenance classification (extracted to a scratch file and read at the passages named per finding).

Every location is written as `docs/plans/plan-phase-a.md:start-end` (or `path:line`, or `path#section`). `arch` abbreviates `docs/architecture-phase-a.md`, `spec` abbreviates `docs/specs/spec-context-oracle.md`, `r2` abbreviates the `149ffc8` revision — used only in evidence prose, never in a Location field. Every literal-content claim was Read at the cited lines in this session; every execution claim states the command and its observed output.

---

## Scope and Inventory

### Round and sources of the post-fix inventory

Per SKILL.md Step 2 (Post-fix review): the prior review's full inventory, the fix-diff files, their dependents, and the prior findings as closure items. The fix-diff is one file (`docs/plans/plan-phase-a.md`, `149ffc8` → `d667534`) plus one new file the fix added (`docs/reviews/2026-09-07-plan-tool-traces.md`); neither has code dependents (the project holds one code file, `tools/check_docs.py`, with zero dependents — re-verified by `ls -a middleware/context-oracle/` this session: `.claude .mcp.json CLAUDE.md OWNER-LEDGER.md RETHINK.md docs tools`).

### Files in scope — every file below is `[x]`

- [x] `docs/plans/plan-phase-a.md` — **Read in full**, lines 1–7106 in twelve contiguous reads (1–600, 601–1200, 1201–1800, 1801–2400, 2401–3000, 3001–3600, 3601–4200, 4201–4800, 4801–5400, 5401–6000, 6001–6600, 6601–7106). Grep-verified in addition: `**T<n>-<m>` definitions (122; 122 unique); every `T<n>-<m>` reference resolves (`comm -23 refs defs` → empty); `plan-tool-traces` (0 hits — the plan does not cite the trace file); narration vocabulary (`round-1|round-2|prior version|earlier version|previous revision|was fixed|re-authored|rewrite|the fix|regression|finding` — hits enumerated: lines 22, 1067, 1659, 3377, 3403, 3406, 3413, 3505, 3770, 3929, 4149, 6642, 6666, 6734, 7051 — every one is a use of the word in its ordinary sense (a finding to investigate, the AD-24 regression fixtures, "Rewrite `docs/STATUS.md`"), none narrates a prior version or a correction); `regret row|regret table|CREATE TABLE regret` (4 hits: 2737, 2746, 2774, 5875 — no table, no DAO); `recognizeQuestions(` (1 definition, line 2220); `recordFault|fault_writer` (7 hits, enumerated in the T10-3 check).
- [x] `git show 149ffc8:middleware/context-oracle/docs/plans/plan-phase-a.md` (5,999 lines) — Read at the passages needed for provenance: Step 6 (r2:1017–1060, already four-part), Step 14 `refreshIfStale` (r2:1474–1484), Step 16 (r2:1565–1580), Step 28 head and pipeline (r2:2262–2300), T3-3 (r2:4208–4224), §14.4 (r2:5856–5880); grep-verified for `verdict.emit` (1 hit, r2:2287), `trivial steps are` (r2:668), `Symbol\[\]` (r2:1455), `liveness|transcript_bytes` (1 hit, the pid-liveness sentence r2:1481 — no liveness row), `integrity-check` (0 hits), `stale_index|index_head` (Step 14 only — none in Step 16), `three replay|two replay|replay tests above` (0 hits), `Guard (|Watchdog start` (r2:2276–2277), `cold-container|clean container` (no CI job; r2:5396 "invoked from CI's clean container job").
- [x] `docs/reviews/2026-09-07-round-2-expert-review.md` — Read in full (394 lines). Provenance source for closure items C1, S1–S5, SY1, M1–M11, m1–m11, T1–T2.
- [x] `docs/reviews/2026-09-07-round-2-collapse-hunt.md` — Read in full (581 lines). Provenance source for closure items S-1…S-5, M-1…M-8, m-1…m-7.
- [x] `docs/reviews/2026-09-07-round-3-author-gates-review.md` — Read in full (99 lines). Treated as the author's claims; every row re-derived.
- [x] `docs/reviews/2026-09-07-plan-tool-traces.md` — grep-verified: headings (`## 1. CodeGraph survey`, `## 2. Clear Thought trace`); `totalFiles: 1`, `totalDocFiles: 71`, `parseErrors: 0`, `builtAt: 2026-09-07T15:08:53.583Z` (matches §11.6); the NOTE at line 726; every `[D<n> <topic>` label (D1 build-order 18, D2 test-execution 16, D2b typescript-pin, D3 exit-run 10, D4 settings-marker 13, D5 model-seam 13, D6 lag-window 8, D7 thresholds 8, D8 recursion-guard 8, D9 hooks-timeout-drift 8, D10 L11-verifications 8); `sequential_thinking","prompt"` count = 46; `decision_framework","prompt"` count = 7 (lines 881, 1116, 2168, 2258, 2362, 2462, 2562); Read at lines 1304–1335 (D3 conclusion), 1412–1470 (D4 option B and conclusion), 1574–1605 (D5 option C and conclusion).
- [x] `docs/reviews/README.md` — Read in full (39 lines).
- [x] `.claude/skills/expert-plan/SKILL.md` — Read in full (390 lines).
- [x] `.claude/skills/expert-plan/references/output-contract.md` — Read in full (82 lines).
- [x] `.claude/skills/expert-plan/references/testing-standards.md` — Read in full (140 lines).
- [x] `.claude/skills/expert-standard/SKILL.md` — Read in full (59 lines).
- [x] `claude-plugins/expert-dev-tools/skills/expert-review/SKILL.md` — Read in full (632 lines).
- [x] `docs/specs/spec-context-oracle.md` — Read `:120–1138` (§3–§14 in full: §4 genres, §5, §6 FR-M*, §7, §8 FR-B*/FR-O*/C-*/NF-1, §9, §10, §11.1–11.5, §12 D-*, §13, §14 AC-1…AC-25 and the phasing paragraph); every §11.1 citation of the plan re-read at the cited lines.
- [x] `docs/architecture-phase-a.md` — Read `:60–100` (in-scope list), `:100–700` (V1–V19, AD-1…AD-8), `:700–1300` (AD-9…AD-18), `:1300–1720` (AD-18 regret proxy, AD-19…AD-26, reasoning chain), `:1720–2058` (threat model, ASVS, traceability, L1–L11, standards) — the document in full; every §11.2 citation of the plan re-read.
- [x] `OWNER-LEDGER.md` — Read in full (80 lines).
- [x] `CLAUDE.md` (project) — Read in full (238 lines).
- [x] `docs/collapse-log.md` — Read `:830–902` (2026-08-25), `:1057–1090` (2026-09-03 round 8), `:1121–1156` (2026-09-04).
- [x] `docs/IDEAS.md:92–175` — Read (#14).
- [x] External / executed, all this session (2026-09-07):
  - `https://code.claude.com/docs/en/hooks` — WebFetch (two prompts) **and** `curl` (2,821,561 bytes, tag-stripped, grep'd): timeout semantics ("A timed-out `command`, `http`, or `mcp_tool` hook doesn't block the tool call. The call continues through the normal permission flow"); the settings-entry fields (`type`, `if`, `timeout`, `statusMessage`, `once`; command: `command`, `args`, `async`, `asyncRewake`, `shell`; http/mcp_tool/prompt/agent fields); `SessionStart` matcher values `startup, resume, clear, compact, fork`; the common input fields incl. `prompt_id`, `permission_mode`, `effort`; the `transcript_path` lag sentence verbatim; `UserPromptSubmit` input example with `prompt`; Stop input `stop_hook_active`, `last_assistant_message`, `background_tasks`, `session_crons` and "Claude Code overrides the hook and ends the turn after 8 consecutive blocks"; Stop `hookSpecificOutput.additionalContext` ("the same loop protections as `decision: "block"`, namely the `stop_hook_active` input and the 8-consecutive-continuation cap … no hook error notification is shown"); `SubagentStop` input `agent_id`, `agent_type`, `agent_transcript_path`; `PreToolUse` output `hookEventName`/`permissionDecision`/`permissionDecisionReason`/`updatedInput`/`additionalContext`; `PostToolUseFailure` ("After a tool call fails") with `updatedToolOutput` on `PostToolUse`; the three settings-file locations.
  - `list_repos` (query `maxcogar`) — 41 repositories, `has_more: false`; `Maxcogar/NOVA` pushed 2026-09-04, `Nova-Integrations` 2026-08-22, `Design-Navigator-MCP-UI` 2026-08-02, `Project-Manager` 2026-08-01, `Turbine-Studio` 2026-07-31 — as §11.4 states.
  - Layout reproduction `scratchpad/layoutprobe/` (Node v22.22.2, typescript 5.9.3, the Step 1 tsconfig with `exclude`): executions recorded per finding (T3-3 timeline ×3; per-fixture `tsc --noEmit … --types node <fixture>` with a `src/` import → `TS2554`, exit 2; `tsc -p` with the exclude → exit 0 and no fresh `dist/test/build/fixtures/` output — the one file present there is the round-2 reviewer's `bad.js` dated 16:15, before this session's 16:5x run; brand-cast and brand-annotation fixtures; `unshare -rn true` → exit 0).

### Tool plan (Step 3)

| Claim type | Instrument used |
|---|---|
| Literal content of the plan / spec / architecture / ledger / collapse-log / trace file | `Read` at file:line (recorded per finding) |
| Absence claims over the plan ("no table X", "no citation of Y") | `grep` over the whole plan with query and hit count recorded, then Read of the candidates |
| Library / runtime behaviour (`node:sqlite` busy handling, `tsc` single-file compilation, TypeScript brand semantics, `unshare`) | direct execution on Node v22.22.2 / typescript 5.9.3 in the layout reproduction |
| Hooks-contract claims | WebFetch + `curl` of `code.claude.com/docs/en/hooks`, quotes recorded |
| Registry / repository facts | `list_repos` (the registry reads of §11.4 were re-verified by round 2 on the same date and are not re-run; no finding rests on them) |
| Prior-document claims (both round-2 reviews, the author's gates review) | re-derived from the current plan by Read/grep before any closure was recorded |
| Provenance (new / recurring / regression) | `git show 149ffc8:…`, Read/grep at the passage |
| Structural / blast-radius | not needed — a document with no code dependents (CodeGraph unavailable in this session; not load-bearing) |

**Unavailable instruments and disposition.** Clear Thought MCP is not loadable in this session (no `clear_thought` tool is exposed; `.mcp.json` configures it for `npx`, which this reviewer did not launch); the mandatory `metacognitivemonitoring` baseline and the pre-delivery `collaborativereasoning` check were performed manually (recorded below and in Observations). A Node 22.16.0 binary is not present (v22.22.2 only) — floor-only behaviour stays at the plan's own status (CI matrix). No CI runner is reachable from here — claims about the GitHub Actions runner image (`unshare` under its user-namespace policy) are tentative. No rigor waivers were requested.

### Metacognitive baseline (recorded before drafting)

Known from verified reads this session: everything cited by line above. Inferred and therefore verified before use: SQLite busy-handler behaviour under the T3-3 timeline (executed, three runs); whether a TypeScript `unique symbol` brand blocks a type assertion (executed); whether the tsconfig `exclude` keeps a fixture out of the project build while a per-file `tsc` still reaches its `src/` imports (executed); whether the current hooks reference still documents `additionalContext` on `Stop` and the eight-continuation cap (fetched, quoted). Not verifiable here and left at that status: the GitHub Actions runner's user-namespace policy; the Node 22.16.0 floor; the Clear Thought server's own run.

### Round-2 closure table (each of the 49 prior findings re-derived from the current plan; closed only against its originally named standard)

| Prior finding | Original standard | Current state (Read) | Status |
|---|---|---|---|
| ER C1 / CH S-2 — must-fail fixtures inside the build | Gate C (paths confirmed); testing-standards (able to fail) | `plan:708-725` `"exclude": ["test/build/fixtures"]`, per-fixture `tsc --noEmit …` via `test/build/tsc_fixture.ts`; §11.4 `plan:4695-4706`. Re-executed here: with the exclude `tsc -p` exits 0; the per-fixture command on a fixture importing `src/` fails with `TS2554`. | **Closed** |
| ER S1 — generator/harness after their consumers; false §9 attestation | expert-plan Step 8 Verification "after building it"; Step 10 checkpoint | Generator + transcript fixtures at Step 1 (`plan:727-744`); harness + `hook` verb at Step 28 (`plan:2568-2587`); §9 rewritten (`plan:3457-3506`). | **Closed for the named artifacts** — the same standard is violated at other sites the round-2 finding did not name (Step 28 consumes Step 29 and Step 32; Step 14 consumes Steps 28/32): see S1 and S2 below |
| ER S2 / CH M-5 — three AD-17 detectors unscheduled; AC-9/AC-13 rows false | output-contract §2 coverage; Step 9 traceability | `index_stale` at `plan:1727-1730` + T14-2; liveness row `plan:2594-2596` + T28-5; `produced_but_undelivered` `plan:2612-2615` + T28-4; `hooks_not_firing` `plan:2965-2970` + T33-4; §12.4 AC-9 row `plan:6579`, AC-13 row `plan:6583`; T16-1 stale case `plan:5314-5317`. | **Closed** (the closure introduced SY-1's regression component, S2, and M2 below) |
| ER S3 / CH — regret population narrowed | SKILL.md:50 (no silent narrowing); Gate C coverage | `plan:2737-2748` population = store-held facts incl. `never_triggered`; T30-1 `plan:5862-5877`; Q42 `plan:6937-6940`. Read against `spec:649-655`, `arch:1301-1307`. | **Closed** (where the row is stored is undefined — M4 below, a defect present at `149ffc8` too) |
| ER S4 / CH S-4 — floor number without a denominator | Gate A/C (no deferred choice); collapse-log 2026-08-25 item 1 | `plan:3326-3339` seeded labelled sample, rule independent of the `?` recognizer, label table published, recall + precision with `N`; D-plan-10 `plan:3725-3729`. | **Closed** |
| ER S5 — proxy listener cannot observe egress | expert-plan Step 4; testing-standards anti-pattern 4 | T32-3 structural scan `plan:5972-5984`; T32-2 under `unshare -rn` `plan:5953-5970`; §11.4 `plan:4721-4734` (Node docs + executions); D-plan-23. `unshare -rn true` exits 0 here. | **Closed** (the CI runner's namespace policy is Tentative T3 below) |
| ER SY1 — convention tests a conformant build cannot pass | output-contract Gate A (reviewer can check a build); testing-standards (failure condition) | Instance 1 (response fields): `blocks/verdict.ts` internal branded verdict `plan:2287-2299`; `types/hook_response.ts` type-only; `deliverStop` returns an internal object `plan:2056-2060`; T24-2 is an importer-set scan `plan:5624-5634`; T28-2 keeps the string scan `plan:2624-2628`. Instance 2 (DAO allow-list): T10-3 re-derived as an importer scan on the two writer tables only `plan:1470-1475`, `plan:5151-5164`; Steps 19/20/28 no longer collide with it. | **Partially closed → recurring.** The response-field and DAO instances are closed; T28-2's *input*-field list (`error`, `prompt`, `source`, `cwd`, `transcript_path`) still collides with modules the plan's own steps create — SY-1 below |
| ER M1 / CH M-3 — clear-recognizer rule vs T23-2 | testing-standards (failure condition); AD-9 fidelity | `plan:2227-2240` sentence-level discard anywhere; T23-2 `plan:5579-5598` consistent with it; AD-9 `arch:781-782` carries no positional restriction. | **Closed** |
| ER M2 / CH m-3 — "concluding position" undefined | Gate C (deferred choice) | `plan:1955-1960` defined (final non-empty sentence, not ending `?`); T18-8 `plan:5445-5462` derives from it. | **Closed** |
| ER M3 — shared types have no creating step | Step 8 Dependencies; Gate C | `plan:1113-1127` Step 6 creates `types/events.ts`, `candidate.ts`, `index_types.ts`; §5.1 `plan:408-412`. | **Partially closed → recurring.** Step 14's interface still names `Symbol[]` (`plan:1706`) while Step 6 defines `SymbolRow` — m2 below |
| ER M4 — keying-mode detection unimplementable | Gate C; AD-20 | `plan:2794-2800` computes every applicable rule's key and looks for a store at each. | **Closed** |
| ER M5 — settings-file mechanics unspecified | Gate C; AC-7 | `plan:2816-2827` create/record/re-serialize rules; `deinit` `plan:2881-2887`; T31-1 (b), T32-1 (b). | **Closed** (one residual mechanic — m7 below) |
| ER M6 — T3-3 / T29-1 timing-dependent | testing-standards (deterministic; anti-pattern 10) | T3-3 now states offsets `plan:4970-4977`; T29-1 uses `--deadline-ms 1` and a bound, no boundary case `plan:5819-5836`. | **Closed in form** — the stated offsets yield the wrong outcome (S3 below, executed) |
| ER M7 / CH m-6 — no cold-container CI job | Gate C; Step 8 Verification | `plan:759-766` `cold-container` job on `node:22.16.0-bookworm-slim`. | **Closed in form** — the job invokes a script Step 38 creates (M1 below) |
| ER M8 — no verb for the detached `quick_check` child | Gate C | `plan:2890-2894` `hook integrity-check` in Step 32; §5.1 `plan:331`; T32-4. | **Closed in form** — Step 28 now names a Step 32 verb (S1 below) |
| ER M9 — §2.3 omits CLI surface and packaging | output-contract §2 | `plan:177-178` two rows. | **Closed** |
| ER M10 / CH M-7 — leg 2 option set; minimum inconsistent; leg 1 recovery | Gate C zero tolerance; OL-11 | `plan:3286-3292` two named repositories + deterministic replacement; three sessions on at least one repository `plan:3307-3309`, `plan:3320-3323`; leg 1 `cwd` rule `plan:3260-3267`; Max's path = `export` `plan:3310-3314`. | **Closed** (the agent-driven remote path re-opens M-7's data-locality gap — M5 below) |
| ER M11 — DDL and DAO surface deferred | Step 8 ("Name the functions"); Gate A | `plan:1195-1292` full DDL; `plan:1388-1410` DAO method table. Read against `arch:424-534`: every AD-4 row present with its CHECKs; `q_open_dedup` present; `questions` correctly carries no provenance block (AD-4 `arch:460-468`); `observed_actions`/`whisper_audit`/`session_log`/`faults` match. | **Closed** |
| ER m1 / CH m-1 — narration | Gate C (no self-corrections) | D-plan-1 `plan:3527-3546` carries no "earlier order" sentence; §14.4 `plan:6962-6981` is a pass record; §7 intro `plan:677-679` states the recognizers' true position. Narration grep enumerated above: 0 narrative hits. | **Closed** (§14.4's own count is inconsistent — m5 below) |
| ER m2 — §12.3 entries lacked Level / Real-doubles | output-contract §12 (five fields) | Every T38-* entry carries both (Read `plan:6098-6549`). | **Closed** |
| ER m3 — fourth wrongful-deny component unrecorded | output-contract §10 | D-plan-20 `plan:3847-3854` + §10A `plan:4260-4274`. | **Closed** |
| ER m4 — Step 15 claims without §11 | expert-plan Step 4; Gate C | `plan:4735-4749` (package `files`, `import.meta.resolve`, `.d.ts` read). | **Closed** |
| ER m5 / CH S-1 — marker basename `ctxoracle` never written | Gate C; AC-7 | `plan:2803-2815` command = `"<execPath>" "<realpath argv[1]>" hook <event>`, pattern on `dispatch\.js`; §11.4 `plan:4707-4720` (four invocation modes executed). | **Closed** |
| ER m6 — AC-13 staleness untested | Step 9 traceability | T16-1 stale case `plan:5314-5317`, `plan:5322-5323`. | **Closed in form** — the case cannot be built against the stated signature (M2 below) |
| ER m7 — T38-26 clause cannot fail | testing-standards anti-pattern 4 | `plan:6428-6442` asserts no store/diagnostics write and no live process across the gap. | **Closed** |
| ER m8 — exit report location unrecorded | CLAUDE.md information policy | D-plan-21 `plan:3856-3863` + §10A. | **Closed** |
| ER m9 / CH M-7 — leg-2 launch and the `CTXORACLE_INTERNAL` hazard | CLAUDE.md rule 1; Gate C | `plan:3292-3300` launched through the agent's session tooling, never the wrapper; the `CTXORACLE_*` check printed and required empty; validity rule `plan:3320-3323`. | **Closed** |
| ER m10 — §5.1 annotations; runner range | Gate C reconciliation | `plan:497-499`, `plan:555` annotated; Step 38 range `plan:3241-3246`; §12.4 `plan:6638`. | **Closed** |
| ER m11 — `decideDeny` receives no tool input | Step 8 (signature delivers behaviour) | `plan:2370-2377` `decideDeny(store, consumer, toolName, toolInput)`; T25-3 `plan:5689-5691`. | **Closed** |
| ER T1 — planning-tool run unverifiable | (tentative) | `docs/reviews/2026-09-07-plan-tool-traces.md` exists; CodeGraph counts match §11.6. | **Resolved as verifiable** — and the trace contradicts the current §10 in three decisions (M3 below) |
| ER T2 — floor behaviour executed only on 22.22.2 | (tentative) | Unchanged (`plan:4603-4604`, Q28 `plan:6878-6882`); no floor binary here either. | **Still tentative** (T2 below) |
| CH S-3 — leg 1 mounts the whole transcript | CLAUDE.md rule 3; IDEAS #14 | `plan:3272-3277` growing prefix per event; D-plan-10 `plan:3720-3725`. Read against `docs/IDEAS.md:94-97`. | **Closed** |
| CH S-5 — scrub never executed as shipped; over-broad | collapse-log 2026-07-22 class | `plan:1023-1032` six-variable `SCRUBBED_ENV`; §11.4 `plan:4678-4694` three runs of the shipped command; Step 36 `plan:3115-3123`. (Not re-executed here — the `claude -p` invocation is outside this reviewer's permitted commands; the plan's evidence names the command and outputs and is internally consistent.) | **Closed on the plan's recorded execution** |
| CH M-1 — clear floor 40 | FR-B5 / P3 | `plan:1587` = 2; D-plan-7 `plan:3641-3646`; T23-2 "No." clears `plan:5585-5586`; T38-30 `plan:6497-6500`. | **Closed** |
| CH M-2 — restraint tests assert a list | collapse-log 2026-09-03 round 8 lesson 2 | T23-1 generated complement of 300 + 50 `plan:5565-5577`; T23-2 no question text `plan:5591-5593`; T18-8 50-paraphrase corpus `plan:5456-5459`; D-plan-19 `plan:3830-3845`. | **Closed** |
| CH M-4 — `T5-3` greps one spelling | AD-21 / FR-J4 structural guard | `plan:1035-1039`, T5-3 `plan:5039-5051` both specifiers. | **Closed** |
| CH M-6 — fixed timestamps vs wall-clock horizon | AD-13/AD-15 reference instant | `plan:1651-1655` reference instant = `HEAD` committer time; T13-1 `plan:5239-5245`; Step 16 `plan:1827-1828`. | **Closed** |
| CH M-8 — L11(a) reportable from a remote corpus | AD-24 `arch:1602-1606`; V12 | `plan:3278-3284` origin-keyed table; `plan:3350-3353` *verified* only with an owner-local interactive transcript; T38-32 `plan:6533-6538`. | **Closed** |
| CH m-2 — `questions` has no provenance block | AD-4 | `plan:2167-2172` `openQuestion` takes no provenance; DDL `plan:1253-1261`. | **Closed** |
| CH m-4 — env-gated test hooks | FR-O3 | D-plan-22 `plan:3865-3873`; T28-3 real induced failures `plan:5780-5791`; T29-1 `--deadline-ms`; T29-3 injected clock. Grep `CTXORACLE_TEST` → 0 hits. | **Closed** |
| CH m-5 — `ORDER BY <pk>` undefined | Gate C | D-plan-4 `plan:3582-3589`; Step 7 note `plan:1294-1296`. | **Closed** |
| CH m-7 — `tuning_missing` undefined | Gate C | `plan:1108-1111` in `FAULT_CODES`; T6-1 `plan:5058-5060`. | **Closed** |
| CH D-plan-17 note — `T25-3` hold clause tautological | testing-standards anti-pattern 4 | T25-3 `plan:5680-5696` scoped to `decideDeny`'s own properties. | **Closed** |

47 of the 49 prior findings are closed against their original standard; 2 are partially closed and recur below (SY1 → SY-1; M3 → m2). Eleven of this round's findings are defects the round-2 corrections introduced (tagged REGRESSION).

---

## Summary

This review returns **NEEDS FIXES**. The round-2 corrections closed 47 of 49 findings, and on the axis the owner weighs most the plan is sound: Steps 21–27 are still the AD-9 safe skeleton and nothing more (re-read line by line against `arch:747-879` — the only additions are the `denyFired` flag recorded as D-plan-20 and the sentence-level deferral discard, which is AD-9's rule applied, not widened); the package pins and the Node floor are unchanged; no scope element was added or dropped; every hooks-reference claim the plan makes was re-read at the live page today and holds. But the plan still cannot be built and tested as written, and most of the reasons are new: the adapter-isolation convention test still cannot pass on a conformant build because its input-field list collides with identifiers the plan's own steps mandate (a partially closed round-2 finding, made worse by the new liveness row's `transcript_path` key); the handler step consumes two modules and one verb that later steps create, so Checkpoint 3 is not executable where placed; the new `index_stale` test spawns a binary that does not exist until fourteen steps later; the new concurrency-test timeline, executed here three times against `node:sqlite`, produces the opposite of its asserted outcome; the new CI job runs a script created thirty-seven steps after the job; the new stale-index bar case needs an input the bar's signature does not carry; the exit run's agent-driven remote sessions leave their stores in containers the report cannot read; and §10's attestation that every decision was reasoned through Clear Thought is contradicted by the trace file the round-2 fix supplied. Twenty findings: 1 Serious-Systemic, 3 Serious, 5 Moderate, 11 Minor — eleven of them regressions.

---

## Upstream Contract Verification

The plan's upstream artifacts are the spec (OL-C6-signed, `spec:3-5` per the plan's own citation; `OWNER-LEDGER.md:71`) and the Phase A architecture. Both were Read in full this session.

**Spec §14 acceptance criteria — Phase A subset** (verified state = the plan's §12 specification; method = Read of the cited plan lines against the cited spec lines):

| AC | Plan mapping (§12.4, `plan:6555-6595`) | Verification | Verdict |
|---|---|---|---|
| AC-1, 1a–1d | T18-2/T38-10; T18-1/T38-11; T18-3/T38-12; T18-4/T38-13; T18-6/T38-14 | Read `plan:5347-5427`, `plan:6223-6284` vs `spec:914-931`; each headline and the AC-1 obviousness clause pinned | Honored |
| AC-2 | T24-1, T24-2, T24-3, T28-2 | Read `plan:5611-5647`, `plan:5744-5753` vs `spec:932-939` | Honored in design; T28-2 unsatisfiable on a conformant build (SY-1) |
| AC-2a, AC-2a-i allow-half | T25-1, T25-3, T38-1, T38-2, T38-3 | Read `plan:5649-5696`, `plan:6098-6136` vs `spec:940-960` | Honored |
| AC-2c over-fire / under-fire | T38-30, T38-4 / T34-2 | Read `plan:6490-6505`, `plan:6138-6151`, `plan:6058-6069` vs `spec:979-988`; "No." clears at the seeded floor of 2 | Honored |
| AC-3, 3a, 4, 5, 6 | T16-1/T38-15; T16-1/T38-16; T20-1/T38-17; T20-1/T38-20; T13-1/T38-18 | Read `plan:6286-6356` vs `spec:1000-1014` | Honored |
| AC-7 | T31-1, T31-2, T32-1 | Read `plan:5891-5951` vs `spec:1015-1016`; both settings-file states covered | Honored |
| AC-8, AC-8a | T18-7/T38-28; T27-2/T38-9 | Read `plan:5429-5443`, `plan:6458-6473`, `plan:6207-6221` vs `spec:1017-1034` | Honored |
| AC-9 | T10-1, T29-1, T14-2, T28-4, T33-4, T26-1 + T38-6, T38-8, T33-1 | Read `plan:6579` and each test vs `spec:1035-1049`; every Phase A class now has an induction | Honored in specification; T14-2 is not buildable at its step (S2) |
| AC-10 | T29-1, T28-3 | Read `plan:5819-5836`, `plan:5780-5791` vs `spec:1050-1052` | Honored |
| AC-11 | T11-1..4, T38-22, T32-3 | Read `plan:6370-6389`, `plan:5972-5984` vs `spec:1053-1055` | Honored |
| AC-12 (deterministic parts) | T38-1, T38-4, T36-1 | Read vs `spec:1056-1062` | Honored |
| AC-13 | T13-1, T14-1, T14-2, T16-1 | Read `plan:5233-5280`, `plan:5304-5323` vs `spec:1063-1065` | Honored in specification; the stale-index case cannot be built against `passesBar`'s signature (M2) |
| AC-14, AC-15, AC-17 | T19-1; T38-23; T38-24 | Read vs `spec:1066-1080` | Honored |
| AC-18 | T38-27 + Step 39 leg 3 | Read `plan:6444-6456`, `plan:3318` vs `spec:1081-1086` | Honored |
| AC-19 | T32-2, T32-3 | Read `plan:5953-5984` vs `spec:1087-1089` | Honored (runtime leg's CI availability is Tentative T3) |
| AC-20 | T38-25 | Read `plan:6416-6426`, `plan:759-766` vs `spec:1090-1092` | Honored in specification; the CI job is red until Step 38 (M1) |
| AC-21 (mechanism) | T5-2, T5-3, T29-2 | Read vs `spec:1093-1096` | Honored |
| AC-22 | T38-26 | Read `plan:6428-6442` vs `spec:1097-1100` | Honored |
| AC-23, AC-24 | T30-2, T34-1, T35-1, T35-2; T30-1 | Read `plan:5879-5889`, `plan:6048-6088`, `plan:5862-5877` vs `spec:1101-1113` | AC-23 honored; AC-24 honored in scope, but the regret row has no home in the schema (M4) |
| Deferred per spec §14 phasing: AC-2a-i deny-half, AC-2a-ii, AC-2b, AC-2c skill under-fire, AC-16, AC-21 full, AC-25 | — | `plan:6565-6570`, `plan:6586`, `plan:6591`, `plan:6595` vs `spec:1122-1130` | Correctly deferred |

**Architecture decisions** (honored / violated; method = Read of the cited ranges):

| AD | Verdict | Evidence |
|---|---|---|
| AD-1, AD-7, AD-8 | Honored | `plan:2589-2622` pipeline order; exit 0 always |
| AD-2, AD-25, AD-26 | Honored | `plan:701-712` pins (`web-tree-sitter` 0.26.13, `tree-sitter-wasms` 0.1.13, `engines >=22.16.0`) = `arch:138`, `arch:327-328`; `plan:879-900`; `plan:2727-2735` |
| AD-3 | Honored | `plan:992-1017`; D-plan-15 |
| AD-4, AD-5 | Honored | `plan:1195-1292` and `plan:1345-1360` re-read row by row against `arch:424-534`, `arch:572-621` (see the M11 closure) |
| AD-6, AD-20 | Honored | `plan:2803-2827`; the marker is the documented `command` field (`arch:1385-1386` asks for a marker on each entry; the current hooks page lists no unknown-field tolerance) |
| AD-9 | **Honored — the safe skeleton, verified line by line.** Question recognizer `plan:2220-2226` = `arch:750-760`; clear recognizer `plan:2227-2240` = `arch:778-786` (the sentence-level discard realises "not a recognized content-free deferral" with no positional narrowing); move recognizer `plan:2241-2243` = `arch:793-797`; intake `plan:2352-2355` = `arch:747-760`; catch-up `plan:2356-2369` = `arch:762-788`; deny decision `plan:2370-2377` = `arch:790-810`; hold `plan:2379-2388` = `arch:812-826`; detectors `plan:2441-2464` = `arch:819-826`, `arch:841-850`; lifetime and backstop `plan:2501-2518` = `arch:828-839`, `arch:852-867`; seam `plan:2165-2185` = `arch:869-879`. No question-type classifier, no Bash classifier, no per-question clear matcher, no lag estimator. Additions: `denyFired` (D-plan-20). | as cited |
| AD-10 | Honored in design | `plan:2287-2308`; the brand's "cannot be written" claim is overstated (m6) |
| AD-11, AD-12, AD-13, AD-14, AD-15, AD-16 | Honored | Steps 21, 14/15, 13, 16, 17/18, 20 vs `arch:947-1211` |
| AD-17 | Honored | every named detector now has a step and an induction (`plan:1727-1730`, `plan:2594-2596`, `plan:2612-2615`, `plan:2965-2970`) |
| AD-18 | Honored in scope | `plan:2737-2751` = `arch:1301-1316`; storage of the regret row undefined in both (M4) |
| AD-19, AD-21, AD-22, AD-23 | Honored | Steps 11/19; 5/29/36 (the scrub narrowed to the executed set, recorded in §16 item 5); §2.2; Step 29 — except that Step 16's per-event `HEAD` comparison is outside AD-23's inventory as written (M2) |
| AD-24 | Honored in content; tier ordering still not executable at Checkpoint 3 (S1) | `plan:3195-3246`, `plan:3474-3481` |

---

## Critical & Serious Findings

No Critical findings — the full inventory was Read or Grep-verified per Compliance Gate B, and no violation of Critical classification was observed (the Step 1 build itself now compiles, executed here).

### S1 — Step 28's handler consumes `hook/guard.ts` and `hook/watchdog.ts` (created at Step 29) and spawns the `hook integrity-check` verb (registered at Step 32); Checkpoint 3 and the four Step 28 replays are not executable where placed, and the §7/§9 attestations that every Verification names only runnable tests are false — the `integrity-check` half is a **REGRESSION (introduced by the round-2 corrections)**

**Class:** topological inversion between step artifacts and step verifications; false attestation (the round-2 S1 class at sites S1 did not name).
**Location:** `docs/plans/plan-phase-a.md:2589-2591` (handler pipeline items 1–2: "Guard (`CTXORACLE_INTERNAL` set → exit 0; Step 29). Watchdog start (Step 29)."), `docs/plans/plan-phase-a.md:2568-2575` (`hook <event> [--deadline-ms <n>]` "overrides the watchdog deadline (Step 29)"), `docs/plans/plan-phase-a.md:2598-2599` ("detached integrity child (`hook integrity-check`, Step 32, via the wrapper)"), `docs/plans/plan-phase-a.md:2652` (Step 28 Dependencies: 6, 9, 10, 14, 16–20, 24–27 — no 29, no 32), `docs/plans/plan-phase-a.md:2654-2663` (Verification: T28-1, T28-3, T28-4, T28-5 replay "through the built binary"; "**Checkpoint 3** runs here"), `docs/plans/plan-phase-a.md:2671-2684` (Step 29 "Create `src/hook/watchdog.ts` … Create `src/hook/guard.ts`"), `docs/plans/plan-phase-a.md:2711` (Step 29 depends on 6, 28), `docs/plans/plan-phase-a.md:2890-2894` (Step 32 creates `src/cli/integrity_check.ts`), `docs/plans/plan-phase-a.md:680-683` ("every Verification field names only tests runnable at its step"), `docs/plans/plan-phase-a.md:3454-3455` ("Each checkpoint names the tests that are *runnable* at that point"), `docs/plans/plan-phase-a.md:3474-3481` (Checkpoint 3).

**What the plan says.** The handler built at Step 28 imports two modules that Step 29 creates, and the `hook` verb's `--deadline-ms` is routed to a watchdog that does not exist yet. `tsc -p` at Step 28 therefore fails on unresolved imports (`hook/guard.js`, `hook/watchdog.js`), so none of T28-1/T28-3/T28-4/T28-5 can spawn the binary and Checkpoint 3 cannot run. Separately, the `SessionStart` branch spawns `dispatch.js hook integrity-check`, a verb Step 32 registers four steps later; at Step 28 the child exits on an unknown verb (fire-and-forget, so silently), and T28-5 and Checkpoint 3 exercise a `SessionStart` whose integrity path does not exist.

**How verified.** Read of every cited line this session; grep `Guard (|Watchdog start` over the `149ffc8` revision → the same two items at r2:2276–2277 (the guard/watchdog inversion pre-existed and was not reported — **new**); grep `integrity-check` over `149ffc8` → 0 hits (the verb reference is new text from the round-2 M8 correction — **REGRESSION**).

**Standard violated.** expert-plan Step 8 ("**Dependencies** — what must complete before this step"; "**Verification** — how the implementer will confirm this step is correct *after building it*"); Step 10 (a checkpoint "is a specific verification of the accumulated state at that point"); the plan's own §7 rule (`plan:664-665`, "a step's `Dependencies` field names every earlier step it consumes, and no step names a later one" — Step 28 consumes Steps 29 and 32 and names neither, because naming them would break the sort); Gate C ("File paths … confirmed").

**Required change.** Move `src/hook/guard.ts` and `src/hook/watchdog.ts` (and T29-3) into the substrate before Step 28 — they depend only on Step 6 — or into Step 28 itself, and re-derive Step 29 as the replay-level verification of the built handler; create `src/cli/integrity_check.ts` and register `hook integrity-check` in Step 28 (it depends only on Step 3's `integrityCheck()` and Step 10's writer) or make Step 28's `SessionStart` branch spawn nothing until Step 32 adds it, stating which; then re-derive Step 28's Dependencies, §7's and §9's attestations, and Checkpoint 3 from the corrected order.

---

### S2 — Step 14's `refreshIfStale` spawns "a detached `ctxoracle index` child" whose command is unspecified and whose binary (Step 28) and verb (Step 32) do not exist at Step 14; T14-2 asserts "spawns exactly one detached reindex" with no stated observation mechanism, so it cannot be built at the step that names it — **REGRESSION (introduced by the round-2 corrections)**

**Class:** unbuildable-as-written test; deferred choice; topological inversion.
**Location:** `docs/plans/plan-phase-a.md:1727-1734` ("spawns a detached `ctxoracle index` child through the spawn wrapper (Step 5)" — no command, no path to the entry file), `docs/plans/plan-phase-a.md:1762-1768` (Step 14 Verification names T14-2), `docs/plans/plan-phase-a.md:5269-5280` (T14-2: "real detached child through the spawn wrapper"; "**Fails when** the stale call records no `index_stale` fault, OR spawns zero or two children, OR the fresh call records a fault or spawns a child"), `docs/plans/plan-phase-a.md:326` (`cli/dispatch.ts` — Step 28), `docs/plans/plan-phase-a.md:2888-2889` (`index [--full]` verb — Step 32), `docs/plans/plan-phase-a.md:1033-1034` (Step 5: "the detached reindex and integrity child (Steps 14, 32) … go through it").

**What the plan says.** At Step 14 there is no `dist/src/cli/dispatch.js` and no `index` verb, so a "real detached child" has nothing to run; the indexer module also has no stated way to locate the entry file (`process.argv[1]` is the test file in a unit test and `dispatch.js` only in the handler process). The test's failure clause counts spawned children but names no observable through which a test could count them (the lock file the child would create cannot exist without the child; a spy on the wrapper would be the interaction assertion testing-standards forbids unless declared and justified).

**How verified.** Read of the cited lines; grep `^\*\*T14-` over `149ffc8` → only T14-1 (T14-2 is new text from the round-2 S2/M-5 correction); r2:1474–1484 shows the same spawn sentence without a command, so the missing command pre-existed, but the test that depends on it is new.

**Standard violated.** expert-plan Step 8 ("Name the file paths. Name the functions."; no choice deferred to implementation); Step 9 / output-contract §12 (the failure condition must be observable: "what observable outcome makes this test fail"); Gate C ("File paths … confirmed against the current codebase, not assumed").

**Required change.** State the spawn command (`process.execPath`, the real path of `dist/src/cli/dispatch.js` resolved from `import.meta.url` inside `indexer.ts`, `index`), make the reindex-spawning half of `refreshIfStale` a Step 28-or-later concern (or inject the spawn as a parameter the handler supplies), and re-specify T14-2 so its spawn assertion has a real observable — the `.reindex.lock` file written by a real child, at a step where the child exists — or split it: `index_stale` fault at Step 14, spawn count at Step 28/32.

---

### S3 — T3-3's stated timeline cannot produce its asserted outcome: under `busy_timeout` 100 ms with retry-once, child C (issued at t = 230 ms while A holds until 250 ms) acquires the lock on its first attempt and never raises `StoreBusy` — **REGRESSION (introduced by the round-2 corrections)**

**Class:** test that fails on a correct implementation (the failure condition contradicts the engine's behaviour).
**Location:** `docs/plans/plan-phase-a.md:4970-4977` ("child A holds `BEGIN IMMEDIATE` from t = 0 for 250 ms; child B issues its write at t = 120 ms … child C issues its write at t = 230 ms and is still contended at 330 ms, so both of its attempts hit the busy timeout and it raises `StoreBusy`"), `docs/plans/plan-phase-a.md:4979-4981` ("**Fails when** … the third write does not raise `StoreBusy`"), `docs/plans/plan-phase-a.md:881-888` (`busy_timeout=100`, retry once).

**What the plan says.** C's first attempt starts at 230 ms with a 100 ms busy window; A commits at 250 ms and B's retry (a single write) commits within milliseconds; C acquires the lock inside its first window. "Still contended at 330 ms" requires a writer holding the lock from 250 to 330 ms, and the timeline names none.

**How verified.** Executed in the layout reproduction on Node v22.22.2 with `node:sqlite` (`DatabaseSync(path, {timeout: 100})`, WAL): A `BEGIN IMMEDIATE` at t=0, `COMMIT` at t=250; B `BEGIN IMMEDIATE`+`INSERT`+`COMMIT` at t=120 with one retry on `SQLITE_BUSY`; C the same at t=230 — three runs, identical: `B t=222 busy attempts=1`, `A t=250 committed`, `B t=259 success attempts=2`, `C t=265 success attempts=1`; rows `[B, C]`. Provenance: r2:4208–4224 stated no offsets ("a third write is issued while B is in its retry window"); the offsets are the round-2 M6 correction.

**Standard violated.** testing-standards core discipline ("Every test must be able to fail, and the specification states what makes it fail" — here the stated failure fires on correct behaviour) and anti-pattern 5 in reverse (an expectation not derivable from the mechanism); expert-plan Step 9 item 5 (the failure condition must be the observable outcome of broken behaviour).

**Required change.** Make C's contention real: have B hold its transaction (e.g. B acquires at ~250 ms and holds 250 ms) so C's two windows (230–330, 330–430) both find the lock held; or issue C at a time that overlaps A and B's combined hold and derive the outcomes from the arithmetic; state the hold durations for every child and re-derive the "Fails when" clause. The plan's §11.4 should carry the executed timeline, not the arithmetic alone.

---

## Systemic Patterns

### SY-1 — The adapter-isolation convention test (T28-2) forbids every input field name in `dist/src/**` outside `adapter.js`, but the plan's own steps require `error`, `prompt`, `source`, `cwd`, and now `transcript_path` as identifiers in other modules, so a plan-conformant build fails `npm test` from Step 28 onward — the round-2 SY1 partially closed (recurring); the `transcript_path` liveness-row key is a **REGRESSION (introduced by the round-2 corrections)**

**Proactive scan.** grep over the plan for each of the Step 28 input field names as identifiers a step mandates in a non-adapter module — read at each hit:
- `cwd`: `docs/plans/plan-phase-a.md:1020-1021` (Step 5, `oracleSpawn(cmd, args, {cwd, env?, detached?, scrub?})` — an option key in `util/spawn.js`); `docs/plans/plan-phase-a.md:1118` (`InternalEvent.cwd`, read by the handler).
- `source`: `docs/plans/plan-phase-a.md:1353-1354` (Step 8 DDL, `tuning.source` — a column named in the migration SQL and in `stores/dao/tuning.js`); `docs/plans/plan-phase-a.md:1566-1568` (`set(store, key, value, source)`, `addToList(…, source)`); `docs/plans/plan-phase-a.md:1225-1226` (`test_map.source`); `docs/plans/plan-phase-a.md:2050` (`reconcileDedupOnSessionStart(store, consumer, source)` in `hook/delivery.js`); `docs/plans/plan-phase-a.md:2501-2502` (`handleSessionStart(…, source)` in `blocks/answer_drift.js`); `docs/plans/plan-phase-a.md:1117` (`InternalEvent.source?`).
- `prompt`: `docs/plans/plan-phase-a.md:1117` (`InternalEvent.prompt?`, read by the handler to call `intakeFromPrompt`); `docs/plans/plan-phase-a.md:3099-3100` (Step 36, `ModelInvocationRequest { prompt: string; … }`).
- `error`: `docs/plans/plan-phase-a.md:1117` (`InternalEvent.error?`); every `catch (error)` / `new Error(...)` the modules necessarily contain (Step 2 `plan:840-841` throws an `Error`).
- `transcript_path`: `docs/plans/plan-phase-a.md:2594-2596` (Step 28 item 4, the liveness row's `detail_json = {transcript_path, transcript_bytes}` — written by `hook/handler.js`, not the adapter); `docs/plans/plan-phase-a.md:2965-2970` (Step 33's detector reads "the transcript file the row names" from that `detail_json` in `diag/status.js`); `docs/plans/plan-phase-a.md:5813-5817` (T28-5 "**Fails when** … its `detail_json` lacks the path").
- The test: `docs/plans/plan-phase-a.md:2624-2628` ("built-output grep over `dist/src/**` outside `dist/src/hook/adapter.js` for every input and response field name listed above; no match permitted"), `docs/plans/plan-phase-a.md:5744-5753` (T28-2 "**Fails when** the clean build leaks").

Instances: 5 field names, ≥ 10 mandated occurrences outside `adapter.js`. Type-only members (`InternalEvent`) are erased, but their *uses* (`ev.prompt`, `ev.source`, `ev.cwd`, `ev.error`) are not; `tuning.source` is a literal in a `.sql` file loaded at runtime and in the DAO's prepared statements; `transcript_path` is a literal object key the handler writes.

**How verified.** Read of every cited line this session; the `149ffc8` revision's Step 28 list (r2:2262–2270) already contained `error`, `prompt`, `source`, `cwd` (the input-field collision was present then and was not the instance round 2 named — round 2's SY1 named the response fields and the DAO allow-list); the liveness row and its `transcript_path` key are absent from `149ffc8` (grep `liveness|transcript_bytes` → r2:1481 only, the pid-liveness sentence).

**Named standard.** output-contract Gate A ("Can a reviewer check a build against this and reach a defensible conclusion … including whether each test was built to its specification?") — a conformant build fails the test; testing-standards core discipline (the failure condition is met by every correct build); Gate A's own requirement in round 2's "what correct looks like": every module a step creates under the scanned path is either permitted by the convention or the step is changed. The author's closure (`2026-09-07-round-3-author-gates-review.md:43`) re-read only the response fields and the DAO allow-list.

**What correct looks like (the standard's requirement, not a patch).** AD-6's structural property is that one file *parses the hook JSON and emits the hook response* — a property of the import graph and of where the wire format is constructed, not of English words. The convention test must state exactly that property in a form a conformant build satisfies: either the scan is restricted to the names that are unambiguous wire identifiers (`hook_event_name`, `session_id`, `agent_id`, `agent_type`, `tool_name`, `tool_input`, `tool_response`, `agent_transcript_path`, `last_assistant_message`, `stop_hook_active`, `hookSpecificOutput`, `hookEventName`, `permissionDecision`, `permissionDecisionReason`, `additionalContext`) with the common-word names (`error`, `prompt`, `source`, `cwd`, `transcript_path`) excluded and the reason stated, or the internal event type renames those members (`promptText`, `startSource`, `workingDir`, `transcriptPath` — the last already exists at `plan:1117`), the liveness row's key is renamed (`transcriptPath`), and every step that uses the words is re-derived. The specification must then be checked against every §5.1 module by enumeration, as round 2 asked.

**No other systemic pattern** was found. Scans run: every `**Dependencies.**` field against the step order (40/40 name earlier steps only — the S1 and S2 inversions are *unnamed* consumptions, not later-step citations); every `File.` path in §12 against §5.1 (122/122 present); every `T<n>-<m>` reference against the definitions (0 undefined); the six convention tests' allow/deny sets against every module §5.1 places in their scope — T3-2 (only `stores/adapter.ts` may import `node:sqlite`: no other step names the module — pass), T5-3 (only `util/spawn.ts` imports `child_process`: Step 1's generator uses it directly but lives under `test/**`, outside `dist/src/**` — pass), T10-3 (importers of `faults.js` and `session_log.js`: every writer in Steps 10, 25–30, 33 goes through `recordFault`/`writeSessionEvent` or is a listed reader — pass), T24-2 (importers of `blocks/verdict.js`: only `blocks/answer_drift.js` — pass, provided the handler does not import it for the undefined `verdict.emit`, m3), T32-3 (no network module or `fetch(` under `dist/src/**`: no step imports one — pass), T36-1 (no importer of `model/invoke.js` — pass); narration vocabulary (0 narrative hits); deferral vocabulary over §7 (`either … or|one of the following|alternatively|if needed|as appropriate|TBD` → 0 hits; the deferred choices below were found by Read).

---

## Moderate & Minor Findings

### Moderate

**M1 — Step 1's `cold-container` CI job executes `scripts/check-cold-container.sh`, a file Step 38 creates, so the job is red on every pull request from Step 1 through Step 37; and the job's network policy is stated two incompatible ways — REGRESSION (introduced by the round-2 corrections).**
Location: `docs/plans/plan-phase-a.md:759-766` (Step 1: "`cold-container` runs in a fresh `node:22.16.0-bookworm-slim` container image with no cache restore, executes `scripts/check-cold-container.sh` (Step 38, `T38-25`)"), `docs/plans/plan-phase-a.md:321` (§5.1: the script is "Step 38"), `docs/plans/plan-phase-a.md:3210-3214` (Step 38: the job runs the script "with only the registry reachable"), `docs/plans/plan-phase-a.md:6416-6423` (T38-25: "A fresh container with only the harness's network access"). Step 1 explicitly defers the replay tier to Step 28 for exactly this reason (`plan:766-769`) and does not do the same for the cold-container job. "Only the registry reachable" is a network policy a GitHub Actions container job does not provide by itself and no step implements; Step 1 says only "no cache restore". Verified by Read; grep `cold-container` over `149ffc8` → no job (the job is the round-2 M7/m-6 correction). Standard: expert-plan Step 8 Verification ("What to run … what the expected output … looks like" — a job whose script does not exist has no expected output); Gate C (paths confirmed; no deferred choice on the network policy). Required change: add the job at Step 38 (or gate it on the script's existence and say so), and state the network policy once, as something the workflow can implement (the harness's default network, with the no-install-script assertion carried by the `npm ci --loglevel` capture T1-1/T38-25 already specify).

**M2 — Step 16's `passesBar(candidate: Candidate, tuning: TuningReader)` must reduce confidence "whenever `schema_meta.index_head` differs from `HEAD`", but neither parameter carries that fact, `Candidate` (Step 6) has no staleness member, and reading `HEAD` per event is outside AD-23's inventory; T16-1's stale-index case is not buildable against the stated signature — REGRESSION (introduced by the round-2 corrections).**
Location: `docs/plans/plan-phase-a.md:1822-1831` (signature; "by `bar.stale_index_factor` whenever `schema_meta.index_head` differs from `HEAD`"), `docs/plans/plan-phase-a.md:1120-1124` (`Candidate` members: `genre`, `subjectKey`, `factClass`, `pointers`, `support?`, `ratio?`, `lastTs?`, `hazard`, `headline`, `evidenceJson`), `docs/plans/plan-phase-a.md:5314-5317` (T16-1 "the same candidate with `schema_meta.index_head` equal to and different from `HEAD`"), `docs/plans/plan-phase-a.md:5322-5323` ("**Fails when** … the stale-index case is not reduced by the seeded factor"); `arch:1490-1492` (the `HEAD` read is a "`SessionStart`-only" item of AD-23's inventory). Verified by Read; grep `stale_index|index_head` over `149ffc8` → Step 14 only (the comparison in Step 16 and the T16-1 case are the round-2 m6 correction). Standard: expert-plan Step 8 ("Name the functions. Name the types." — the signature cannot deliver the behaviour, the round-2 m11 class); AD-23 fidelity. Required change: carry staleness as an input the handler computes once at `SessionStart` and persists (e.g. a `schema_meta.index_stale` flag written by `refreshIfStale`, read by the handler into `EventContext`), add the member to `Candidate` or a third `passesBar` parameter, and re-derive T16-1's data from that input.

**M3 — §10 attests that "Every decision below was reasoned through the Clear Thought MCP server … its conclusions and reasoning are what appears here", but the trace file supplied for round-2 T1 records conclusions the current decisions contradict (D-plan-6, D-plan-8, D-plan-10) and contains no trace for D-plan-20…23 (nor D-plan-4, 5, 13, 15–19); §11.4's count ("43 `sequential_thinking` thoughts") does not match the file (46) — REGRESSION (introduced by the round-2 corrections).**
Location: `docs/plans/plan-phase-a.md:3514-3517` (the attestation), `docs/plans/plan-phase-a.md:4768-4787` (§11.4 entry: "43 `sequential_thinking` thoughts and 5 scored `decision_framework` evaluations"; the trace file is not cited anywhere in the plan — grep `plan-tool-traces` → 0 hits), `docs/plans/plan-phase-a.md:3606-3626` (D-plan-6: interpreter + real path of `dispatch.js`) vs `docs/reviews/2026-09-07-plan-tool-traces.md:1412-1470` (D4 conclusion: pattern `(^|/|\\)ctxoracle hook <event-name>$` — the basename form the plan now rejects), `docs/plans/plan-phase-a.md:3669-3691` (D-plan-8: the six-variable scrub) vs `docs/reviews/2026-09-07-plan-tool-traces.md:1574-1605` (D5 option C: "environment scrubbed of CLAUDE_* and ANTHROPIC_* variables"), `docs/plans/plan-phase-a.md:3704-3733` (D-plan-10: two named repositories, per-event prefix, labelled sample) vs `docs/reviews/2026-09-07-plan-tool-traces.md:1277-1335` (D3: "the candidate set the implementer draws at least two from"; no prefix, no sample), `docs/plans/plan-phase-a.md:3847-3885` (D-plan-20…23, no trace label exists for them: the file's labels are D1, D2, D2b, D3–D10). Verified by grep of every `[D<n> <topic>` label and Read of the cited trace lines; `sequential_thinking","prompt"` → 46, `decision_framework","prompt"` → 7 (two invalid + five valid, per the file's own NOTE at line 726 — the "5 scored" count holds). Standard: expert-plan Step 6 ("Clear Thought is mandatory for every plan … The conclusion AND the reasoning that led to it both go into the plan's Decisions section"); Gate C ("Every entry in Output section 11 carries read-level evidence" — the §11.4 entry's evidence for the revised decisions is a trace that says something else); CLAUDE.md rule 1 (verify before you assert). Provenance: the attestation existed at `149ffc8`; the revised and added decisions, and the trace file that exposes the mismatch, are the round-2 corrections. Required change: run the revised decisions and D-plan-20…23 through the tool and append the trace (the file is written-once per CLAUDE.md, so a second dated trace file), cite the trace files from §11.4 with the counts as they are, and restate the attestation to cover exactly the decisions the traces cover.

**M4 — The regret proxy "records a regret row" that `status` renders as a rate and splits by `never_triggered`/held, but no table, DAO, or column in the plan holds it; T30-1 cannot be built without a schema decision the plan does not make.**
Location: `docs/plans/plan-phase-a.md:2737-2751` ("A regret row names the fact, the churn, and whether a candidate existed (`held_below_bar`, `held_dedup`, `never_triggered`) so `status` can show the split"), `docs/plans/plan-phase-a.md:1195-1292` (Step 7 DDL — no regret table; grep `CREATE TABLE regret|regret table` → 0 hits), `docs/plans/plan-phase-a.md:1388-1410` (DAO table — no regret DAO), `docs/plans/plan-phase-a.md:2957-2959` (`status` renders "regret rate"), `docs/plans/plan-phase-a.md:3349-3350` (the report's regret rate "split by `never_triggered` / held"), `docs/plans/plan-phase-a.md:5862-5877` (T30-1 "**Fails when** any of the three TP cases records no regret row, OR the never-triggered row is not labelled"); `arch:1301-1316` (AD-18 says "a regret row is recorded" and names no table either — a plan-level decision). Verified by Read and grep; present at `149ffc8` too (r2:2409 "a regret row is recorded") — unreported, **new**. Standard: expert-plan Step 8 ("Name the file paths. Name the functions. Name the types."); Gate C (no deferred choice); AD-4's table-creation criterion (a Phase A writer needs a Phase A table in migration 001). Required change: add the `regret` table to Step 7's DDL (fact kind/ref, session, churn kind, candidate state label, `PROV`), its DAO to Step 9, its writer to Step 30, and its reader to Step 33; add its negatives to T7-1 and T9-1.

**M5 — Leg 2's agent-driven sessions are "a Claude Code Remote session created on a fresh clone", whose `~/.ctxoracle` stores live in that session's container; D-plan-10's claim that agent-driven sessions keep "the stores that feed the report on the report machine" is false for that path, and no step moves those stores to the report — the round-2 M-7 gap re-opened for the remote path — REGRESSION (introduced by the round-2 corrections).**
Location: `docs/plans/plan-phase-a.md:3292-3296` ("a Claude Code Remote session created on a fresh clone of the repository, or a local `claude` session where the agent runs locally"), `docs/plans/plan-phase-a.md:3310-3314` (only Max Cogar's sessions get the `export`/`import` transfer), `docs/plans/plan-phase-a.md:3379-3382` (D-plan-10 Gate 3 part 3: "making the sessions agent-driven by construction keeps the exit run's data on the machine that writes the report"), `docs/plans/plan-phase-a.md:3729-3731` ("Leg 2 is agent-driven so the stores that feed the report are on the report machine"), `docs/plans/plan-phase-a.md:3397-3399` (Checkpoint 5: "`ctxoracle status` on each imported or agent-driven leg-2 store"). A remote session is a separate container (§15 G3 states the same fact about transcripts: "a remote session's container holds only its own transcript", `plan:7018-7023`); its store is reachable from the report only by the same `export` + hand-over the plan reserves for the owner's sessions. Verified by Read; the remote-session sentence is the round-2 M-7/m9 correction (round 2's leg 2 was owner-driven). Standard: Gate A/C (an implementer must invent the transfer); CLAUDE.md rule 1 (a report that cannot read its inputs would report zero or fail loudly at Checkpoint 5 — the second is likelier, but the plan should not leave it to chance). Required change: state that every agent-driven session ends with `ctxoracle export <dir>` and a transfer of the directory to the report machine (the same mechanism as Max Cogar's path), and that the report's `import` step folds them; correct D-plan-10's sentence.

### Minor

**m1 — §7 says "The trivial steps are 6, 8, and 40" while Step 6 is written in the full four-part format.** `docs/plans/plan-phase-a.md:688-689` vs `docs/plans/plan-phase-a.md:1143-1166`. Verified by Read; identical at `149ffc8` (r2:668 and r2:1040–1060) — unreported, **new**. Standard: Gate C (internal reconciliation; output-contract §7's trivial/non-trivial marking). Required change: name 8 and 40 only.

**m2 — Step 14's `LanguageFrontend` interface names `Symbol[]` while Step 6 defines `SymbolRow`; in TypeScript `Symbol` resolves to the global ES `Symbol` type, so the interface compiles to the wrong contract (recurring — round-2 M3 partially closed).** `docs/plans/plan-phase-a.md:1706` vs `docs/plans/plan-phase-a.md:1125-1126`. Verified by Read; `Symbol\[\]` present at `149ffc8` (r2:1455). Standard: Gate C (types confirmed). Required change: `SymbolRow[]`.

**m3 — Step 28's pipeline item 7 calls "`verdict.emit`", which nothing defines: Step 24 exports the `DenyVerdict` type and `makeDenyVerdict` only; if the handler imports `blocks/verdict.js` for it, T24-2 (exactly one importer) fails.** `docs/plans/plan-phase-a.md:2603-2604` vs `docs/plans/plan-phase-a.md:2287-2292`, `docs/plans/plan-phase-a.md:5624-5634`. Verified by Read; present at `149ffc8` (r2:2287) — unreported, **new**. Standard: expert-plan Step 8 ("Name the functions"). Required change: "on a verdict → `adapter.toHookResponse({deny: verdict})`, diagnostics, exit 0".

**m4 — Step 28 and Checkpoint 3 say "the three replay tests above" / "the three Step 28 replays" while Step 28 names four (T28-1, T28-3, T28-4, T28-5) and §5.1 lists four replay files for Step 28 — REGRESSION (introduced by the round-2 corrections).** `docs/plans/plan-phase-a.md:2661-2663`, `docs/plans/plan-phase-a.md:3476-3478` vs `docs/plans/plan-phase-a.md:2654-2661`, `docs/plans/plan-phase-a.md:500-503`. Verified by Read; grep `three replay|replay tests above` over `149ffc8` → 0 hits (T28-4/T28-5 and the sentence are the round-2 S2 correction). Standard: Gate C (reconciliation). Required change: "four".

**m5 — §14.4's sweep record does not establish its own terminating condition: pass 3 "added Q33–Q42", and the only zero-addition pass recorded (pass 2) precedes them; "the final read added zero entries" is not a recorded pass — REGRESSION (introduced by the round-2 corrections).** `docs/plans/plan-phase-a.md:6962-6981`. Verified by Read; the `149ffc8` record had two passes (r2:5856–5880). Standard: expert-plan Step 12 item 3 ("The sweep is complete only when an entire pass adds zero new register entries") and output-contract §14 ("number of passes performed, confirmation the final pass added zero entries"). Required change: record the fourth full pass (or run it) and state the count as four.

**m6 — Step 24 claims "a `DenyVerdict` literal cannot be written outside this file", but a type assertion (`{…} as DenyVerdict`) compiles; only an annotated construction fails, and T24-3's fixture form ("returning `{…}` as a `DenyVerdict`") is ambiguous between the two — REGRESSION (introduced by the round-2 corrections).** `docs/plans/plan-phase-a.md:2288-2292`, `docs/plans/plan-phase-a.md:5636-5647`. Verified by execution in the layout reproduction (typescript 5.9.3, the Step 1 command): `export const v = { kind:'deny', reason:'x', audit_id:'y' } as DenyVerdict;` → exit 0; `export function f(): DenyVerdict { return { kind:'deny', reason:'x', audit_id:'y' }; }` → `TS2741: Property '[denyBrand]' is missing`, exit 2. The brand is the round-2 SY1 correction. Standard: CLAUDE.md rule 1 (verify before you assert); Gate C (the fixture's form is a deferred choice). Required change: state the property truthfully (the brand blocks *annotated* construction; the importer scan T24-2 is the structural guard) and fix T24-3's fixture as an annotated return.

**m7 — `deinit` removes the settings file "when … the file now holds nothing but the empty structure `init` wrote" (`{}`), but after the eight entries are removed the file holds `{"hooks": {}}` (or empty event arrays) unless `deinit` also prunes the emptied `hooks` object, which no sentence says — REGRESSION (introduced by the round-2 corrections).** `docs/plans/plan-phase-a.md:2881-2887` vs `docs/plans/plan-phase-a.md:2816-2819` ("creates it (`{}` before editing)"), T32-1 `docs/plans/plan-phase-a.md:5943-5951` (case (b) must leave no file). Verified by Read; the file mechanics are the round-2 M5 correction. Standard: Gate C (deferred choice). Required change: state that `deinit` prunes an event array it emptied and the `hooks` object when it becomes empty, then compares the result to `{}`.

**m8 — T31-1's third invocation mode, "via `npx` from the package", names no command; §11.4's `npx --no-install ctxoracle` succeeded only because an earlier install had populated the npx cache, which a CI checkout does not have — REGRESSION (introduced by the round-2 corrections).** `docs/plans/plan-phase-a.md:5897-5901`, `docs/plans/plan-phase-a.md:4715-4717`. Verified by Read (the §11.4 entry's own argv1 path is `/root/.npm/_npx/…`). Standard: Gate C (no deferred choice). Required change: name the command (e.g. `npx --yes --package=<packed tarball path> ctxoracle init`, offline-capable) and the cache-population step.

**m9 — The sentence-level deferral discard (any sentence containing a deferral phrase is dropped whole; the turn clears on what survives) is a plan-level interpretation of AD-9's "not a recognized content-free deferral" recorded only as register answer Q41, not as a §10 decision.** `docs/plans/plan-phase-a.md:2227-2240`, `docs/plans/plan-phase-a.md:6932-6936`; §10 has no entry (D-plan-19 covers the negative tests, not the rule). `arch:781-782`. Verified by Read. Standard: output-contract §10 ("interpretations of how a standard applies to this situation"); the owner's fidelity criterion (every plan-level addition recorded in §10). Provenance: the `149ffc8` "opening clause" rule was equally unrecorded — the class recurs; the entry is **new**. Required change: a D-plan entry with the FR-B5/FR-B1 rationale.

**m10 — Several steps consume earlier steps their Dependencies field does not name, contrary to §7's own rule ("names every earlier step it consumes").** `docs/plans/plan-phase-a.md:664-665` (the rule); Step 25 depends on 12, 21, 22, 23, 24 (`plan:2418`) but appends `whisper_audit` rows (Step 9, `plan:2374-2375`) and records faults (Step 10, `plan:2360-2362`); Step 20 depends on 9 (`plan:2080`) but takes `Candidate` (Step 6, `plan:2047`); Step 19 depends on 9, 11, 18 (`plan:2033`) but takes `Candidate` and records `whisper_dropped_stale` (Step 6/10, `plan:2000-2011`). Verified by Read; the same at `149ffc8` (r2:2130) — unreported, **new**. Standard: expert-plan Step 8 ("**Dependencies** — what must complete before this step … State explicitly"). Required change: name the direct dependencies (transitive satisfaction is real, but the field's stated contract is explicit naming).

**m11 — Step 34 routes `--missed-question` text "through `recognizeQuestions` (Step 23) **without** the `?` requirement", but `recognizeQuestions(text, stoplist)` has no mode parameter; the signature cannot deliver the behaviour.** `docs/plans/plan-phase-a.md:3022-3024` vs `docs/plans/plan-phase-a.md:2220-2222`. Verified by Read and grep (`recognizeQuestions(` → one definition). Standard: expert-plan Step 8 (the round-2 m11 class). Required change: `recognizeQuestions(text, stoplist, {requireTerminalMark: boolean})` (default `true`), with T23-1 asserting the intake path never passes `false`.

---

## Tentative Findings

**T2 (carried) — Behaviour at the Node 22.16.0 floor** (`fs.readdirSync` recursive, `import.meta.resolve`, `node --test` glob handling, `node:sqlite` surface) is executed only on v22.22.2 (`plan:4603-4604`; Q28 `plan:6878-6882`); this reviewer's machine has only v22.22.2. Verification gap: an execution on a 22.16.0 binary — the plan assigns it to CI's matrix entry, which is honest.

**T3 — `unshare -rn` on the CI runner.** T32-2 and T38-22 run their verbs inside `unshare -rn` "available on the Linux CI runners — executed 2026-09-07, §11.4" (`plan:2905-2909`, `plan:5957-5961`, `plan:6382-6384`), but the execution was in this sandbox, Step 1's workflow names no runner image (`plan:759-766`), and the test handles only the case where `unshare` is *absent*, not the case where it is present and refuses (`EPERM` under a user-namespace restriction). Ubuntu 24.04 ships an AppArmor restriction on unprivileged user namespaces; whether GitHub's `ubuntu-24.04` image lifts it could not be checked from here. Verification gap: run `unshare -rn true` on the named runner image, then either name the image in Step 1 or add the `EPERM` branch to T32-2's "not executed" reporting.

No other tentative findings — every other finding's premise was verified per Gate B.

---

## Observations

- **Multi-perspective check (manual; Clear Thought unavailable).** Standards persona: eight of this round's findings are one shape — a correction that closed the *named* instance of a rule while leaving or creating a sibling instance (SY-1 input fields; S1's Step 32 verb; S2's binary; M1's script; M2's signature; M5's remote path; m7; m8) — the author's scripts cross-check IDs and paths, not what a module must contain or where a file must exist at a given step. Implementer persona: the first red run is T3-3 at Step 3 (S3); the next is the missing `guard.js`/`watchdog.js` at Step 28 (S1), then T28-2 at Step 28 (SY-1), with T14-2 at Step 14 un-runnable in between (S2). Owner persona: the answer-drift block is still the skeleton Max Cogar asked for (verified above), the exit measurement now has a real denominator, and every defect this round is build/test mechanics or record-keeping — the 2026-09-04 failure did not recur in Steps 21–27 or in Step 39's measurement design.
- The trace file's own NOTE (line 726) records that the first two `decision_framework` calls were mis-keyed and re-run — the file is honest about itself; the mismatch in M3 is between the file and the plan, not inside the file.
- The `149ffc8` reviewer's `bad.js` remains under `scratchpad/layoutprobe/dist/test/build/fixtures/` from before the `exclude` was added; this session's `tsc -p` did not touch it (mtime 16:15 vs the 16:5x run), which is consistent with the plan's §11.4 statement that the excluded directory is not emitted.

---

## What's Actually Good

- **Steps 21–27 are the AD-9 safe skeleton and nothing more.** Property: every recognizer, the intake, the catch-up, the deny decision, the hold, the four detectors, the lifetime rules and the Stop-time backstop transcribe `arch:747-879`; the two additions are recorded (D-plan-20) or are AD-9's rule applied (the deferral discard, m9 asks only that it be recorded); T23-1/T23-2/T18-8 assert the rule's complement over generated corpora (`plan:5565-5577`, `plan:5591-5593`, `plan:5456-5459`). Standard: spec §11.5 / D-41 ("a skeleton, not 'the block working'"); CLAUDE.md rule 3; collapse-log 2026-09-03 round 8 lesson 2. Verified by the line-by-line Read recorded in the AD-9 row above.
- **The exit measurement is now a measurement.** Property: leg 1 hands the handler a per-event transcript prefix (`plan:3272-3277`, matching `docs/IDEAS.md:94-97`'s "reconstructing … the `transcript_path` state"); the floor number has a labelled denominator under a rule independent of the recognizer, with the label table published (`plan:3326-3339`); the report keys marker presence by transcript origin and reports L11(a) as *verified* only on an owner-local interactive transcript (`plan:3278-3284`, `plan:3350-3353`, matching `arch:1602-1606`). Standard: collapse-log 2026-08-25 item 1 (a guard derived independently of what it guards); AD-24. Verified by Read.
- **Every hooks-contract premise the plan states holds against the live page today.** Property: the timeout clause, the entry fields, the `SessionStart` sources including `fork`, the `transcript_path` lag sentence, `UserPromptSubmit.prompt`, Stop's `stop_hook_active`/`last_assistant_message`, Stop's `hookSpecificOutput.additionalContext` with the eight-continuation cap, `SubagentStop`'s `agent_id`/`agent_type`/`agent_transcript_path`, `PostToolUseFailure`, the three settings locations — each quoted from `code.claude.com/docs/en/hooks` (fetched and `curl`ed 2026-09-07) and found as §3, §4, §11.4, Step 20, Step 21, Step 28 and Step 31 state them. Standard: expert-plan Step 4 / output-contract §11 documentation-read evidence; CLAUDE.md ("the hooks contract has drifted before"). Verified by WebFetch and grep of the fetched page.
- **The package pins and the runtime floor are exactly the architecture's.** Property: `web-tree-sitter` 0.26.13, `tree-sitter-wasms` 0.1.13, `engines >=22.16.0` (`plan:701-706`) = `arch:138`, `arch:327-328`; no version changed anywhere in the plan (grep `0\.27\.0` → 4 hits: `plan:224` §3, `plan:3551` D-plan-2, `plan:4635` §11.4, `plan:7063` §16 — every one names it as the current release the plan does not adopt; no pin, script, or manifest line carries it). Standard: the owner's fidelity criterion; AD-2/AD-25. Verified by Read and grep.
- **The must-fail fixture mechanics work as specified.** Property: with `"exclude": ["test/build/fixtures"]` the project build exits 0 and emits nothing for the fixtures; the exact per-fixture command Step 1 states (`tsc --noEmit --strict --target ES2022 --module NodeNext --moduleResolution NodeNext --verbatimModuleSyntax --types node <fixture>`) follows a fixture's `.js`-suffixed import into `src/` and reports the intended diagnostic (`TS2554`). Standard: testing-standards (a test that can fail); Gate C. Verified by execution in the layout reproduction.

---

## Convergence Record

- **Round number:** 3.
- **Trajectory (expert-review series):** R1: 10 (3 Serious, 5 Moderate, 2 Minor) → R2: 29 (1 Critical, 5 Serious, 1 Serious-Systemic, 11 Moderate, 11 Minor) → R3: 20 (1 Serious-Systemic, 3 Serious, 5 Moderate, 11 Minor). The collapse-hunt series' items (R1: 13, R2: 20) are counted in the closure tables, not in the trajectory.
- **Flow counts this round:** prior findings closed 47 of 49 (29 expert-review items: 27 closed, 2 partially closed and recurring — SY1 → SY-1, M3 → m2; 20 collapse-hunt items: 20 closed); recurring 2 (SY-1, m2); new 7 (S1, M4, m1, m3, m9, m10, m11); regressions 11 (S2, S3, M1, M2, M3, M5, m4, m5, m6, m7, m8) — plus regression *components* inside two findings counted elsewhere (SY-1's `transcript_path` key; S1's Step 32 verb reference).
- **Tripwire evaluation — not fired.** Condition (a), new + regression ≥ closed for two consecutive post-fix rounds: round 2 held (25 + 4 = 29 ≥ 10); round 3 does not (7 + 11 = 18 < 47). Condition (b), total not strictly decreasing for two consecutive post-fix rounds: round 2 held (10 → 29); round 3 does not (29 → 20, a strict decrease). Neither condition holds for two consecutive rounds. **Tripwire: not fired.** Arithmetic for round 4: it fires if round 4's new + regression ≥ its closed count, or if its total is ≥ 20.
- **For the owner's separate regression rule** (two consecutive rounds of correction-induced defects): round 2 reported 4 regressions (10 across both reviews); this round reports 11 regression-tagged findings plus two regression components. That is a second consecutive round of correction-induced defects; this record states the count and leaves the rule's consequence to the owner.

---

## Open Findings Ledger

Not applicable — no operator-directed cycle stop.

---

## Recommended Priority

1. **SY-1, S1, S3** — the three defects that make `npm test` red on a conformant build (T28-2's field list; the handler's missing modules and verb at Step 28; T3-3's timeline). Re-derive each from its standard (AD-6's structural property; the topological rule; the engine's busy-handler arithmetic, executed), not by editing the sentence the finding quotes.
2. **S2, M1, M2, M4** — the steps and tests that cannot be built where placed (T14-2's spawn; the cold-container job; the bar's staleness input; the regret table).
3. **M3, M5** — the record-keeping and data-locality defects in §10/§11 and Step 39, which decide whether the exit report can be written from its inputs and whether the plan's decisions are the reasoned ones.
4. **m1–m11**, then the two tentative items, which are CI-side executions.

---

## What could not be checked

- Behaviour at the Node 22.16.0 floor (T2).
- `unshare -rn` on the GitHub Actions runner image the plan does not name (T3).
- The Clear Thought server's own run beyond what the trace file records; the file was checked for content, not re-executed.
- The shipped `claude -p` scrub runs (`plan:4678-4694`) — outside this reviewer's permitted commands; closed on the plan's recorded execution, which names the command, the variable set, and the outputs.
- Whether a Claude Code Remote session can install hooks and start in the target repositories (R13 discloses the 2026-09-06 refusal; not reproducible from here).

---

Verdict: NEEDS FIXES (20 findings: 1 Serious-Systemic, 3 Serious, 5 Moderate, 11 Minor)
